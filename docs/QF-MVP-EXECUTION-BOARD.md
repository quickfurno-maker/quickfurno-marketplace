# QF-MVP — Execution Board

**Branch:** `mvp/qf-mvp-00-core-cleanup-v1` · **Base:** `a4d289a` · **Document type:** live execution board (update as phases progress)

Statuses used: `LOCKED` · `NOT_STARTED` · `IN_PROGRESS` · `BLOCKED` · `COMPLETE` · `DEFERRED`.

---

## 1. Program status
- **QF-MVP-00:** **COMPLETE**
- **QF-MVP-10:** **COMPLETE** (production SELECT-only reconciliation completed 22 July 2026)
- **QF-MVP-20:** **IN_PROGRESS**
- **Completed:** QF-MVP-20.0 — Authority Repair Design · QF-MVP-20.1 — Consumer and Call-Path Audit · QF-MVP-20.2A — Staging Schema Baseline Audit · QF-MVP-20.2B — Generate Reviewed Staging Baseline · QF-MVP-20.2C1 — Staging Application Preflight · QF-MVP-20.2C1R — Managed Function Parity Reconciliation
- **Completed (20.2 chain):** 20.2A · 20.2B · 20.2C1 · 20.2C1R · 20.2C2 · 20.2C2R · **20.3A — Remediation Migration Design** · **20.3A1 — Remediation Decision Closure** · **20.3A1R — Assignment Lineage Idempotency Correction**
- **Current task:** **QF-MVP-30 VENDOR CRM — COMPLETE** — **`QF_MVP_30_VENDOR_CRM_COMPLETE_READY_FOR_PRIVATE_SERVER_STAGING_DEPLOYMENT`** (branch `mvp/qf-mvp-30-vendor-crm-v1`). QF-MVP-30.1–30.5 complete; migrations through `20260728001600` applied and verified on authorized staging (count **17**); final real Next.js-over-HTTP application workflow smoke **56/56**. **No active frequency policy exists and campaign handoff stays fail-closed without one.** No Meta/n8n/provider transport in MVP-30. **Next: private server staging deployment (NOT_STARTED)**, then QF-MVP-40 → 50 → 70 → 60 → 80. Server deployment remained deferred throughout MVP-30 and has not occurred.
- **QF-MVP-20.3B1:** **APPLIED_ON_STAGING — VERIFICATION INCOMPLETE** (A + A2 + B1 all committed; verifier 57 PASS / 1 FAIL)
- **Migration A (`20260723000100`):** **APPLIED_IMMUTABLE** (`b6307094…`)
- **Migration A2 (`20260723000200`):** **APPLIED_IMMUTABLE** — created 0 rows (empty staging, as designed) (`9d77f446…`)
- **Migration B1 (`20260723000300`):** **APPLIED_IMMUTABLE** (`46ce7377…`) — applied at exit 0; its own §7 block ran to completion
- **Phase verifier:** **CORRECTED** (`b66ec060…`, was `688ab439…`) — carried the same defect in 6 rows; 2 would have failed post-apply
- **Phase validator:** **HARDENED — 126/126 PASS** (`734b6a13…`, was `4497a3c0…`) with fixtures A–G
- **Replay semantics:** **LOCKED** (normalized request fingerprint + persisted result; `idempotency_conflict` on key reuse)
- **Assignment credit cost:** **ONE_WALLET_CREDIT** (`ASSIGNMENT_CREDIT_COST = 1`, internal locked constant)
- **Client ownership:** **R1_REVIEW_REQUIRED** — `client_selected` mode is fail-closed, `R1_BLOCKED_PENDING_OWNER_BINDING`
- **Baseline validator:** **PASS** (external source `269c9265…` verified; the 20.3B1 `BLOCKED_EXTERNAL_EVIDENCE` status is closed)
- **QF-MVP-20.3B1P:** **COMPLETE**
- **QF-MVP-20.3B1A:** **`PARTIAL_APPLICATION_REQUIRES_REVIEW`** — applied: A, A2 · not applied: B1
- **QF-MVP-20.3B1R2:** **COMPLETE — correction proven** (rows 607 and 613 both passed post-apply; they would have failed uncorrected)
- **QF-MVP-20.3B1A2:** **`B1_APPLIED_VERIFICATION_FAILED_REQUIRES_REVIEW`**
- **Staging migration-history rows:** **FIVE** (baseline, A, A2, B1, G)
- **Phase verifier:** **62 PASS / 0 FAIL** — the lineage append-only boundary is now in force
- **Migration G (`20260723000400`):** **APPLIED_AND_VERIFIED_ON_STAGING** (`91544524…`) — forward-only, REVOKE-only lineage grant repair, applied 2026-07-23T15:12:59Z UTC, exit 0
- **Phase verifier:** **62 checks** (`e1d9edb8…`) — history now expects baseline + A + A2 + B1 + G
- **Phase validator:** **165/165 PASS** (`e27d62d0…`) with load-bearing fixtures A–J
- **Advisors:** **READ (staging only)** — security 44 lints, performance 233 lints; **none blocking**, none attributable to Migration G
- **Staging migrations applied:** **BASELINE + A + A2 + B1 + G** (all immutable)
- **Real `db push` (20.3B1G-A):** **exit 0** — applied only `20260723000400`, 2026-07-23T15:12:59Z UTC
- **Staging write to date:** **SCHEMA/ACL ONLY — zero application data ever written**
- **Migration repair:** **NOT_REQUIRED** · **Staging reset:** **NOT_REQUIRED**
- **Staging data:** **EMPTY** (67 tables, 0 rows; `auth.users` 0)
- **Pending staging migration:** **NONE** — A, A2, B1, G, B2, C, D and E are applied and immutable (E: 9 local / 9 remote, 21/21, proven ACL no-op). **QF-MVP-20.4A is design-only** — a SELECT-only production audit pack + validator + empty evidence-manifest schema; it creates **no migration** and accesses **no database**. Next is a separately founder-authorized production SELECT-only evidence audit
- **Runtime R1:** **NOT_STARTED** · **Migration B2:** **NOT_CREATED** · **Migration C:** **NOT_CREATED** · **Migration D:** **NOT_CREATED** · **Migration E:** **NOT_CREATED**
- **Implementation:** **NOT_STARTED**
- **Migration:** **NOT_CREATED**
- **Runtime implementation:** **NOT_STARTED**
- **Staging project:** **PROVISIONED** (ref `uckafzuochmbvtiodmcl`)
- **Staging baseline:** **APPLIED_AND_VERIFIED** (40/40 verification rows PASS)
- **Staging mutation to date:** **baseline + A + A2 schema only; zero application data ever written**
- **Staging tables:** **67** (62 baseline + 5 from A), all **0 rows**; **45** public functions; `auth.users` = 0; anon holds **no** table privileges
- **Production access:** **SELECT_ONLY** · **Production mutation:** **NONE**
- **Baseline SHA256:** `920a4aa0143b7c91231a3c83d01452e49b8b9a829c322f15c7df4fe9f07ecc81` (unchanged)
- **Verification SHA256:** `7ba9792f300119b7c1aa84a4c02394186116a507c9097bd6f95f23f55e504193` (unchanged)
- **Open prerequisite:** `AUTH_USERS_HANDLE_NEW_USER_TRIGGER`
- **Open production exposure (closed by Migrations C + E):** 4 blocker RPCs `anon`-executable; `anon` holds `INSERT/SELECT/UPDATE/DELETE/TRUNCATE` on `leads` and `vendors`; always-true `leads` INSERT policy
- **Production access (20.3B1P):** **NONE** · **QF-Jarvis access:** **NONE**
- **QF-MVP-20.3B1GP:** **COMPLETE** — dry-run proposed exactly one migration; G not applied at that time
- **QF-MVP-20.3B1G-A:** **COMPLETE** — one `db push --linked` (exit 0) applied **only** G; phase verifier **62 PASS / 0 FAIL**; advisors read, none blocking
- **QF-MVP-20.3R1:** **COMPLETE (code only)** — 5 consumers migrated to `qf_assign_lead_vendors_v2`, 5 client-selected paths fail-closed; **no migration, no database access, nothing pushed**
- **QF-MVP-20.3B2:** **GENERATED + REVIEWED, NOT APPLIED** — 4 functions + 4 triggers make active-3 / lifetime-6 / lineage-append-only universal
- **QF-MVP-20.3B2P:** **PREFLIGHT COMPLETE** — one dry run (exit 0) proposed **exactly** B2; **0 remote history rows created**; B2 still local-only, still unapplied
- **QF-MVP-20.3B2A:** **APPLICATION FAILED — CLEAN ROLLBACK** — the one real `db push` aborted at statement 20 (SQLSTATE 42883, `name[] = text[]`); B2 **not** recorded remotely; history stayed 6 local / 5 remote; no retry
- **QF-MVP-20.3B2R1:** **CORRECTION COMPLETE (offline)** — cast catalog `name` arrays to `text` at all 5 sites (migration §5.7, verifier B13/B15); validator rule **R23** added; semantics unchanged; B2 still unapplied
- **QF-MVP-20.3B2R1P:** **PREFLIGHT COMPLETE** — corrected B2 preflighted; dry run proposed exactly it; 0 remote rows
- **QF-MVP-20.3B2R1A:** **APPLIED + VERIFIED** — corrected B2 applied; 6 local / 6 remote; verifier 34/34; staging empty
- **QF-MVP-20.3CG:** **GENERATED + REVIEWED, NOT APPLIED** — vendor_public_v safe projection + revoke anon/PUBLIC on vendors/leads + drop 3 unsafe policies
- **QF-MVP-20.3CP:** **PREFLIGHT COMPLETE** — one dry run (exit 0) proposed exactly C; 0 remote rows
- **QF-MVP-20.3CA:** **C APPLIED (exit 0), VERIFIER 22/23** — C recorded remotely once, 7 local / 7 remote, staging empty; row **C03** was a **false negative** (asymmetric array comparison), not a migration defect
- **QF-MVP-20.3CVR1:** **VERIFIER CORRECTED (offline)** — C03 now normalizes both sides in PostgreSQL; validator rule R20 added
- **QF-MVP-20.3CVR1V:** **C VERIFICATION COMPLETE** — read-only re-run returned **23 PASS / 0 FAIL**; **Migration C is COMPLETE**
- **QF-MVP-20.3DG:** **GENERATED + REVIEWED, NOT APPLIED** — restores the `on_auth_user_created` trigger AND closes a self-signup **admin escalation** in `handle_new_user()`
- **QF-MVP-20.3DR1:** **CORRECTED, NOT APPLIED** — DG's blanket `role='vendor'` would have misclassified every homeowner/client OTP auth user; the role is now classified **only** from the server-set `app_metadata.qf_principal` marker and defaults to a **neutral NULL**; **no database contacted, nothing pushed**
- **Next:** QF-MVP-20.3D **staging preflight** (not application), then D application → E → QF-MVP-20.4
- **Roadmap:** LOCKED
- **Launch market:** Pune
- **Meta voice:** excluded
- **AI scoring / ranking:** excluded
- **Vendor CRM:** inside QuickFurno (extension module)
- **Jarvis:** separate repository
- **Core authority:** locked
- **Legacy governance:** non-blocking

## 2. Current active phase
**QF-MVP-20 — Marketplace Transaction Engine** — **`IN_PROGRESS`**. **Current task: QF-MVP-20.4B — Production Historical Credit-Ledger Evidence Audit (SELECT-only) — `PRODUCTION_LEDGER_EVIDENCE_AUDIT_COMPLETE_READY_FOR_FOUNDER_REVIEW`.** Staging now holds **four** truthful history rows, 67 tables, 45 public functions, all five canonical functions, and zero application rows. B1's first application (20.3B1A) had failed on an authoring defect in its own §7.5c guard — a negative regex over `pg_get_functiondef()` matching the function's own comments. That correction is now **proven on staging**: corrected B1 applied at exit 0, its §7 block ran to completion, and verifier rows 607 and 613 — the two that would otherwise have failed — both passed. Staging holds four truthful history rows. Migration **G** has since been applied (2026-07-23, exit 0), so staging now holds **five** truthful history rows and the locked phase verifier returns **62 PASS / 0 FAIL**. The lineage append-only application-role boundary is in force: PUBLIC/anon/authenticated hold nothing and `service_role` holds **SELECT + INSERT only**; owner authority is documented break-glass, not an application-role failure. The **R1 consumer migration is implemented**, and **Migration B2 is now generated and reviewed** (not applied): four functions and four triggers make active-3, lifetime-6 and lineage append-only universal for every write path and every role including the owner, while B1's API, caps and credit authority are untouched. The **B2 preflight completed**, then the one authorized **application attempt (B2A) rolled back atomically** on a runtime type defect in B2's self-verification (`array_agg(attname) = text[]`, SQLSTATE 42883) — B2 was never recorded remotely. **B2R1 has corrected it**: the catalog `name` arrays are cast to `text` at all five sites (migration §5.7, verifier B13/B15), enforcement semantics unchanged, and the offline validator now rejects the defect family (rule R23). B2 is applied and verified on staging. **Migration C is now generated and reviewed** (not applied): `vendor_public_v` is a safe owner-rights projection exposing 21 allowlisted public fields (zero monetization/PII/internal), and C revokes all direct `vendors`/`leads` access from PUBLIC/anon (authenticated → vendors SELECT only, leads none) and drops the three unsafe anon policies. The consumer trace proved every vendor read and lead write is already server-owned, so no runtime consumer code needed migration. **Migration C is now APPLIED on staging** (2026-07-24T02:16:04Z, exit 0, recorded exactly once, 7 local / 7 remote, application data still empty), and its own self-verification reported *vendor_public_v (21 cols, 0 leaks), anon access revoked, unsafe policies dropped, B1/B2/G/legacy intact, D absent*. The locked verifier then returned **22 PASS / 1 FAIL**: row **C03** compared a DB-sorted array against a raw hand-ordered literal whose `cover_image_url`/`covers_full_city` pair was transposed — a **false negative**, with no migration, view, ACL, policy or data defect. **CVR1 corrected it** and the read-only re-verification returned **23 PASS / 0 FAIL**, so **Migration C is COMPLETE**. **Migration D is now generated and reviewed** (not applied): it restores the `on_auth_user_created` trigger that the staging baseline dropped (auth-schema triggers are excluded from a public-schema dump) and, in the same forward-only step, closes a **self-signup administrator escalation** — the original function derived `profiles.role` from attacker-controlled `raw_user_meta_data`, and `is_admin()` reads that column. **QF-MVP-20.3DR1 then corrected a principal-classification defect in that generation**: DG's blanket `role='vendor'` would have stamped every homeowner created by the client WhatsApp-OTP login (`signInWithOtp` through the anon client) as a vendor, so the role is now classified **solely from the server-set Auth `app_metadata` key `qf_principal`**, defaulting to a **neutral NULL** that grants nothing, with `'admin'` unreachable. The **D staging preflight (DP) then correctly HALTED before staging contact** (`D_PREFLIGHT_BLOCKED_VERIFIER_SOURCE_PROOF_INSUFFICIENT`): the verifier proved the contract only from the function COMMENT, which cannot detect a drifted body. **QF-MVP-20.3DVR1 corrected the verifier and validator** (Migration D untouched) to prove the installed `pg_proc.prosrc` comment-stripped in-SQL — rows 27 + 29-37 — satisfying both the B1R2 (no unstripped negative regex) and DP (no COMMENT-only proof) policies at once; a runtime-SQL quote-balance defect in the new literal regex was caught in review and fixed with dollar-quoting. **QF-MVP-20.3DP2 completed the preflight** (source-proof gate PASS; dry run proposed exactly D, zero write), and **QF-MVP-20.3DA then APPLIED Migration D to staging** (2026-07-24T06:47:57Z, exit 0): history **8 local / 8 remote**, D recorded once; the locked **37-row verifier returned 37 PASS / 0 FAIL** (all installed-body rows D27,D29-D37 pass); live catalog facts confirm the hardened D-form `handle_new_user` (SECURITY DEFINER, pinned search_path, owner postgres, no dynamic SQL) and the `on_auth_user_created` AFTER INSERT trigger (tgtype 5, enabled, sole auth trigger) with anon/authenticated EXECUTE revoked; **67 public tables, 0 rows — no data, no backfill**; C/B1/B2/G/legacy all preserved; the only advisor ERROR is the known C `vendor_public_v` one and D itself raises none. **Migration D is COMPLETE on staging.** **Migration E is now generated and reviewed** (not applied): an **ACL-only** re-assertion pinning the six legacy state-changing lead-assignment RPCs to `service_role`-only EXECUTE (`REVOKE FROM public, anon, authenticated` + `GRANT service_role`). **Honest reconciliation:** the applied baseline ALREADY locked all six and no applied migration re-granted them, so E closes no live hole — it is a current-object ACL re-assertion + catalog-verification milestone (a no-op on the accepted current posture, corrected in EGR1 from an impossible "survives future DROP+CREATE" overclaim); the canonical `qf_assign_lead_vendors_v2` and the safe `get_public_eligible_vendors` (called via `publicClient()`) are excluded, and R1 left zero live `.rpc()` calls to the six. Validator **51/51**, SELECT-only verifier **21 rows**; one `name`-vs-`text` robustness defect (E20) caught and fixed. **QF-MVP-20.3EGR1 then retracted an impossible durability overclaim** (“survives future DROP+CREATE”): E is a current-object ACL re-assertion, not a future-object or default-privilege guarantee, so any later migration recreating a target must itself re-revoke PUBLIC/anon/authenticated (encoded by validator rule R12); the executable REVOKE/GRANT design is unchanged. **QF-MVP-20.3EP completed the preflight** (source-proof gate PASS; live pre-state a confirmed no-op; dry run proposed exactly E, zero write), and **QF-MVP-20.3EA then APPLIED Migration E to staging** (2026-07-24T11:18:22Z, exit 0): history **9 local / 9 remote**, E recorded once; the locked **21-row verifier returned 21 PASS / 0 FAIL** (per-target server-only, safe/canonical preserved, B1/B2/G/C/D intact, 20.4/owner-binding fenced); a SELECT-only catalog snapshot **before vs after is byte-identical** — unchanged OIDs (no DROP+CREATE) + unchanged `prosrc` md5 + untrusted-EXECUTE=0 — so **E is a PROVEN ACL no-op**; **67 public tables, 0 rows** (no data, no RPC invoked); C/B1/B2/G/D all preserved; E raised **zero** new advisors. **Migration E is COMPLETE on staging** — the locked A→A2→B1→G→R1→B2→C→D→E sequence is fully applied. **QF-MVP-20.4A then designed the historical credit-ledger reconciliation**: from the QF-MVP-10 evidence (46/19/27, historical not invariant) it froze a structural candidate population (credit_deducted assignment with no canonical `lead_assignment_debit` ledger row), a closed 8-class evidence vocabulary + 5 correction modes, a founder-approval authority matrix (Jarvis none), and a **SELECT-only production audit pack** + offline safety validator (39/39, 16 fixtures) + **empty** evidence-manifest schema — **no blind backfill**, `credit_deducted`/balances prohibited as proof, `vendor_credit_logs` judged unsafe for evidence-only rows (recommend a separate immutable reconciliation table later). **No database accessed in 20.4A; no migration; nothing pushed.** **QF-MVP-20.4B then executed that SELECT-only audit against production `yqpgcsduqbxulrlzwzap`** (founder-authorized, `transaction_read_only=off` so SELECT-only was process-enforced): R00-R11 ran once each, the R02 fingerprint gate PASSED (canonical fn MD5 matches QF-MVP-10), and the result is **UNCHANGED from 46/19/27** — **27 candidates** (admin 5 / automatic 16 / client-selected 6), 0 conflicts, 0 arithmetic violations, and **all 27 → `INSUFFICIENT_EVIDENCE`** (no strong-evidence path, so no PROVEN_* asserted). Evidence + a schema-valid manifest live **outside Git**; **no data changed, no correction authorized, no UUID/secret in Git**. **Next: founder evidence review + correction-plan decision**, not a migration application. Replay semantics are **LOCKED** (normalized request fingerprint, persisted result, `idempotency_conflict` on key reuse); assignment cost is **ONE_WALLET_CREDIT**; `client_selected` mode is **fail-closed pending R1 owner binding**; the baseline validator now runs to **PASS**, closing the previous `BLOCKED_EXTERNAL_EVIDENCE`. The binding release order is **A → A2 → B1 → R1 → B2 → C → D → E → QF-MVP-20.4**.

### Design record (QF-MVP-20.3A)
**Marketplace Authority Remediation Migration Design** (design + repository audit + documentation only; no SQL migration, no runtime change). The remediation is specified as **five separate forward-only migrations** (A foundation · B canonical authority + enforcement triggers · C public projection + privilege hardening · D Auth trigger · E legacy revokes) in [`QF-MVP-20-3A-REMEDIATION-MIGRATION-DESIGN.md`](QF-MVP-20-3A-REMEDIATION-MIGRATION-DESIGN.md) with a [`SCHEMA-CONTRACT`](QF-MVP-20-3A-SCHEMA-CONTRACT.md), [`CONSUMER-MIGRATION-MATRIX`](QF-MVP-20-3A-CONSUMER-MIGRATION-MATRIX.md), [`STAGING-TEST-PLAN`](QF-MVP-20-3A-STAGING-TEST-PLAN.md) and [`ROLLBACK-PLAN`](QF-MVP-20-3A-ROLLBACK-PLAN.md). Locked: active set `{assigned,delivered,accepted}`, lifetime = distinct vendors in append-only `lead_assignment_events`, no caller-controlled limit, ledger-only credit, approval-gated restoration, `vendor_public_v` projection, intent-only communication. The 27-row historical ledger gap is assigned to **QF-MVP-20.4**. **Next: QF-MVP-20.3B — Generate Marketplace Authority Remediation Migration.**

### Superseded status line
**QF-MVP-20.2C2 is `COMPLETE`: the staging baseline is APPLIED_AND_VERIFIED.** The reviewed baseline was applied to staging `uckafzuochmbvtiodmcl` (single `db push --linked`, exit 0, one honest history row, zero application data, production untouched), and the **corrected** SELECT-only verifier (`7ba9792f…`) returns **40/40 PASS** — function parity by exact `to_regprocedure` OID resolution, index parity by `pg_constraint.conindid` classification. Baseline SQL unchanged; no reapplication, reset, repair or history change. Advisors read (staging only) — none blocking. **Next: QF-MVP-20.3A — Marketplace Authority Remediation Migration Design.**

**QF-MVP-20 subphase status:**
- **20.0 Authority Repair Design** — `COMPLETE`. Canonical engine, credit authority, replacement, eligibility, public projection, communication boundary, migration plan, acceptance-test matrix in [`QF-MVP-20-AUTHORITY-REPAIR-DESIGN.md`](QF-MVP-20-AUTHORITY-REPAIR-DESIGN.md), [`QF-MVP-20-MIGRATION-PLAN.md`](QF-MVP-20-MIGRATION-PLAN.md), [`QF-MVP-20-ACCEPTANCE-TEST-PLAN.md`](QF-MVP-20-ACCEPTANCE-TEST-PLAN.md).
- **20.1 Consumer & call-path audit** — `COMPLETE`. Repository consumer map in [`QF-MVP-20-CONSUMER-CALL-PATH-AUDIT.md`](QF-MVP-20-CONSUMER-CALL-PATH-AUDIT.md): 4 Supabase clients, all `.rpc()` consumers, table-mutation matrix, 8 eligibility evaluators, ledger-bypass paths, no replacement authority, comms-in-transaction only in `assign_lead_to_vendors`, all AOS `DORMANT_KEEP_DISABLED`; 6 ACTIVE_BLOCKERs.
- **20.2A Staging schema baseline audit** — `COMPLETE`. Read-only audit of the external production public-schema dump (SHA256 `269c9265…`) in [`QF-MVP-20-STAGING-BASELINE-AUDIT.md`](QF-MVP-20-STAGING-BASELINE-AUDIT.md) + [`QF-MVP-20-STAGING-BASELINE-PLAN.md`](QF-MVP-20-STAGING-BASELINE-PLAN.md). Inventory (62 tables / 39 functions / 33 SECURITY DEFINER / 67 policies / 62 RLS / 180 indexes / **0 triggers** / **0 destructive** / **0 secrets**); classification matrix; the 4 blocker RPCs confirmed `GRANT ALL TO anon+authenticated` (EXCLUDE_UNSAFE); `GRANT ALL ON vendors TO anon` monetization exposure; live-body evidence resolves the 20.1 "which body is live" unknown (3 un-ledgered blockers vs 3 ledgered RPCs). Truthful migration-history strategy (one baseline row, no fake 68), sanitization rules, application order, parity plan. Raw dump **not** committed.
- **20.2B Generate reviewed staging baseline** — `COMPLETE`. Deterministic generator + offline validator + grant manifest + generated baseline SQL + SELECT-only verifier in `scripts/mvp/staging/**` and `supabase/staging-baseline/**`; review in [`QF-MVP-20-STAGING-BASELINE-REVIEW.md`](QF-MVP-20-STAGING-BASELINE-REVIEW.md). Baseline SHA256 `920a4aa0…` (byte-identical across two runs); verifier SHA256 `89362a35…` (later superseded by `e82b757f…` in 20.2C1R). Strips 102 owner + 201 grant + 28 revoke + 12 default-privilege statements; injects safe `search_path` into 6 invoker helpers; the 4 blockers + legacy credit primitives + `qf_apply_vendor_credit_delta` are **service_role-only**; anon gets **no** table/monetization access. FK-cycle unknown resolved (all 69 FKs added post-table). Baseline lives **outside** `supabase/migrations/`; **not applied**; production history untouched; raw dump not committed.

- **20.2C1 Staging application preflight** — `COMPLETE`. SELECT-only staging access + `db push --dry-run` in [`QF-MVP-20-STAGING-APPLICATION-PREFLIGHT.md`](QF-MVP-20-STAGING-APPLICATION-PREFLIGHT.md). Artifact hashes re-verified (baseline `920a4aa0…`, verify `89362a35…` — later superseded by `e82b757f…` in 20.2C1R); offline validator PASS; deterministic. External apply workspace prepared (outside repo, 1 migration, no seed); **linked to staging only** (`uckafzuochmbvtiodmcl`, no prod ref in link markers). Staging SELECT-only: PG 17.6; public tables 0 / policies 0 / indexes 0 / history rows 0; `auth.users` + `gen_random_uuid` present; no provider accounts; no prod ref in public objects. `migration list --linked` local 1 / remote 0; **dry-run proposes exactly `20260722000100`** (no seed/edge/reset/repair/config/prod). Staging remained EMPTY. **Finding:** 1 Supabase-managed `rls_auto_enable` function (via `ensure_rls`) → 20.2C2 must reconcile the post-apply function count (40 vs 39) without dropping the managed default; `auth.users→handle_new_user` trigger absent (forward migration).

- **20.2C1R Managed function parity reconciliation** — `COMPLETE`. Offline correction of the verification SQL (baseline SQL **unchanged**, SHA256 `920a4aa0…`). Function parity is now **identity-scoped**: an expected-function CTE encodes the exact **39** QuickFurno identities (schema.name + `pg_get_function_identity_arguments`), with `03a` count=39, `03b` missing=0, `04` SECURITY DEFINER scoped=33, `03c` allowed managed `public.rls_auto_enable()`=1, `03d` unexpected=0, and `03e` total public functions=40 (supporting only). Old total-only `=39` check removed; `rls_auto_enable` preserved (not dropped). Offline validator extended to prove all 39 identities are encoded, the managed exception is singular, total=40, SD=33, and the verify SQL is SELECT-only. Verify SQL SHA256 `e82b757f…` supersedes `89362a35…`.

- **20.2C2 Apply and verify staging baseline** — **`FAILED_REQUIRES_REVIEW`**. Baseline **applied** to staging (single `npx supabase db push --linked`, **exit 0**); migration history holds **one** row `20260722000100` / `qf_mvp_staging_baseline_269c9265` (821 statements); all 62 public tables zero-row; `auth.users` = 0. Verification: **24 PASS / 6 FAIL** → not complete. Failing: `03a` (39→3), `03b` (0→36), `03d` (0→36), `04` (33→3), `09b` (32→109), `11` (180→257) — all **verification-artifact expectation defects**: `pg_get_function_identity_arguments()` returns `argname type` (not type-only), and `pg_indexes`/`indisunique` include constraint-backed indexes (standalone measured **180**/**32**, matching the reviewed inventory). Passing: 62 tables, 62 RLS, 67 policies, 62 PK, 69 FK, 15 UNIQUE, 169 CHECK, 0 triggers/views/matviews, total functions 40 (39 QF + 1 managed), 6 assignment RPCs, all blocker/credit/anon privilege lockdowns, providers empty + Meta disabled, honest single history row. Advisors **not** read (gated on full pass). `auth.users→handle_new_user` trigger **absent** (`OPEN_FORWARD_MIGRATION_PREREQUISITE`). No patch, no re-push, no reset/repair; production never accessed. Record: [`QF-MVP-20-STAGING-BASELINE-APPLICATION-RESULTS.md`](QF-MVP-20-STAGING-BASELINE-APPLICATION-RESULTS.md).

- **20.2C2R Correct and reverify applied staging baseline** — `COMPLETE`. Verification artifact corrected **offline** (staging untouched, baseline byte-identical `920a4aa0…`): functions resolved by exact **`to_regprocedure` OID** (never string-comparing `pg_get_function_identity_arguments`), indexes classified by **`pg_constraint.conindid`**. Re-executed SELECT-only against staging → **40/40 PASS** (functions 39/0 missing/0 dup/33 SD/1 managed/0 unexpected/40 total; indexes 77 constraint-backed / 180 standalone / 32 standalone-unique / 47 combined / 257 / 109). Extended proofs: 0 tables with rows, `auth.users` 0, all provider/communication tables 0, no production ref or URL in function bodies/defaults/comments, Meta disabled, `on_auth_user_created` **absent**. Advisors (staging only): security 32 INFO `rls_enabled_no_policy` (fail-closed, expected) + 7 WARN (permissive `leads public insert`, intentional/managed SECURITY DEFINER exposure); performance 212 (147 unused-index artifacts of an empty DB, 36 multiple-permissive-policies, 18 unindexed FKs, 7 auth_rls_initplan, 3 duplicate indexes, 1 platform Auth config) — **none blocking**, routed to 20.3A. Verification SHA256 `7ba9792f…`. **No db push, no reapplication, no reset/repair, no history change, no staging write, no production access.**

- **20.3A Marketplace authority remediation migration design** — `COMPLETE`. Five forward-only migrations specified (A/B/C/D/E) with exact objects, dependencies, rollback boundaries, staging gates and catalog deltas. Grounded in the applied baseline: `lead_assignments` cascades on lead/vendor delete → **append-only `lead_assignment_events` is mandatory** for lifetime-6; `vendor_status` is CRM pipeline, so a **new `lifecycle_status`** column carries the 10-state lifecycle with `ACTIVE={assigned,delivered,accepted}`; `UNIQUE(lead_id,vendor_id)` means replacement transitions the row and a replaced vendor can't be re-assigned to the same lead. Canonical RPC `qf_assign_lead_vendors_v2` (**no caller-controlled limit**, no trusted-actor-by-parameter), four separate idempotency boundaries (corrected in 20.3A1R), ledger-only credit with approval-gated restoration, `vendor_public_v` allow-list projection, `communication_intents` outbox (uncertain = terminal), consumer matrix for all 40 call sites, 22-test staging matrix, per-migration rollback. Historical 27-row gap → **QF-MVP-20.4**. Advisors triaged (none blocking; do not remove indexes on empty-staging evidence). **No SQL, no runtime change, no database access.**

- **20.3A1 Remediation decision closure** — `COMPLETE`. SELECT-only reconciliation of **production + staging** closed all nine decisions ([`QF-MVP-20-3A1-DECISION-CLOSURE.md`](QF-MVP-20-3A1-DECISION-CLOSURE.md)). Production profile: `lead_assignments` identical to the baseline; 46 rows, all `vendor_status='New'`, all `credit_deducted=true`, 0 nulls/orphans/duplicates, max 3 rows and 3 distinct vendors per lead; 27-row ledger gap reconfirmed; `vendor_packages` **0 rows**; 4 blocker RPCs **live-verified anon/PUBLIC-executable**; **anon holds INSERT/SELECT/UPDATE/DELETE/TRUNCATE on `leads` and `vendors`** with an always-true `leads` INSERT policy exposing 17 internal columns (incl. `lead_quality_score`) — enough to forge the auto-distribution gate. Decisions: all 46 rows → `assigned`; lineage = 46 events + 1 batch op, idempotent; **Migration B split into B1/B2** with the runtime release between (fixing the ordering inversion); 5 additive suspension columns on `vendors`; server-owned service-role lead intake; `vendor_wallet_package_divergence_v` (wallet is sole authority); **ACTIVE = {assigned, delivered, accepted}** with `in_progress` excluded and the 20.0 draft superseded; lineage FKs **RESTRICT** with anonymisation-in-place. Final order: **A → A2 → B1 → R1 → B2 → C → D → E → 20.4**. No database write of any kind.

- **20.3A1R Assignment lineage idempotency correction** — `COMPLETE`. **Documentation only** — no database access (production **or** staging), no SQL, no migration, no runtime change, no deploy. Removes a **design defect**: the 20.3A/20.0 drafts made `lead_assignment_events` idempotent through **`UNIQUE (lead_id, vendor_id)`** and seeded it with `ON CONFLICT (lead_id, vendor_id) DO NOTHING`, which is incompatible with an append-only lifecycle event stream — a single (lead, vendor) pair must record many events (`assigned → delivered → accepted → completed`, or `rejected`/`invalid`/`replaced`), and the constraint would silently discard every event after the first. **Corrected model:** `event_idempotency_key text NOT NULL` with `UNIQUE (event_idempotency_key)` as the table's **only** unique constraint; seed key `legacy_assignment_seed_v1:<assignment_id>`; runtime key `assignment_event:<operation_id>:<assignment_id>:<event_type>` (+ ordinal where one operation legitimately emits repeats), always derived by the **authoritative transaction**, never by a caller. **Lifetime is a query, not a constraint** — `COUNT(DISTINCT vendor_id)` over `event_type='assignment_created' AND lifecycle_to='assigned'`; later events and failed candidates consume no slot. Idempotency boundaries are now **four and separate** (operations · assignment rows · ledger · events) and may not be merged. The existing `lead_assignments UNIQUE(lead_id, vendor_id)` is **unchanged**. Ten binding staging tests added (**T33–T42**); T34/T35/T37/T42 fail under the withdrawn model and are the regression guards. [`QF-MVP-20-AUTHORITY-REPAIR-DESIGN.md`](QF-MVP-20-AUTHORITY-REPAIR-DESIGN.md) is outside the permitted paths and is therefore **superseded in writing**, not edited. Baseline SHA `920a4aa0…` and verification SHA `7ba9792f…` **unchanged**; migrations directory untouched.

- **20.3B1 Generate foundation, lineage backfill and canonical authority migrations** — `COMPLETE`. **Offline authoring only** — no production access, no staging access, no SQL execution, no `db push` (not even `--dry-run`), no history change, no runtime TypeScript, no deploy. Three forward-only migrations generated and left **`GENERATED_NOT_APPLIED`**: **A** `20260723000100_qf_mvp_marketplace_authority_foundation.sql` (`7d569d33…`) — 5 new tables, +4 `lead_assignments` / +4 `vendor_credit_logs` / +5 `vendors` columns, +11 indexes, RLS-on-no-policy + `service_role`-only grants, additively widened `change_type` CHECK retaining all 11 legacy values; **A2** `20260723000200_qf_mvp_assignment_lineage_backfill.sql` (`6bc0285d…`) — idempotent reviewed data step seeding one `assignment_created`/`assigned` event per qualifying assignment via `ON CONFLICT (event_idempotency_key) DO NOTHING`; **B1** `20260723000300_qf_mvp_canonical_assignment_authority.sql` (`151f6b43…`) — 5 canonical functions, `service_role`-only, `search_path` pinned, **zero triggers**. Phase verifier `supabase/staging-verification/verify_qf_mvp_20_3b1.sql` (`ebe0ef75…`, SELECT-only, 44 checks, works on empty **and** production-shaped databases); offline validator `scripts/mvp/staging/validate-qf-mvp-20-3b1.mjs` (`eddec25a…`) **PASS 82/82**. **Two design contradictions were escalated and closed by founder decision before authoring:** `assignment_operations.lead_id NOT NULL` made a single global batch row impossible → A2 now creates **one operation per distinct lead** (`qf_mvp_20_a2_lineage_backfill_v1:<lead_id>`), superseding the global-batch requirement; and `public.audit_logs` is **absent from the applied baseline** (created only by drifted migration `20260621000006`) → not created in A, not written in B1, with `assignment_operations`/`lead_assignment_events`/`vendor_credit_logs`/`credit_restoration_approvals`/`communication_intents` as the domain audit evidence and B1 completing the operation result instead. `source_kind='migration_backfill'` locked. Lifetime-six is `count(distinct vendor_id)` over `assignment_created`/`assigned` — a query, never a constraint; the 7th distinct vendor is rejected before assignment, debit, event and intent. No caller-controlled ceiling, wallet-only debit, intent-only communication, no provider primitive, no suspension/restoration mutation path. **Excluded and verified absent:** B2 triggers, Migration C hardening, Migration D Auth trigger, Migration E revokes. Legacy compatibility fully retained (all 6 legacy assignment RPCs present, no grant broadened, none revoked). Baseline `920a4aa0…` and baseline verifier `7ba9792f…` **unchanged**. `npm run verify:mvp` green (40/40). **Applying A/A2/B1 to production would close no existing production exposure** — the anon privileges and anon-executable blockers remain until C and E. Record: [`QF-MVP-20-3B1-MIGRATION-GENERATION-RESULTS.md`](QF-MVP-20-3B1-MIGRATION-GENERATION-RESULTS.md).

- **20.3B1R Canonical authority contract review and closure** — `COMPLETE`. **Offline SQL review, correction and static validation** — no production access, no staging access, no SQL execution, no `db push` or dry-run, no runtime implementation, no deploy. Four contracts reviewed; **three required correction**. **(1) Idempotent replay was key-only and is now fingerprinted:** the generated B1 trusted `idempotency_key` alone, so one key reused with a *different* authority request would have replayed a foreign result as success. Migration A gains `assignment_operations.request_fingerprint text NOT NULL` (min length 16) + `assignment_operations_terminal_completion_check` (terminal ⇒ `completed_at` **and** non-empty `result`); B1 computes a SHA-256 fingerprint over lead, mode, **deduplicated and sorted** candidates, reason code, replacement ref and actor — excluding `now()`, txids, random values and volatile state — and branches four ways: exact replay → persisted result verbatim + `already_applied=true` (no new id, no recomputation, immune to later state drift); different fingerprint → **`idempotency_conflict`**, zero mutation; `in_progress` → `conflict_retry`; vanished claim → `conflict_retry`. A2 supplies its own deterministic fingerprint. **(2) `ASSIGNMENT_CREDIT_COST = 1` locked** as a single named authority — no caller parameter, no `app_settings`/`get_setting_int` read, no `vendor_packages` inference or debit, no variation by mode, no clamping; replays, rejected, cap-blocked and already-assigned candidates and the A2 backfill all cost zero by construction. **(3) `client_selected` mode now FAILS CLOSED** (`R1_BLOCKED_PENDING_OWNER_BINDING`): the generated phone-equality check was removed because `qf_norm_text` is `lower(trim(...))` — raw-text equality, not canonical phone normalization — and `leads` has no ownership column; the mode returns `unauthorized` **before the operation claim**, so not even an operation row is created. Deliberately withheld authority, not a defect; R1 must add an explicit lead/client binding. **(4) Audit model confirmed unchanged** — no `audit_logs` created or written, A2 fabricates no ledger evidence, the 27 historical gaps are byte-identical. **Baseline-validator blocker RESOLVED:** the approved external source `production-public-schema.sql` was located, hashed to the required `269c9265d32a9f85488d76bfcf9dd528bd9b6b915bafb09ebb024a6bde182a2f`, and `validate-staging-baseline.mjs` returned **PASS** (62 tables / 39 functions / 33 SD / 67 policies / 69 FK / 180 indexes / 0 triggers / anon no table grant / 10 mutation RPCs locked). Raw schema **not** copied into Git and **not** modified. Phase verifier extended to 58 checks (`688ab439…`); offline validator extended to **105/105 PASS** (`4497a3c0…`) with a new structural body view so guard regexes inside string literals cannot self-trigger. 19 new binding staging tests **T49–T67**. Baseline `920a4aa0…` and baseline verifier `7ba9792f…` **unchanged**. `npm run verify:mvp` green (40/40).

- **20.3B1P Marketplace authority migrations staging application preflight** — `COMPLETE`. **No staging write, no real `db push`, no migration applied, no migration-history change, no production access, no QF-Jarvis access, no Auth user, no provider activation, no synthetic fixture.** All seven locked artifact hashes re-verified before any remote call; phase validator **105/105 PASS**; baseline validator **PASS** against the external `269c9265…` evidence (never copied into Git). **Target proof:** `npx supabase projects list` shows `linked=true` for `uckafzuochmbvtiodmcl` **only** — production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` both `linked=false`; `supabase/.temp/project-ref` = staging; a scan of every `.temp` file found 3 staging references and **zero** production/Jarvis references. **External workspace** `qf-staging-apply` (not a Git repo, nothing committed from it) held only the applied baseline beforehand; A/A2/B1 were copied in and all four `.sql` files hash byte-identical to the repository (`920a4aa0…`, `b6307094…`, `9d77f446…`, `a4b5c378…`), with no seed, no config, no Edge Function and none of the other 68 repository migrations. **SELECT-only precheck 20/20 as expected:** PG 17.6 · 62 tables · 39 + 1 managed = 40 functions · 62 RLS · 67 policies · one history row `20260722000100`/`qf_mvp_staging_baseline_269c9265` · all 62 tables zero rows · `auth.users` 0 · providers empty/disabled · zero of the five foundation tables · neither canonical RPC · zero production/Jarvis references in bodies, defaults or comments. **Locked baseline verifier re-hashed immediately before execution and run verbatim → 40 PASS / 0 FAIL**; the phase verifier was deliberately NOT run because A/A2/B1 are unapplied. **`migration list --linked`:** 1 remote / 4 local / **3 pending**, no history replay and no fabricated 68-version history. **Dry-run** `npx supabase db push --linked --dry-run` (once, no `--include-all`, no `--include-seed`) → **exit 0**, `DRY RUN: migrations will *not* be pushed`, proposing **exactly three** in order: A → A2 → B1; baseline not re-proposed, no seed/repair/reset/config/Edge-Function step. **Post-dry-run non-mutation proof:** history still 1 row, A/A2/B1 absent remotely, foundation tables and all five canonical functions absent, `lead_assignments` additive columns absent, 62 tables / 40 functions unchanged, 0 rows everywhere, `auth.users` 0, providers inactive, 0 public-table triggers. **Zero deviations, zero blockers.** `client_selected` remains `R1_BLOCKED_PENDING_OWNER_BINDING` — fails closed, unavailable to PUBLIC/anon/authenticated, and does not block installation. Record: [`QF-MVP-20-3B1-STAGING-APPLICATION-PREFLIGHT.md`](QF-MVP-20-3B1-STAGING-APPLICATION-PREFLIGHT.md).

- **20.3B1A Apply and verify marketplace authority migrations on staging** — **`PARTIAL_APPLICATION_REQUIRES_REVIEW`**. All pre-application gates passed: 8/8 artifact hashes, phase validator **105/105**, baseline validator **PASS**, staging proven the sole linked target (production and QF-Jarvis both `linked=false`, zero refs in `.temp`), 19/19 pre-application SELECT checks, locked baseline verifier **40 PASS / 0 FAIL**, migration list 1 remote / 4 local / 3 pending, and a dry-run at exit 0 proposing exactly A → A2 → B1. One authorized `npx supabase db push --linked` then returned **exit 1**: **A committed**, **A2 committed**, **B1 aborted and fully rolled back**. **Root cause is an authoring defect in B1, not a schema, data or staging problem:** its §7.5c self-verification guard runs a negative regex over `pg_get_functiondef()`, which returns the definition **including comments**, and the body of `qf_assign_lead_vendors_v2` documents that it never reads `app_settings` or `vendor_packages` — so the guard matched its own prose and rejected a compliant function. The executable SQL contains no configuration or package read; `ASSIGNMENT_CREDIT_COST` is still the internal literal `1`. Both offline validators passed because they inspect comment-stripped file text while the in-database guard inspects comment-retaining catalog output — the same defect class fixed in the validator during 20.3B1R was never carried into the migration's own `DO` block. **Post-failure state (SELECT-only, no retry, no repair, no reset, no manual patch, no falsified history):** three truthful history rows (`20260722000100`, `20260723000100`, `20260723000200`); B1 absent; 5 foundation tables present; 67 tables; **40** public functions and **0 of 5** canonical functions, proving B1 rolled back cleanly; `request_fingerprint` NOT NULL present; A2 created **0** operations, **0** events, **0** ledger rows, **0** intents exactly as designed for empty staging; 0 rows across all 67 tables; `auth.users` 0; providers inactive; 0 enforcement triggers; 0 views; 6 legacy RPCs intact; 67 policies intact. Phase verifier **not run** (gate never reached) and advisors **not read** (authorized only after 58 PASS / 0 FAIL). Production and QF-Jarvis **never contacted**. Record: [`QF-MVP-20-3B1-STAGING-APPLICATION-RESULTS.md`](QF-MVP-20-3B1-STAGING-APPLICATION-RESULTS.md).

- **20.3B1R2 Correct canonical authority self-verification** — `COMPLETE`. **Offline only** — no production access, no staging access, no SQL executed, no `db push` or dry-run, no migration repair, no staging reset, no runtime code, no provider access. **B1 corrected in place at version `20260723000300`** (`46ce7377…`, was `a4b5c378…`): the unsound §7.5c negative regex over `pg_get_functiondef()` is withdrawn behind a prominent do-not-reintroduce notice, along with two sibling *positive* source probes that a comment alone could satisfy. All **nine** positive catalog guards retained (`to_regprocedure` identities, `has_function_privilege` for PUBLIC/anon/authenticated and service_role, `proconfig` search_path, `prosecdef` posture, `pg_trigger` absence, `request_fingerprint` NOT NULL, legacy function count). §7.2 is retained deliberately — it matches `pg_get_function_identity_arguments`, a comment-free structured rendering, so a negative match there is a genuine signature fact. **No business behaviour changed:** all eighteen locked contracts are byte-for-byte as reviewed. **The phase verifier carried the same defect in six rows and was corrected** (`b66ec060…`, was `688ab439…`) — rows 607 (`app_settings|vendor_packages`) and 613 (`audit_logs`) **would have failed against a correctly applied B1**, matching B1's own comments; 608/612 were latent; 605/611 were comment-satisfiable false assurance. Each is now a catalog fact, a data fact, or an explicitly labelled informational row naming where the real proof lives; the file still returns **58** checks and remains SELECT-only. **Validator hardened 105 → 126 checks** (`734b6a13…`) with a new executable-SQL view and seven deterministic regression fixtures: forbidden names in line comments, block comments and string literals **PASS**; a real `SELECT FROM public.app_settings` and `UPDATE public.vendor_packages` **FAIL**; a raw `pg_get_functiondef` negative-regex guard is **rejected** (fixture F reproduces the exact failure); unterminated constructs **fail closed**. It now also pins **A and A2 to their applied hashes**, so an applied migration can never be edited. **A (`b6307094…`) and A2 (`9d77f446…`) re-verified byte-identical and untouched**; baseline `920a4aa0…` and baseline verifier `7ba9792f…` unchanged; baseline validator **PASS** against the external `269c9265…` evidence. **Migration repair NOT required, staging reset NOT required** — history is already truthful and B1 is editable precisely because its version is absent remotely. Record: [`QF-MVP-20-3B1R2-CORRECTION-RESULTS.md`](QF-MVP-20-3B1R2-CORRECTION-RESULTS.md).

- **20.3B1A2 Apply and verify corrected canonical authority on staging** — **`B1_APPLIED_VERIFICATION_FAILED_REQUIRES_REVIEW`**. All pre-application gates passed: 8/8 artifact hashes, phase validator **126/126** with fixtures A–G all correct, baseline validator **PASS**, staging proven the sole linked target (production and QF-Jarvis `linked=false`, zero refs in `.temp`), stale external B1 (`a4b5c378…`) replaced with the corrected file and verified byte-identical (`46ce7377…`) while baseline/A/A2 stayed untouched, 23/23 pre-application SELECT checks, migration list 4 local / 3 remote / **exactly 1 pending**, and a dry-run at exit 0 proposing **exactly one** migration. One `npx supabase db push --linked` returned **exit 0**: **B1 committed**. History now holds **four** truthful rows and B1 is stored as `qf_mvp_canonical_assignment_authority`. **The 20.3B1R2 correction is proven** — B1's §7 block ran to completion and verifier rows 607/613 (which would have failed uncorrected) both passed. **Corrected phase verifier returned 57 PASS / 1 FAIL.** The failure is `R03_lineage_append_only_grants`: expected 0 UPDATE/DELETE grants on `lead_assignment_events`, found **4**. Root cause is an **A-scope privilege gap**, not a B1 defect — Supabase `pg_default_acl` grants `arwdDxtm` on every new `public` table to `postgres`/`anon`/`authenticated`/`service_role`, and Migration A revoked from `public`/`anon`/`authenticated` (those hold **nothing** ✓) but only *added* `grant select, insert … to service_role`, never revoking the default `UPDATE`/`DELETE`/`TRUNCATE` from `service_role` or `postgres`. Append-only on lineage is therefore enforced by **neither privileges nor a trigger** (B2's immutability trigger does not yet exist); `service_role` bypasses RLS so RLS cannot close it. **Zero rows at risk, no untrusted exposure, no runtime consumer.** All other 57 checks passed, including all five canonical signatures, zero untrusted EXECUTE, `service_role` EXECUTE on all five, `search_path` pinned on all four DEFINER routines, eligibility INVOKER, no caller cost/delta/limit parameter, 0 B2 triggers, 0 Auth trigger, 6 legacy RPCs intact, 67 policies, 67 RLS tables, 45 functions, and **0 rows across all 67 tables**. Advisors **not read** (gate requires 58/58). No retry, no repair, no reset, no manual patch, no falsified history. Production and QF-Jarvis **never contacted**. Record: [`QF-MVP-20-3B1A2-STAGING-APPLICATION-RESULTS.md`](QF-MVP-20-3B1A2-STAGING-APPLICATION-RESULTS.md).

- **20.3B1G Lineage append-only grant repair** — `GENERATED_REVIEWED_NOT_APPLIED`. **Offline only** — no staging access, no production access, no SQL executed, no `db push` or dry-run; Migration G was deliberately **not** copied to the apply workspace (it still holds exactly four files). Migration **G** `20260723000400_qf_mvp_lineage_append_only_grants.sql` (`91544524…`) is forward-only and **REVOKE-only**: it revokes all privileges on `lead_assignment_events` from PUBLIC/anon/authenticated and revokes `UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, MAINTAIN` from `service_role`, leaving **SELECT + INSERT only**. `MAINTAIN` is included because Supabase's default ACL `arwdDxtm` ends in `m`. Owner/postgres is untouched and documented as break-glass, surfaced only as an informational verifier row. **The implementation was reviewed line by line rather than trusted, and FOUR defects were found and corrected.** (1) G was the only migration in all 69 carrying an explicit `begin;`/`commit;`; the inner COMMIT would have ended the Supabase CLI's transaction early and broken atomicity between the migration and its own history row — wrapper removed, and the validator now rejects transaction control in any migration. (2) Both G's post-check and verifier row 44 proved "PUBLIC holds nothing" via `information_schema.role_table_grants`, which PostgreSQL documents as **omitting PUBLIC grants** — the predicate could never match and would have silently passed; all lineage proofs now use `has_table_privilege()` with `'public'` as a role name, giving effective-privilege detection, and rows 44/45/46 were all converted. (3) Validator fixtures A–H exercised a private helper that was **never applied to Migration G** and had no other caller — vacuous; replaced with `evaluateGrantMigration()`, which now grades the **real** Migration G as a recorded check while each fixture differs from a passing migration by exactly one defect, and the link was **mutation-tested** (injecting a GRANT into the real G makes the real check fail). (4) The premise that the validator passed was **false**: G's verifier rows introduced sequence **46**, colliding with the pre-existing `/46/` production-count guard; that guard now strips row ordinals before testing. **Fixtures A and H together are the discriminator** — A proves the `TRUNCATE` *privilege* in a REVOKE list is not a TRUNCATE *statement*, H proves a real `TRUNCATE TABLE` is still caught. Verifier is **62 rows** (`e1d9edb8…`), SELECT-only, history expects baseline + A + A2 + B1 + G. Validator **165/165 PASS** (`e27d62d0…`), fixtures A–J all with expected outcomes. A (`b6307094…`), A2 (`9d77f446…`), B1 (`46ce7377…`), baseline (`920a4aa0…`) and baseline verifier (`7ba9792f…`) re-verified **unchanged**; baseline validator **PASS**. Record: [`QF-MVP-20-3B1G-LINEAGE-GRANT-REPAIR.md`](QF-MVP-20-3B1G-LINEAGE-GRANT-REPAIR.md).

- **20.3B1GP Lineage grant repair staging preflight** — **`PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. **Migration G was NOT applied.** Executed with direct reviewed shell commands; the previously generated PowerShell wrapper was **not** used — earlier wrapper attempts failed on portability and argument handling and are recorded as **local tooling deviations only**, having never authorized or performed an application. Repository proven at HEAD `5b51241b…`, clean and synchronized with `origin`. All **9** locked hashes verified plus the grants manifest (`11a3ad40…`). Phase validator **165/165 PASS** with fixtures **A–J** all run, the real Migration G evaluated at **0 findings**, and G tokenized and hashed (`91544524…`) so it was not silently skipped; the load-bearing link is structural — `evaluateGrantMigration()` grades `G.raw` at one call site and every fixture at another. `npm run verify:mvp` **40/40** (one stderr edge-runtime warning, exit 0); `git diff --check` exit 0; baseline validator **PASS** on its real interface against the locked external schema. **External apply workspace** proven outside Git (no `.git`, resolves to no worktree), no seed, no Edge Function content, `config.toml` naming neither prohibited project. **Migration G was found already present** from an earlier attempt, so it was **verified byte-identical** (`cmp` → IDENTICAL, `91544524…`) rather than copied again; all five workspace migrations hash-verified and nothing was deleted, renamed or repaired. The `_evidence/01…04` files were inspected and belong to **QF-MVP-20.2C2's** authorized baseline application of `20260722000100`, not to any B1GP wrapper. **Linked target** authoritative proof: `.temp/project-ref` = `uckafzuochmbvtiodmcl`, `linked-project.json` = *QuickFurno Staging*, **0** production and **0** QF-Jarvis references across `.temp`; `projects list` supporting only. **Migration list before**, parsed structurally from the CLI JSON: **5 local / 4 remote / exactly one pending**, pending = G alone, no unexpected version. **One** `npx supabase db push --linked --dry-run` → **exit 0**, `DRY RUN: migrations will *not* be pushed`, proposing **exactly** `20260723000400_qf_mvp_lineage_append_only_grants.sql` by complete filename; baseline/A/A2/B1 not proposed; no claim of application. **Non-mutation proof:** the list re-read immediately after still shows remote = baseline + A + A2 + B1 (**4 rows**), G local-only, and **no remote history row created**. Transcript written outside Git to `qf-staging-workspace\QF-MVP-20.3B1GP-PREFLIGHT-20260723T132554Z.txt`. Record: [`QF-MVP-20-3B1G-LINEAGE-GRANT-REPAIR.md`](QF-MVP-20-3B1G-LINEAGE-GRANT-REPAIR.md) §9.

- **20.3B1G-A Lineage grant repair staging application** — **`MIGRATION_G_APPLIED_VERIFIED_ON_STAGING_READY_FOR_COMMIT_REVIEW`**. All pre-application gates re-verified at HEAD `7d483a35…` (origin synchronized 0/0, clean tree): 9 locked hashes, phase validator **165/165** with fixtures A–J and the real Migration G rule at 0 findings, `npm run verify:mvp` **40/40** (one stderr edge-runtime warning, exit 0), `git diff --check` exit 0, workspace proven outside Git with exactly five hash-correct migrations and G byte-identical to the repository, linked target `uckafzuochmbvtiodmcl` only, and a structural pre-application history parse of **5 local / 4 remote / exactly one pending**. **One** `npx supabase db push --linked` ran at **2026-07-23T15:12:59Z → 15:13:09Z UTC**, **exit 0**, identifying and applying **only** `20260723000400_qf_mvp_lineage_append_only_grants.sql` — baseline, A, A2 and B1 were neither proposed nor re-applied, and the command was never repeated. **Post-application history: 5 local / 5 remote**, G recorded **exactly once**, no pending migration, no unexpected version, no history falsification. **Locked phase verifier** (re-hashed `e1d9edb8…` immediately before execution, SELECT-only, staging only) returned **62 PASS / 0 FAIL**. **Boundary proved live:** `PUBLIC`, `anon` and `authenticated` hold **no** lineage privilege; `service_role` holds **INSERT + SELECT only** and is denied `UPDATE`, `DELETE`, `TRUNCATE`, `REFERENCES`, `TRIGGER` and `MAINTAIN` (row 45 is an *effective* `has_table_privilege` check, so inheritance cannot hide); owner `postgres` is reported informationally as break-glass (row 47) and is **not** an application-role failure. The QF-MVP-20.3B1A2 failure (4 update/delete grants) is **closed**. **State unchanged otherwise:** 67 tables all **0 rows**, `auth.users` 0, providers inactive, 45 functions, 67 policies, 67 RLS tables, 0 triggers, 0 views, no B2/C/D work — only the lineage ACL and one history row changed. **Advisors read (read-only, after 62/62):** security 44 lints (37 INFO `rls_enabled_no_policy` = the deliberate fail-closed posture, 32 baseline + 5 from A; 1 WARN always-true `leads` policy → Migration C; 6 WARN SECURITY DEFINER executable → pre-existing); performance 233 lints (156 INFO `unused_index` = empty-DB artifact, **not** removal candidates; 36 multiple-permissive-policies; 30 unindexed FKs; 7 auth_rls_initplan; 3 duplicate indexes; 1 platform). **None blocking and none attributable to G**, which only revokes privileges. Nothing was auto-fixed. Production and QF-Jarvis **never accessed**; no second push, no repair/reset/up, no data, auth user, provider activation, seed, Edge Function or deployment. Transcript outside Git. Record: [`QF-MVP-20-3B1G-LINEAGE-GRANT-REPAIR.md`](QF-MVP-20-3B1G-LINEAGE-GRANT-REPAIR.md) section 10.

- **20.3R1 Marketplace runtime consumer migration** — **`R1_CONSUMER_MIGRATION_IMPLEMENTED_REVIEWED_READY_FOR_COMMIT_REVIEW`**. Repository/code phase at HEAD `4bcdcc55…` (origin 0/0, clean tree): **no migration created or applied, no database of any kind accessed, nothing pushed or deployed**. A full runtime inventory across `app/`, `services/`, `lib/` and `components/` (build output excluded) classified every assignment consumer. **Five MIGRATE_NOW paths** now call the sole canonical authority `qf_assign_lead_vendors_v2` and nothing else: auto-match (`automatic`/`system`), admin assign and admin manual (`admin_manual`/`admin`, actor bound to the acting superadmin), delayed fill and requirement-group auto-fill (`delayed_fill`/`worker`). **Five BLOCKED_CLIENT_SELECTED_OWNER_BINDING paths** fail closed with **zero** side effects and **zero** database round-trips — including the public unauthenticated `assignLead` action, which closes the QF-MVP-20.1 Blocker H auth gap. New code is a **pure contract module** (`lib/marketplace/canonicalAssignmentContract.ts`, dependency-free: no client, no network, no clock, no randomness) plus **one I/O seam** (`services/canonicalAssignmentAuthority.ts`), the only place in the repository that invokes the authority, through the service-role client alone. **The caller-controlled ceiling is gone**: `p_total_limit`, `p_allow_duplicate` and the admin recovery ceiling of 9 no longer exist in runtime code — active-3 / lifetime-6 / cost-1 belong to the authority. The operation key is deterministic (no clock, no randomness) and order-independent over the candidate set, so an infrastructure retry replays instead of double-assigning; the digest carries no authority, since the database independently fingerprints the request, so a collision can only ever produce an `idempotency_conflict` rejection. **Gates:** new `assignment-authority` suite **26/26** on the real production module; repository-wide static harness **61/61** over comment- and string-stripped source (so prose naming a legacy RPC can never read as a call to one); `npm run verify:mvp` **exit 0** with **66 cases / 3 suites passed, 0 failed**, typecheck, lint and build clean; `git diff --check` exit 0. `test:supabase:lead` deliberately **not** run (it writes to a real database). The static proofs are **load-bearing**: five failed against the first draft and were corrected against evidence, then two adversarial mutations (reintroducing a legacy `.rpc()` call; requesting `client_selected` mode) were both caught and the file restored byte-identical. **Founder-visible behaviour changes:** client-selected assignment no longer happens anywhere (the pick is recorded as an intent, the window opens, the system fills via `delayed_fill`); the admin ceiling of 9 is unreachable through assignment; `adminAssignLead` lost `allowDuplicate`. Legacy RPCs still exist in the database but are now **unreachable from the runtime** — the prerequisite for revoking them later. Record: [`QF-MVP-20-3R1-CONSUMER-MIGRATION.md`](QF-MVP-20-3R1-CONSUMER-MIGRATION.md).

- **20.3B2 Universal assignment enforcement** — **`B2_UNIVERSAL_ENFORCEMENT_GENERATED_REVIEWED_READY_FOR_PREFLIGHT`**. Generated and reviewed at the synchronized R1 commit `5c78ea37…` (origin identical, 0/0, clean tree, no version collision for `20260723000500`). **No database was contacted — no staging, no production, no QF-Jarvis, no dry run — and nothing was pushed.** B2 closes the bypass gap R1 left open: with every runtime consumer on the canonical authority, a write that never goes through it (legacy SQL, a direct psql session, an owner-privileged script) was still uncapped. The migration `20260723000500_qf_mvp_assignment_universal_enforcement.sql` (`ab31023e…`) adds exactly **4 functions and 4 triggers** and **no index, constraint, table, column, grant or row**: `trg_lead_assignments_active_cap` (BEFORE INSERT OR UPDATE, tgtype 23) enforces **active-3** over the exact set `{assigned, delivered, accepted}`; `trg_lead_assignment_events_lifetime_cap` (BEFORE INSERT, tgtype 7) enforces **lifetime-6** as `COUNT(DISTINCT vendor_id)` over `assignment_created`/`lifecycle_to='assigned'` lineage, exempting a repeat event for an already-counted vendor so idempotent replay is never falsely rejected; `trg_lead_assignment_events_immutable` (tgtype 27) and `trg_lead_assignment_events_no_truncate` (tgtype 34) make lineage **append-only for every role including the owner** — the trigger backstop staging test **T70** has been waiting for since G closed the privilege half. **One replacement at a time was already fully enforced** by Migration A's `uq_replacement_requests_open_per_lead`, so B2 asserts it and creates nothing; both cap counts are already served exactly by Migration A's indexes, so B2 adds none. **Concurrency is proved, not asserted:** both cap triggers serialize on the same `leads` row lock B1 takes first (a no-op re-take inside B1), the lock and the count are separate statements so READ COMMITTED gives the count a fresh snapshot, B2 never locks `vendors` so the global lead→vendors order is preserved and no deadlock cycle is added, and neither function writes anything so recursion is impossible by construction. **B1 compatibility is proved:** B2 re-checks the identical predicates at the identical moments, so it never fires on a valid canonical flow; it raises **P0001**, which B1's `unique_violation` handler cannot swallow, so a breach always rolls back whole with no partial assignment, event, ledger row or intent; and B1's five signatures and its service-role-only grant are asserted intact. **One deliberate, documented exception:** lineage UPDATE is permitted only when the sole change is clearing `assignment_id`/`operation_id` — Migration A's declared `ON DELETE SET NULL` retention contract, which a blanket ban would have contradicted. **Verification:** offline validator (`85cd5f9b…`) **57/57** with **25 one-defect fixtures that are all mutations of the real migration run through the same evaluator**, plus three real-artifact mutation tests (cap raised to 4; TS contract drift; immutability trigger downgraded) all caught and restored byte-identical; SELECT-only verifier (`0772409e…`) **34 rows** proving enforcement **structurally from catalog facts without executing a single write**. The locked QF-MVP-20.3B1R2 policy is honoured — no assertion over `pg_get_functiondef`/`prosrc`/`routine_definition` anywhere. **Seven material defects were found and corrected in independent review**, including a guessed legacy-RPC signature that would have aborted the migration on a false alarm, an exact-string `proconfig` match, and the Migration A retention contradiction. **Gates:** B1/G validator **165/165** unchanged, R1 harness **62/62**, `test:mvp` **66/66**, `verify:mvp` **exit 0** (now also running the R1 and B2 validators), typecheck/lint/build clean, `git diff --check` exit 0. `R1_BLOCKED_PENDING_OWNER_BINDING` remains **unresolved and untouched**. Record: [`QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md`](QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md).

- **20.3B2P Universal enforcement staging preflight** — **`B2_PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. Executed at the synchronized HEAD `5ffce96f…` (origin identical, **0/0**, clean tree, parent = the unamended R1 commit `5c78ea37…`). **B2 was NOT applied.** All eleven locked hashes recomputed and verified, with the three B2 hashes cross-checked **three ways** — working tree, the accepted commit object via `git show 5ffce96:<path>`, and the phase documentation — all agreeing; `git diff 5c78ea37..HEAD -- supabase/migrations/` lists only `20260723000500`, so no applied migration was edited. **Offline gates:** B2 validator **57/57**, B1/G validator **165/165**, R1 harness **62/62**, `verify:mvp` **exit 0** (66 test cases + r1 62 + b2 57 + typecheck + lint + build), `git diff --check` exit 0. **Load-bearing was re-proved independently rather than trusted from the validator's self-report:** injecting `begin;/commit;`, `drop table public.lead_assignments;` and a Migration C `create or replace view public.vendor_public_v` into the **real** migration each tripped check 02, and the file was restored **byte-identical** every time; source inspection confirmed all 25 fixtures route through the same `evaluateB2Migration()` that grades the real file, that a no-op mutation is reported **vacuous**, and that check 05 proves every enforced rule has a fixture. **External apply workspace** proved outside Git, with no `seed.sql`, no `supabase/functions` and no non-SQL file: **5 SQL files before** (baseline/A/A2/B1/G, all hash-matching the repository), B2 **absent** so copied exactly once, **6 after**, with `cmp` exit 0 and both copies hashing `ab31023e…` — **byte-identical**. **Linked target** is `uckafzuochmbvtiodmcl` only (`.temp/project-ref`, `linked-project.json` "QuickFurno Staging", staging-only pooler host); **production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` are not linked and were never contacted** — the production ref appears in the workspace only inside the baseline migration's own "MUST NEVER be applied to production" warning comments, which is documentation, not a link. **Migration history before**, parsed structurally from the CLI JSON: **6 local / 5 remote**, remote exactly baseline+A+A2+B1+G, `20260723000500` **local-only** and the **sole pending** version, no unexpected version. **Exactly one** `npx supabase db push --linked --dry-run` ran at **2026-07-23T17:08:21Z → 17:08:26Z UTC**, **exit 0**, printing the `DRY RUN: migrations will *not* be pushed` banner and proposing **exactly one** migration — `20260723000500_qf_mvp_assignment_universal_enforcement.sql` — with **no earlier migration proposed**, **no application claim** (`Applying`/`Applied`/`successfully applied` all absent) and no error text; the command was never repeated. **No write occurred:** the immediate re-list is **byte-identical** to the pre-run listing — still 6 local / 5 remote, B2 still local-only, **zero** remote history rows created. No `db push` without `--dry-run`, no `migration up`/`repair`/`reset`, no hand-executed SQL, no link change, no application data, auth user, provider activation, Edge Function, deployment, PR or push. Transcript outside Git: `qf-staging-workspace\QF-MVP-20.3B2P-PREFLIGHT-20260723T170821Z.txt`. Record: [`QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md`](QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md) section 13.

- **20.3B2A/B2R1 B2 application failure and runtime type correction** — **`B2_RUNTIME_TYPE_CORRECTION_IMPLEMENTED_REVIEWED_READY_FOR_PREFLIGHT`**. The one authorized `npx supabase db push --linked` (2026-07-23T17:22:22Z) exited **non-zero**: after the four `drop trigger if exists` guards (normal NOTICE skips) it aborted at **statement 20**, the `DO $verify$` self-verification block, with `ERROR: operator does not exist: name[] = text[] (SQLSTATE 42883)`. The CLI wraps migration + history insert in one transaction, so the abort **rolled the whole migration back atomically** — read-only re-list proved **6 local / 5 remote** byte-identical to before, `20260723000500` still remote-empty, no B2 object or history row persisted, no retry, no repair/reset/up. **Root cause (my own defect):** `pg_attribute.attname` is catalog type `name`, so `array_agg(a.attname …)` is `name[]`, and `array['lead_id','vendor_id']` is `text[]`; PostgreSQL has no `name[] = text[]` operator. The offline validator (structural — never executes the block) and the dry run (reports *which* migration — never runs its SQL) both passed, because the error is a plan-time type-resolution failure that surfaces only at real application; same defect family as QF-MVP-20.3B1A. **B2R1 correction (offline):** cast the catalog `name` array to `text` on both sides at all five sites — migration §5.7 and verifier rows **B13/B15** — `array_agg(a.attname::text order by a.attname::text) … = array[…]::text[]`; ordering, set membership, null behaviour and cardinality are identical, so **no enforcement semantic changed** and neither the self-verification nor B13/B15 was removed or weakened. A repository-wide search found **no other** `array_agg` over a `name` column and no sibling in any other catalog; every single `name = text` comparison is type-safe and untouched. **Validator hardening:** new rule **R23_catalog_name_array_cast** parses the balanced `array_agg(...)` argument over comment-stripped SQL (whitespace/comment-proof) and flags any uncast aggregate over a known `name`-typed catalog column, enforced on both the real migration (check 02) and the real verifier (check 07b); covered by fixture Z, a discriminating Z-pass/Z-defect pair, and **real-artifact mutation tests** that removed the casts from the actual migration and verifier (each tripped, both restored byte-identical). **Independent runtime-type review** cleared `v_relname`/`v_enabled` (text), `v_tgtype` (smallint vs int), the explicit `text[]` arrays and `unnest(array['…'])` literals — the `array_agg(name)` family was the only defect, and no over-casting was added. **Corrected hashes:** migration `d13703553663271172cfdcedc5e9be8374e7e9c1d225d2c67816fce837450cf3`, verifier `89903749acf61061f5332fe1e57a4808a1bf45c6bb0a929ad703810606270677`, validator `87739ae0abc9e1587754add5016199ad0c6312a23c4f4137e22da55ca08aa00d`. **Gates:** B2 validator **61/61** (26 fixtures), B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. Baseline/A/A2/B1/G byte-unchanged. **No database access, no dry run, no application retry, no production/QF-Jarvis contact, no push.** Record: [`QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md`](QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md) section 14.

- **20.3B2R1P Corrected B2 fresh staging preflight** — **`CORRECTED_B2_PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. Executed at the correction commit `c3350cd…` (origin identical, **0/0**, clean tree). **B2 was NOT applied.** Corrected hashes verified (migration `d13703553663271172cfdcedc5e9be8374e7e9c1d225d2c67816fce837450cf3`, validator `87739ae0…`, verifier `89903749…`); baseline/A/A2/B1/G byte-unchanged; `c3350cd` = exactly the five approved paths. **Offline gates:** B2 validator **61/61** (26 fixtures), B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0; **R23 re-proved load-bearing** — reverting the `::text` cast on the real migration tripped check 02 and on the real verifier tripped check 07b, both restored byte-identical. **Safe workspace correction:** the external workspace held the superseded B2 (`ab31023e…`, state KNOWN_SUPERSEDED_PRESENT) with baseline/A/A2/B1/G hash-exact; **before** replacing, the linked list proved remote B2 **absent** (6 local / 5 remote, sole pending); the superseded file was preserved outside Git and outside the migrations dir as `QF-MVP-20.3B2R1P-SUPERSEDED-B2-ab31023e-20260723T174000Z.sql`, then **only** the workspace B2 was overwritten from the corrected repo file → workspace B2 `d1370355…`, `cmp` byte-identical, migrations dir still six SQL, baseline/A/A2/B1/G untouched. **Linked target** `uckafzuochmbvtiodmcl` only; **production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` not linked, never contacted** (production ref appears only in the baseline migration's warning comments). **Exactly one** `npx supabase db push --linked --dry-run` at **2026-07-23T18:04:38Z → 18:04:42Z UTC**, **exit 0**, DRY RUN banner present, proposing **exactly one** migration — the corrected `20260723000500` — with no earlier migration, no application claim, no error; never repeated. **No write:** immediate re-list byte-identical (6 local / 5 remote), B2 still remote-empty, **zero** remote rows created. No `db push` without `--dry-run`, no `migration up`/`repair`/`reset`, no hand-executed SQL, no link change, no applied migration modified in repo or workspace, no data/auth/provider/deploy/PR/push. Transcript outside Git: `qf-staging-workspace\QF-MVP-20.3B2R1P-PREFLIGHT-20260723T180438Z.txt`. Record: [`QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md`](QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md) section 15.

- **20.3B2R1A Corrected B2 staging application** — **`CORRECTED_B2_APPLIED_VERIFIED_ON_STAGING_READY_FOR_COMMIT_REVIEW`**. At HEAD `4947eec…` (origin identical, 0/0, clean, parent `c3350cd…`), all corrected hashes verified (migration `d13703553663271172cfdcedc5e9be8374e7e9c1d225d2c67816fce837450cf3`, validator `87739ae0…`, verifier `89903749…`), baseline/A/A2/B1/G byte-unchanged, workspace B2 byte-identical (cmp exit 0). Offline gates: B2 validator **61/61** (26 fixtures), B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0; R23 re-proved load-bearing on the real migration (check 02) and real verifier (check 07b), both restored byte-identical. Linked target `uckafzuochmbvtiodmcl` only; **production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` never accessed**. Pre-application history **6 local / 5 remote**, corrected B2 sole pending. **One** `npx supabase db push --linked` at **2026-07-23T18:16:43Z → 18:16:54Z UTC**, **exit 0**, applied **only** `20260723000500` — the self-verification NOTICE reported **4 functions, 4 triggers, 0 unexpected triggers, B1 authority intact** (the corrected `DO $verify$` block ran to completion; the trailing pgdelta-cert Warning is the known post-commit local catalog-cache step and changed nothing). **Post-application history 6 local / 6 remote**, B2 recorded **exactly once**, no pending, no unexpected version; the failed 20.3B2A attempt left no history row (atomic rollback). **Locked SELECT-only verifier** (hash `89903749…` unchanged) returned **34 PASS / 0 FAIL**, none skipped, no DML: 4 functions, both cap functions SECURITY DEFINER + pinned search_path, all four triggers wired (tgtype 23/7/27/34, enabled) with 0 unexpected, the corrected **B13** (UNIQUE lead_id,vendor_id present) and **B15** (event stream not pair-unique) both PASS, 5 canonical B1 functions intact, authority service-role-only, Migration G lineage boundary preserved, owner=postgres informational, legacy RPCs RETAINED, C/D absent, owner-binding deferred, no `in_progress`, RLS on both tables, no live-data cap breach, and all application data 0. **Staging state:** 67 tables, sum of all public rows **0**, auth.users 0, providers 0, C view absent, D trigger absent, legacy RPCs 6, **total public triggers 4** (only B2's). **Advisors (read-only, after 34/34):** security 44 lints (37 INFO rls_enabled_no_policy, 1 WARN always-true `leads` INSERT → C, 6 WARN SECURITY DEFINER executable — all pre-existing); performance 230 lints (153 INFO unused_index empty-DB artifact, 36 multiple-permissive-policies, 30 unindexed FKs, 7 auth_rls_initplan, 3 duplicate_index → C, 1 platform). **No advisor references any B2 object; none blocking; nothing auto-fixed.** One real push only, no repair/reset/up, no hand-executed SQL, no data/auth/provider/deploy/PR/push. Transcript outside Git: `qf-staging-workspace\\QF-MVP-20.3B2R1A-APPLICATION-20260723T181643Z.txt`. Record: [`QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md`](QF-MVP-20-3B2-UNIVERSAL-ENFORCEMENT.md) section 16.

- **20.3CG Public projection + direct-table privilege hardening (generate/review)** — **`C_PUBLIC_PROJECTION_HARDENING_GENERATED_REVIEWED_READY_FOR_PREFLIGHT`**. Generated and reviewed at the synchronized HEAD `7d88519c…` (origin identical, 0/0, clean; no collision for `20260723000600`). **No database contacted, no dry run, nothing pushed.** C closes two anonymous exposures at the DB boundary: the `vendors public listing` RLS policy let anon `select *` every monetization/PII column (remaining_credits, package_name, paid_status, gst_number, phone, email, user_id, precise address), and `leads public insert` was `WITH CHECK (true)`. A full consumer trace proved **every** vendor read and lead write is already server-owned (service role) or a vendor-own authenticated read under RLS — **no anon/browser table access exists**, so C is pure DB hardening with **no runtime consumer code to migrate**. The migration `20260723000600_qf_mvp_public_projection_privilege_hardening.sql` (`0d3d871b…`) creates **`public.vendor_public_v`** — an owner-rights view (security_invoker OFF, documented) exposing **exactly 21 allowlisted public fields and zero forbidden fields**, over the same row set the dropped anon policy exposed; **revokes all direct `vendors` privileges from PUBLIC/anon** and all writes from `authenticated` (keeping vendors SELECT for the dashboard + admin RLS); **revokes all direct `leads` privileges from PUBLIC/anon/authenticated**; **drops** `vendors public listing`, `vendors public register` and the always-true `leads public insert` policies; grants the view SELECT to anon/authenticated/service_role only. service_role GRANT ALL and the safe admin/vendor-own RLS policies are preserved; `get_public_eligible_vendors` keeps its anon EXECUTE. **No D/E/20.4/owner-binding work.** Self-verification is runtime type-safe (attname::text — the B2R1 defect class). **Validator** `validate-qf-mvp-20-3c.mjs` (`9115ed0b…`) **79/79** with **23 one-defect fixtures** plus repository static proofs, and **three real-artifact mutation tests** all caught and restored byte-identical. **SELECT-only verifier** `verify_qf_mvp_20_3c.sql` (`1d2bb61a…`) **23 rows**, catalog-fact PASS/FAIL. **Gates:** B2 validator **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0 (now runs the C validator), typecheck/lint/build clean, `git diff --check` exit 0. Record: [`QF-MVP-20-3C-PUBLIC-PROJECTION-HARDENING.md`](QF-MVP-20-3C-PUBLIC-PROJECTION-HARDENING.md).

- **20.3CP Public projection hardening staging preflight** — **`C_PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. Executed at the C generation commit `13eeac8…` (origin identical, **0/0**, clean tree). **C was NOT applied.** All three C hashes verified (migration `0d3d871b…`, validator `9115ed0b…`, verifier `1d2bb61a…`); baseline/A/A2/B1/G/B2 + B2 verifier/validator byte-unchanged; `13eeac8` = exactly the 7 approved paths, no applied migration. **Offline gates:** C validator **79/79** (23 fixtures), B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0; **load-bearing re-proved on the real artifacts** — leaking gst_number tripped R08(02), removing the anon-leads revoke tripped R11(02), reverting the verifier cast tripped 07b, all restored byte-identical. **Workspace** proven outside Git (no seed, no functions): **6 SQL before** (all hash-exact), C **absent** so copied once, **7 after**, `cmp` exit 0, workspace C `0d3d871b…` byte-identical. **Linked** `uckafzuochmbvtiodmcl` only; **production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` not linked, never contacted**. **History before** 7 local / 6 remote, C local-only and sole pending. **Exactly one** `npx supabase db push --linked --dry-run` at **2026-07-23T19:06:25Z → 19:06:31Z UTC**, **exit 0**, DRY RUN banner, **exactly one** proposed (`20260723000600`), no earlier migration, no apply claim, no error; never repeated. **No write:** immediate re-list byte-identical (7 local / 6 remote), C still remote-empty, **zero** remote rows created. No `db push` without `--dry-run`, no `migration up`/`repair`/`reset`, no hand-executed SQL, no link change, no data/auth/provider/deploy/PR/push. Transcript outside Git: `qf-staging-workspace\QF-MVP-20.3CP-PREFLIGHT-20260723T190625Z.txt`. Record: [`QF-MVP-20-3C-PUBLIC-PROJECTION-HARDENING.md`](QF-MVP-20-3C-PUBLIC-PROJECTION-HARDENING.md) section 13.

- **20.3CA/CVR1 Migration C application and verifier false-negative correction** — **`C_VERIFIER_CORRECTION_IMPLEMENTED_REVIEWED_READY_FOR_STAGING_REVERIFY`**. The one authorized `npx supabase db push --linked` ran **2026-07-24T02:16:04Z → 02:16:09Z UTC**, **exit 0**, applying **only** `20260723000600`; its self-verification NOTICE reported *vendor_public_v (21 cols, 0 leaks), vendors/leads anon access revoked, unsafe policies dropped, B1/B2/G/legacy intact, D absent*. **Post-history 7 local / 7 remote**, C recorded **exactly once**, no pending; staging application data still **entirely empty** (sum of all public rows = 0, auth.users 0, providers 0, public views = 1). The locked verifier returned **22 PASS / 1 FAIL**. **Row C03 was a FALSE NEGATIVE:** it compared a DB-sorted `array_agg(attname::text order by ...)` against a **raw hand-ordered** 21-element literal in which `cover_image_url` and `covers_full_city` were **transposed** — the literal's typed order silently became part of the assertion (`expected 21, actual 21, FAIL`). Proved read-only: re-sorting the same literal in PostgreSQL reproduced the actual array exactly and `set_matches_when_both_sorted = true`; Migration C's own §5.2 passed because it already sorts **both** sides. **No migration, view, ACL, policy or data defect existed** — rows 4–23 all PASSED, proving the hardening is genuinely live (anon holds 0 privileges on vendors and leads, view granted to anon/authenticated/service_role only, 3 unsafe policies gone, B1 5/5, B2 4/4, G boundary intact, legacy 6 retained, D absent). **CVR1 correction:** C03 now normalizes **both** sides in PostgreSQL (`array_agg(x order by x) from unnest(array[...])`), and the literal is **deliberately left hand-transposed** to prove the normalization is load-bearing rather than a cosmetic re-sort. Contract preserved exactly: 23 rows, C03 row name, `expected '21'`, the identical 21-field set, SELECT-only, nothing weakened or removed. **Validator hardening (R20):** a backward-window detector rejects any `array_agg(...)` compared to a raw `array[...]` literal (membership `= any(array[...])` correctly exempt), plus check 13b (C03 and its allowlist must remain) and a discriminating 13c/13d control pair; a **real-artifact mutation** reverting C03 to the original asymmetric form made the validator FAIL and the verifier was restored byte-identical. One defect was found *in the hardening itself* — the first detector walked forward from `array_agg(` and was defeated by the intervening `from … )`, so control 13d did not fire; it was rewritten as a backward scan. **Order-sensitivity search:** 1 FIX_REQUIRED (C03, corrected); B2's B13/B15 literals are already in correct sort order so no defect is proven and that locked artifact is out of scope; all `= any(array[...])` and `unnest(array[...])` sites are order-insensitive. **Hashes:** applied Migration C **UNCHANGED** `0d3d871b…`; corrected verifier `1f7bf9a511eb77f37578ef92771fdddf85cd2aa0522ac4648a7041b56586a980`; hardened validator `d632aa2584976cce1ac6058e782ac1910675c3cbaa70ccf6f30593f9c2c3725d`; baseline/A/A2/B1/G/B2 and the B1/G + B2 validators/verifiers byte-unchanged. **Gates:** C validator **83/83** (79 prior + 4 hardening), B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **No database access, no dry run, no verifier re-run, no migration application/repair/reset/up, no production or QF-Jarvis contact, no push.** Record: [`QF-MVP-20-3C-PUBLIC-PROJECTION-HARDENING.md`](QF-MVP-20-3C-PUBLIC-PROJECTION-HARDENING.md) section 14.

- **20.3CVR1V Migration C read-only staging re-verification and completion** — **`C_APPLIED_VERIFIED_COMPLETE_READY_FOR_DOCUMENTATION_PUSH`**. Executed at the verifier-correction commit `003f0190…` (origin identical, **0/0**, clean). **Read-only phase: no db push, no dry run, no migration applied/reapplied/repaired/reset, no data mutation.** Migration C hash re-verified **IMMUTABLE** (`0d3d871b…`); corrected verifier (`1f7bf9a5…`) re-hashed immediately before execution; corrected validator (`d632aa25…`); baseline/A/A2/B1/G/B2 and the B1/G + B2 validators/verifiers byte-unchanged. **Offline gates:** C validator **83/83**, B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0; C03 symmetry re-proved from source and a mutation reverting it to the raw hand-ordered literal made check 13 FAIL, with the verifier restored byte-identical. **History (read-only): 7 local / 7 remote**, C recorded exactly once, no pending. **The corrected verifier ran exactly once (2026-07-24T02:44:46Z UTC), SELECT-only, returning 23 PASS / 0 FAIL — none skipped, no expectation altered, no DML.** Row **C03 is now PASS** (expected 21, actual 21), closing the earlier false negative. **Live posture proved:** `vendor_public_v` exists once with **exactly** the 21-column allowlist and **zero** forbidden columns; owner-rights (security_invoker OFF); grants narrow (anon + authenticated + service_role, PUBLIC excluded); **PUBLIC/anon hold no direct `vendors` privilege**; `authenticated` is **SELECT-only** on `vendors`; **PUBLIC/anon/authenticated hold no direct `leads` privilege**; service_role preserved; all three unsafe policies absent; safe owner/admin policies remain; RLS enabled; `get_public_eligible_vendors` keeps anon EXECUTE; B1 5/5, B2 4/4, G boundary intact; 6 legacy RPCs RETAINED with EXECUTE untouched; D absent; owner binding deferred. **Empty state:** 67 tables, sum of all public rows **0**, auth.users 0, providers 0, **public views = 1 (`vendor_public_v`, 0 rows)**, total public triggers 4, history rows 7. **Advisors:** security **44** — 37 INFO rls_enabled_no_policy (pre-existing), **1 ERROR `security_definer_view` on `vendor_public_v` (C-related, EXPECTED, non-blocking — all four acceptance conditions verified live: anon base access revoked, denied columns absent, explicit allowlist + deterministic filter, anon view-only)**, 6 WARN SECURITY DEFINER executable (pre-existing); performance **227**, **zero** referencing any C object. **Two C-related IMPROVEMENTS confirmed live:** the pre-C `rls_policy_always_true` WARN on `leads` is **gone**, and `multiple_permissive_policies` fell **36 → 34**. Nothing auto-fixed; nothing blocking. Production and QF-Jarvis **never accessed**; no PR, deployment or push. Transcript outside Git: `qf-staging-workspace\\QF-MVP-20.3CVR1V-REVERIFY-20260724T024446Z.txt`. Record: [`QF-MVP-20-3C-PUBLIC-PROJECTION-HARDENING.md`](QF-MVP-20-3C-PUBLIC-PROJECTION-HARDENING.md) section 15.

- **30.5C1/C2 Application integration and final workflow smoke — MVP-30 COMPLETE** — **`QF_MVP_30_VENDOR_CRM_COMPLETE_READY_FOR_PRIVATE_SERVER_STAGING_DEPLOYMENT`**. **Authorized staging `uckafzuochmbvtiodmcl` only**; production and QF-Jarvis never contacted. **No Meta/WhatsApp/n8n/SMS/email/webhook transport anywhere in MVP-30; no deployment occurred.** Migrations through **`20260728001600`** applied and verified (count **17**, 1600 latest); `verify_qf_mvp_30_5b2b.sql` **18/18** and `verify_qf_mvp_30_5a.sql` **24/24 in scope** in `READ ONLY` transactions — its other four checks are point-in-time assertions whose documented precondition expired once 1600 was applied and fixtures were exercised, so each durable invariant was re-proven instead of weakening committed evidence. **Final real application workflow smoke 56/56** through a genuine `next start` process on 127.0.0.1 with a real SSR Superadmin session and the ACTUAL content-hashed server actions over HTTP — no mocked Supabase, no direct service substitution. Unauthenticated 307 and forged session failed closed; genuine CRM admin succeeded. **Approval created ZERO intents**; handoff is a separate explicit action, fail-closed on both approval state and active policy. With no policy the application returned **`FREQUENCY_POLICY_NOT_CONFIGURED`** and created zero intents; after publishing a TEST-ONLY policy through the real operator action, handoff created exactly **one provider-neutral `pending` intent** with an opaque sha256 recipient reference, counts reconciling exactly, and replay created **no duplicate**. Segment drift mapped to `SEGMENT_EVIDENCE_MISMATCH` and consent revocation to `skippedConsent = 1`, both with zero new intents and no PII. Duplicate active policy refused, empty threshold rejected rather than defaulted, retired policy retained in history with values unchanged and reactivation refused. **No active frequency policy exists — the threshold remains an owner decision.** All `qf_30_5*` fixtures retired through canonical lifecycle with **no DELETE**: zero active objects, zero sendable intents, append-only rows preserved. **Dispatch-time consent/suppression revalidation remains owned by the later transport phase.** **Next: private server staging deployment**, then QF-MVP-40 → 50 → 70 → 60 → 80.
- **30.5A/B Campaign execution handoff — staging validated (COMPLETE)** — **`QF_MVP_30_5B_CAMPAIGN_HANDOFF_STAGING_VALIDATED_READY_FOR_APPLICATION_INTEGRATION`**. **Authorized staging `uckafzuochmbvtiodmcl` only**; production and QF-Jarvis never contacted. **No Meta/n8n/WhatsApp/SMS/email/webhook call, no provider message id, no delivery claim, no outbound flag; deployment deferred until MVP-30 completes.** Migrations **`20260728001500`** (handoff foundation) and **`20260728001600`** (policy history hardening) each applied **once and latest** in a single transaction with its history row, prior rows unchanged, staging count 15 -> 16 -> **17**; both applications created **zero** intents and **zero** policy rows. Verifiers **`verify_qf_mvp_30_5a.sql` 28/28** and **`verify_qf_mvp_30_5b2b.sql` 18/18**, each run once in a `READ ONLY` transaction before fixtures. Functional behaviour **57/58** and concurrency **42/42** on real fixtures: segment/template drift fail closed; the `trg_vsg_definition_pair` triple is enforced (definition-only, fingerprint-only and no-version-bump all refused, legitimate atomic update succeeds); consent revocation, STOP suppression and a non-canonical phone each exclude deterministically; replay creates no duplicate; **all six** intent statuses count toward frequency. **Cross-campaign serialization proven with observer evidence** — B blocked on `wait_event = advisory` with the **same** `pg_locks` advisory object granted to A and ungranted for B, then resumed, counted A's intent and was frequency-blocked, leaving one live intent against a threshold of one; overlapping batches with reversed shared recipients produced **no deadlock**. **30.5B2A's one real finding** — `service_role` could DELETE/TRUNCATE the policy table because 1500 revoked only `public`/`anon`/`authenticated` while `pg_default_acl` grants `arwdDxtm` — is closed by 1600: ACL reset, historical meaning frozen, DELETE and TRUNCATE refused **by trigger**, retirement still one-way and a new policy version still insertable. **No frequency number was ever seeded** — it remains an owner decision. All `qf_30_5b2a_*`/`qf_30_5b2b_*` fixtures retired through canonical lifecycle with **no DELETE**: zero active campaigns/segments/templates/vendors/policies, zero sendable intents, append-only audience and event rows preserved. **Dispatch-time consent/suppression revalidation remains owned by the later execution/transport phase.** **Next: QF-MVP-30.5C — application integration and Vendor CRM closeout.**
- **30.4D Vendor campaign management staging validation (COMPLETE)** — **`QF_MVP_30_4_VENDOR_CAMPAIGN_MANAGEMENT_COMPLETE_READY_FOR_EXECUTION_HANDOFF`**. **Authorized staging `uckafzuochmbvtiodmcl` only**; production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` never contacted. **No provider/Meta/n8n/WhatsApp/SMS/email/webhook request; `communication_intents` 0 before and 0 after; no frequency, dispatch or delivery authority added — QF-MVP-30.5 still owns all of it.** **30.4D-A application workflow smoke — 64/64**, driving the **real Next.js server actions over HTTP** with a genuine `@supabase/ssr` admin session. Two harness defects were caught before any result was trusted: a proximity heuristic had assigned **three different server actions the same 40-hex action id** (replaced with a deterministic `.next/server/app/**/page.js` registry parse plus a two-way uniqueness assertion), and a PostgREST count with **`head:true` masked a missing table** as `204`/`null`, indistinguishable from a real zero (counting without `head` surfaces `PGRST205`). A third was environmental: a bare `npm run build` **baked production environment values** into `.next`, after which every request `307`'d pre-auth and no action ran. **30.4D-A1 staging build environment gate — commit `40c4bee`, 43/43 including 4 mutants** proving the effective-ref check, post-build scan, outbound-flag check and child-failure check are each load-bearing; a child that could not be *started* is a failure, never a pass; **`npm run build` is unchanged** and normal production builds require no staging markers. **30.4D-B two-connection behavioural lock smoke — 46/46**, over the **session pooler (5432)** with two independent `pg.Client` sessions on **distinct backend PIDs**, each transaction proven to persist across separate round trips. **Four blocking proofs** — prepare×segment, prepare×template, approve×segment, approve×template — each with the **real** RPC returning `ok:true` inside A's open transaction while B's concurrent non-key `UPDATE` blocked: `pg_stat_activity.wait_event_type='Lock'` (`wait_event=transactionid`), `pg_locks` showing an **ungranted `ShareLock`**, B not completed while A held the lock, and B completing **only after A committed** (620–657 ms). **`FOR SHARE` proven MINIMUM sufficient by two controls:** a concurrent second `FOR SHARE` re-acquired in 174–194 ms (two campaigns pinning one segment do **not** serialise), and under **`FOR KEY SHARE` the same non-key `UPDATE` completed unblocked in 64 ms**, confirming it would have left the dispatch-critical non-key field class unprotected. **Evidence freeze:** the approved `snapshot_fingerprint` equalled a value computed **independently of the database**; after drifting both sources the approved evidence stayed byte-identical, approve replay returned `replayed:true`, and a fresh prepare was refused `SEGMENT_EVIDENCE_MISMATCH`; audience `UPDATE`/`DELETE`, campaign `DELETE` and event `DELETE` all refused `P0001`. A genuine `TEMPLATE_FINGERPRINT_MISMATCH` was observed when the blocking `UPDATE` touched `description`, which **is** a fingerprint input — the hardening working, not a defect; the pass was redone on `updated_at`. **Retirement is archival by design** (`trg_vcm_no_delete`, `trg_vcam_immutable`, `trg_vce_immutable` make these tables append-only): all seven `qf_30_4d_b_*` campaigns archived, fixture segment archived, fixture template `is_active=false, readiness_status='disabled'`, and a prepare against them refused `SEGMENT_ARCHIVED`. **Honest permanent staging delta:** campaigns 29→36, audience members 28→36, events 114→120, segments 8→10, templates 2→4; **`vendors` 5 and `lead_assignments` 0 untouched**. No session left idle in transaction, no lock left waiting, **1,501 files scanned with zero** occurrences of the connection string or password. **Direct-database capability:** the pooler URL lives in a DPAPI-encrypted vault outside Git, injected only by the staging wrapper; **three runs were blocked on `28P01`** before it resolved and those blocked reports are retained unaltered. **Two Supavisor topology facts are load-bearing for any future pooled test:** `application_name` is **rewritten by the pooler** (correlate sessions by backend PID), and **`pg_stat_ssl` describes the pooler→Postgres leg**, reporting `ssl=false` for a pooled session whose client leg is encrypted — client-leg TLS was asserted at the socket (**TLSv1.3 / TLS_AES_256_GCM_SHA384**) and corroborated by the same credentials on the direct host reporting `pg_stat_ssl.ssl=true`. **Gates:** 30.4D-B 46/46, 30.4D-A 64/64, build gate 43/43, 30.4A 151/151, `test:mvp` 110/110, **`verify:mvp` exit 0**, typecheck/lint clean. **Next: QF-MVP-30.5 — execution handoff (NOT_STARTED)**; it must own provider mapping, a fail-closed frequency rule and a send-time recheck of both fingerprints, since approval authorises an audience and never dispatches. Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §30.

- **30.4C3 Campaign evidence hardening staging APPLICATION + verification** — **`QF_MVP_30_4C3_CAMPAIGN_EVIDENCE_HARDENING_APPLIED_VERIFIED_READY_FOR_STAGING_SMOKE`**. Applied from pushed source commit **`907906d5661c3c454472ffd459f0e17b5d86298b`** (`fix(mvp): lock campaign evidence sources`, the branch tip on origin). **Authorized staging `uckafzuochmbvtiodmcl` only**; production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` never contacted; **no provider call; no fixture created; this record is documentation only — no runtime/migration/validator/verifier change.** **Application:** pre-state **14** migrations (latest `20260723001300 qf_mvp_vendor_campaign_foundation`) → applied exactly one, **`20260723001400 qf_mvp_vendor_campaign_evidence_hardening`** (SHA-256 `C554704378E4D673273344C797A3B68EB875ACE766553C3F5AB5BB81CF30E31A`, **57,619 bytes**), recorded **exactly once** → final **15**, latest version `20260723001400`. **Method — version-preserving and byte-exact:** the file was transferred as base64 chunks and reconstructed server-side to exactly 57,619 bytes; a **database-side SHA-256 guard matched the locked migration hash** before execution; one transaction re-checked the 14-migration/zero-row pre-state, executed the exact migration with its embedded `$preflight$`/`$verify$` self-verification, and inserted the history row with the **exact version and name** — no auto-generated timestamp, no partial DDL, no migration-repair command; it proved 15 migrations and zero campaign rows before committing; the temporary transfer object was removed and **no residue remains**. **Post-state (verified read-only):** all three fingerprint functions present (`qf_canonical_text_field_v1`, `qf_campaign_snapshot_fingerprint_v1`, `qf_communication_template_fingerprint_v1`); prepare/approve **signatures unchanged** with bodies replaced; **lock contract live in BOTH RPCs — campaign `FOR UPDATE` → segment `FOR SHARE` → template `FOR SHARE`**; **no `FOR KEY SHARE`**; database-authoritative snapshot **and** template fingerprints live in both prepare and approve; `vcm_prepared_evidence_complete` now requires a non-null `prepared_template_fingerprint`; RLS enabled on all three campaign tables with **0** untrusted policies; campaign/audience/event rows **0/0/0**. Both 30.4C1 fingerprint defects and the 30.4C2 concurrency defect are therefore **closed on staging**. **Verifier** `supabase/staging-verification/verify_qf_mvp_30_4c1.sql` (SHA-256 `99F07CC28787832E41D8F8BB6F4330F67B4BF6F33B9CB4CD28B1DD6C6A7069A1`, **24,496 bytes**) run **exactly once after application and before fixtures — 34/34 PASS (C01–C34)**, every statement executed exactly once. **The zero-row verifier must not be re-run once fixtures exist**; `verify_qf_mvp_30_4.sql`, `verify_qf_mvp_30_3.sql` and `verify_qf_mvp_30_1b.sql` were **not** re-run. **Zero mutation:** campaign/audience/event **0**, `vendor_credit_logs` **0**, `lead_assignments` **0**, `vendor_package_orders` **0**, `communication_consent_events` **0**, `communication_suppressions` **0**, `communication_intents` **0**; no package/credit/assignment/consent/suppression/provider mutation, no campaign communication intent, no provider canary, no outbound request. **Intentional advisor posture:** campaign tables report `rls_enabled_no_policy` at **INFO** — the designed server-only default-deny (RLS on, no anon/authenticated policy, untrusted roles zero privileges, `service_role` the only application data-access role); **do not add policies to silence it**; no new campaign-related ERROR/WARN, and unrelated pre-existing findings were not auto-fixed. **Next: QF-MVP-30.4D — full campaign staging workflow + two-connection concurrency smoke.** No fixture, no smoke and no QF-MVP-30.5 work in this phase. Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §29.

- **30.4C2 Campaign evidence-source row locking (GENERATED, NOT APPLIED)** — **`QF_MVP_30_4C2_CAMPAIGN_EVIDENCE_LOCKING_COMPLETE_READY_FOR_STAGING_APPLICATION_REVIEW`**. QF-MVP-30.4C1 correctly made both fingerprints database-authoritative but was **not** authorised for push or staging application: one **concurrency** defect remained. **The defect:** both RPCs locked the campaign head `FOR UPDATE` yet read the two authoritative evidence rows — `public.vendor_segments` and `public.communication_templates` — with **no locking clause at all**. Reproduced structurally: exactly two `FOR UPDATE` clauses existed in the entire migration (the two campaign heads) and **zero** `FOR SHARE` / `FOR NO KEY UPDATE` / `FOR KEY SHARE` anywhere. That permits: T1 locks the campaign head, verifies segment and template, T2 commits `UPDATE communication_templates SET provider_template_name=…` (taking `FOR NO KEY UPDATE`, which nothing conflicts with), then T1 commits — **approving evidence that was already stale**. **Lock mode `FOR SHARE`, the minimum sufficient mode:** a plain non-key UPDATE takes `FOR NO KEY UPDATE` and a DELETE takes `FOR UPDATE`; `FOR SHARE` conflicts with **both**, so every UPDATE and DELETE of a locked row blocks until the transaction ends. **`FOR KEY SHARE` is insufficient** — it does not conflict with `FOR NO KEY UPDATE`, exactly the class of change at issue. `FOR NO KEY UPDATE`/`FOR UPDATE` would also work but are stronger than needed and would needlessly serialise two campaigns pinning the same segment. **Deterministic lock order campaign → segment → template**, identical in both RPCs, so prepare and approve can never deadlock against one another; in approve both evidence locks are taken **before the first evidence check** and are still held at the approving UPDATE and the event INSERT; both RPCs short-circuit idempotent replay **before** taking evidence locks so a replay never blocks a concurrent edit. **Preserved exactly:** a locking clause does not change which rows match, so `SEGMENT_MISSING`/`TEMPLATE_MISSING` are unchanged; database-authoritative fingerprints, the `QFC01` subtransaction rollback, idempotency, privacy, no-Core-write and no-provider rules are untouched. `SELECT … FOR SHARE` needs UPDATE privilege alongside SELECT — both RPCs are SECURITY DEFINER so the **owner** must hold it, not `service_role`, asserted by self-verification §9.13(f) and verifier row C33. **Enforcement:** the migration re-asserts the contract against the **installed** definitions via `pg_get_functiondef` with comments stripped first, so a comment mentioning FOR SHARE can never satisfy a lock assertion. New validator rules `H11_evidence_row_locks`, `H12_deterministic_lock_order`, `H13_lock_contract_selfcheck` with **7** one-defect fixtures (AG–AM), each restoring the exact race or a plausible half-fix; all trip. **Gates:** 30.4A **151/151** (13 migration + 2 contract + **19** hardening fixtures), 30.4C 101/101, 30.3C 64/64, 30.3 120/120, 30.2 62/62, 30.1B 46/46, blueprint 36/36, `test:mvp` 66/66, R1 62 · B2 61 · C 83 · D 110 · E 51 · 20.4A 39 · 20.4C 42 · 20.5A 40, `verify:mvp` exit 0, typecheck/lint clean, clean wrapper-mediated rebuild with zero prohibited leakage, `git diff --check` exit 0. **Verifier** `verify_qf_mvp_30_4c1.sql` — still unapplied and unrun, so updated in place — grows 28 → **34** SELECT-only rows (C28–C33 lock contract; projection renumbered C34); **not run in this phase**. `20260723001300` and the three historical verifiers remain **byte-for-byte unchanged**. **Planned QF-MVP-30.4D concurrency smoke (NOT performed):** connection A holds the segment/template locks during approval; connection B's UPDATE of either source row must **block** until A completes; after A commits, a later edit must be **detected** by the approved-evidence recheck; **no send or provider path is involved**. **No staging/production/Jarvis/provider access, no fixture, no migration application, no push.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §28.

- **30.4C1 Campaign approval-evidence hardening (GENERATED, NOT APPLIED)** — **`QF_MVP_30_4C1_CAMPAIGN_EVIDENCE_HARDENING_GENERATED_READY_FOR_STAGING_APPLICATION_REVIEW`**. **QF-MVP-30.4C was BLOCKED from push and staging smoke** by a governing audit that found **two real approval-integrity defects** — not UX gaps — in the applied `20260723001300`. Both were reproduced against the accepted code before any correction was written. **Defect 1 — the snapshot fingerprint was never database-verified:** prepare accepted `p_snapshot_fingerprint` from the caller, stored it verbatim after only a `^[0-9a-f]{64}$` shape check, and approval compared row **count** alone; neither RPC contained a single `sha256`/`digest` call, so a shape-valid but unrelated fingerprint survived prepare AND approval with the count still matching, and the declared `SNAPSHOT_FINGERPRINT_MISMATCH` code was **unreachable from SQL** while the column comment already claimed a recheck that did not exist. **Defect 2 — the template fingerprint was missing entirely:** `prepared_template_fingerprint` was nullable, **absent from the prepared-evidence completeness CHECK**, written from an unverified caller value and **never read at approval**; the 30.4C runtime passed `null` on every prepare, and approval compared only version/category/readiness/is_active, so `template_key`, `channel`, `description`, `language`, `provider_template_name` or `provider_template_id` could change **under an unchanged version** undetected — e.g. repointing `provider_template_name` at a different approved provider template would silently re-target an already-approved frozen audience. **`20260723001300` remains IMMUTABLE and applied** — nothing about it was edited, rewritten, renamed or re-applied (its sha256 `3a92fd06…` is asserted by the validator). The correction is **one forward migration, `20260723001400_qf_mvp_vendor_campaign_evidence_hardening.sql`**, adding exactly three deterministic functions — `qf_canonical_text_field_v1`, `qf_campaign_snapshot_fingerprint_v1`, `qf_communication_template_fingerprint_v1` — replacing the two RPC **bodies** via `create or replace` (signatures and parameter names unchanged) and tightening one CHECK. **No table, no audience header, no provider table, no intent state, no frequency policy, no dispatch state, no destination column, no `audit_logs`/`admin_notifications`.** **Canonical snapshot contract:** `'qf-campaign-snapshot-v1'` + per row `RS(0x1E)` + `ordinal US vendor_id US disposition US reason_code US policy_version US suppression_reason` (`US`=0x1F), lowercase sha256 hex — a **fixed-position tuple** encoding replacing the previous JSON-object form, which depended on object key ordering. Excludes row ids, wall-clock values, mutable metadata, PII and provider payloads; returns **NULL** (never a partial hash) on an empty, sparse, duplicated, contaminated or out-of-charset set. **Canonical template-catalog contract:** ten fields in a locked order (`template_key, version, channel, category, language, readiness_status, is_active, provider_template_name, provider_template_id, description`), each **length-prefixed** so free text cannot forge a field boundary; excludes the row id and timestamps. **This is the CATALOG fingerprint only — explicitly NOT a provider-mapping or message-body fingerprint, because this schema stores neither; QF-MVP-30.5 owns provider mapping and send-time rechecks.** **Prepare is now database-authoritative:** it inserts the rows, then inside an explicit subtransaction verifies persisted count, snapshot ownership and dense ordinals, computes the fingerprint **from the inserted rows**, and compares it with the server-supplied *expectation*; a mismatch raises a dedicated `QFC01` SQLSTATE so every inserted row **rolls back** (only `QFC01` is caught, so a genuine constraint/trigger error still aborts the transaction). The head stores **only** database-computed values. **Approval now recomputes both fingerprints** under the existing row lock and fails closed, making `SNAPSHOT_FINGERPRINT_MISMATCH`, `SNAPSHOT_OWNERSHIP_MISMATCH`, `SNAPSHOT_ORDINAL_INVALID` and `TEMPLATE_FINGERPRINT_MISMATCH` reachable for the first time; it still never resolves recipients, never alters audience rows, creates no intent and calls no provider. **Prepared evidence now requires a non-null `prepared_template_fingerprint`** for ready/approved (drafts unaffected), with a fail-closed preflight that **refuses to run** — naming the count and remedy — rather than fabricating a fingerprint for a pre-existing prepared row. **Runtime:** the service reads the full dispatch-critical template field set, computes the canonical fingerprint in pure TypeScript and passes it as the expected value — **it no longer passes null**; four new stable codes map to fixed admin text; the review UI now displays the frozen template fingerprint. **Cross-language parity** is proven by field-order extraction from the migration, header/separator literal comparison, and **pinned golden vectors** (`f6807a63…` snapshot, `d9af14ec…` template) asserted on the TypeScript side by `test:crm:30-4c` and on the SQL side by rows C13/C14 of the new verifier. Honest limit: PostgreSQL is not executed offline, so the SQL half is proved on staging. **Gates:** 30.4A **143/143** (13 migration + 2 contract + **12 hardening** one-defect fixtures, all tripping), 30.4C **101/101** (15 service + 3 action + 2 route + 1 client), 30.3C 64/64, 30.3 120/120, 30.2 62/62, 30.1B 46/46, blueprint 36/36, `test:mvp` 66/66, R1 62 · B2 61 · C 83 · D 110 · E 51 · 20.4A 39 · 20.4C 42 · 20.5A 40, `verify:mvp` exit 0, typecheck/lint clean, clean wrapper-mediated rebuild with a zero-hit prohibited-ref/service-key scan, `git diff --check` exit 0. The 30.4A migration ceiling was a phase-scoped trap this authorised forward migration would have tripped; it is re-based onto "appears once, every later migration is a declared later phase" with a regression assertion. **New verifier** `supabase/staging-verification/verify_qf_mvp_30_4c1.sql` — 28 SELECT-only rows, to run **exactly once** after `20260723001400` and **before any fixture**; **not run in this phase**. `verify_qf_mvp_30_4.sql`, `verify_qf_mvp_30_3.sql` and `verify_qf_mvp_30_1b.sql` are **byte-for-byte unchanged** (hashes asserted). **No staging or production access, no fixture, no migration application, no push.** **Next: controlled staging application of `20260723001400`, then QF-MVP-30.4D.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §27.

- **30.4C Admin campaign management runtime (IMPLEMENTED, runtime/UI)** — **`VENDOR_CAMPAIGN_RUNTIME_IMPLEMENTED_NOT_SMOKED`** (superseded in part by 30.4C1 — its approval evidence was defective; see the 30.4C1 entry above). Built on the applied `20260723001300` foundation. **No migration added, edited or re-applied; no staging project contacted; no campaign fixture created; nothing pushed.** **Surface (admin-only):** `services/vendorCampaignService.ts` (server-only, the single writer), `app/actions/vendorCampaignActions.ts` (guarded actions, actor from session only), `app/admin/vendor-crm/campaigns/page.tsx` + `[campaignId]/page.tsx`, `components/admin/crm/campaigns/VendorCampaignDirectory.tsx` + `VendorCampaignEditor.tsx`, and the **pure** `lib/crm/campaignAudiencePlan.ts`. **No public route, no vendor route, no API route** — campaign mutation is server-action only behind `requireCrmAdmin()`. **Freeze at prepare:** `prepareCampaignForReview` resolves the segment through the existing deterministic engine and then calls **only** `qf_prepare_vendor_campaign_v1`, so head + audience rows + the `prepared` event are written in **one transaction inside the RPC**; the service never inserts an audience row and `approveVendorCampaign` delegates entirely to `qf_approve_vendor_campaign_v1`, **never resolving or altering recipients**. **Consent from the sole authority, batched:** `decideCommunicationConsent` is reused unchanged, but its default deps issue one suppression + one preference query **per call**, so `buildBatchConsentDeps` backs them with maps loaded in **two** batched reads — each per-vendor decision performs **zero DB I/O** while suppression precedence, re-consent and policy-version handling run untouched; one frozen `evaluatedAt` for the whole freeze; duplicate rows preserved so the authority can still detect an integrity violation; a NULL vocabulary field projected as the literal `"null"` so corruption **fails closed** rather than being repaired into permission. **Inclusion rule LOCKED:** `blocked` never included; **marketing** includes **only** `marketing_opted_in` (`unknown`/`no_consent_objection` are absence of objection, not marketing permission); **transactional** additionally includes those two; exclusions reduced to sanitized reason **counts** only (`consent_blocked`, `suppressed`, `vendor_disabled`, `vendor_unverified`, `missing_contact_channel`, `duplicate`) — **no excluded identity is ever stored**. **Destination handling:** a vendor phone is read **in memory only**, solely to derive the canonical `hashPhoneE164` hash the authority requires, then discarded — never stored, returned or logged; reusing the one canonical hasher is a correctness requirement (a second implementation would silently miss stored suppression rows and make a suppressed vendor look eligible). **Bounded work:** candidates page the segment engine at **≤100** under a hard page cap so query count is `ceil(candidates/100)`, never per vendor; cross-page ids de-duplicated and counted as `duplicate`; an audience above **5000** is **refused**, never truncated. **Event provenance stated honestly:** prepare/approve are atomic inside their RPCs; `create`/`update`/`return_to_draft`/`cancel`/`archive` have no RPC and PostgREST cannot span two statements, so they write the **head first** (authoritative status/revision/actor/timestamp) then append a best-effort event — **no atomicity is claimed that the schema cannot provide**, and head-first ordering means a failure can lose a trail line but never assert a transition that did not happen. **Return-to-draft retains prepared evidence** because the prepare RPC derives the next snapshot revision from `coalesce(prepared_snapshot_revision,0)+1`; the sole exception is `prepared_template_category`, cleared when a draft edit changes consent scope or template so a stale value cannot trip `vcm_marketing_requires_marketing_template`. **Optimistic concurrency** on every non-RPC transition (expected `revision` + UPDATE predicated on the observed value); both RPCs enforce the same token under a row lock. **Still out of scope deliberately:** no communication intent, no provider call, no message rendering, no delivery state, no test-send, and **no frequency authority — none exists; until QF-MVP-30.5 adds a fail-closed frequency gate, no campaign may send**. Both admin surfaces state “approval is not a send” and name that gate. **No Core table is written.** **Self-review corrections before commit (4 real defects):** an **unchunked `in()`** would have built a ~325 KB GET URL for a maximum audience and been rejected — every evidence read is now chunked at **200** ids; **`??` collapsed “omitted” into “explicit null”** so a segment/template could never be de-selected — omission now keeps the stored value while an explicit null clears it; the **channel had two sources** (batches used `campaign.channel`, the consent call hardcoded `whatsapp`) — now one validated variable; a **returned-to-draft campaign displayed its retained snapshot as if current** — now labelled “Last frozen audience” with an explicit note. Rules `W11_bounded_reads` (chunking) + `W15_explicit_clear` added with fixtures S/T. **Gates:** new **30.4C validator 77/77** (14 service + 3 action + 2 route + 1 client one-defect fixtures, all proven to trip; real pure planner executed; the consent authority graded **statically** because the MVP loader refuses to resolve Supabase/service modules), 30.4A **113/113**, 30.3C 64/64, 30.3 120/120, 30.2 62/62, 30.1B 46/46, blueprint 36/36, `test:mvp` 66/66, R1 62 · B2 61 · C 83 · D 110 · E 51 · 20.4A 39 · 20.4C 42 · 20.5A 40, `verify:mvp` **exit 0**, typecheck/lint clean, clean **wrapper-mediated** `.next` rebuild (428 files) with a **zero-hit** prohibited-ref scan and zero `service_role`/`SUPABASE_SERVICE`/`vendor_campaigns` occurrences in 83 client bundles, `git diff --check` exit 0. **30.4A checks 12/13/14 re-based** from “no campaign runtime exists yet” (a ceiling that expires) onto the enduring boundary they protected — routes admin-guarded with no service-role client, service server-only and freezing the audience only via the RPC with guarded actions, UI holding no credential and never value-importing the service — each negative-controlled to confirm it still discriminates. **Next: QF-MVP-30.4D — campaign staging smoke; not started.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §26.
- **30.4B Vendor campaign foundation staging APPLICATION + verification** — **`VENDOR_CAMPAIGN_FOUNDATION_APPLIED_AND_VERIFIED_ON_STAGING`**. Applied from foundation commit `4e7c80b1…` (`feat(mvp): add vendor campaign management foundation`, parent `b742d3ff…`, origin identical, **0/0**, clean). **Authorized staging `uckafzuochmbvtiodmcl` only**; production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` never contacted; **no provider call; no fixture created; this record is documentation only — no runtime implementation.** **Application:** pre-state **13** migrations (latest `20260723001200 qf_mvp_vendor_segment_foundation`) → applied exactly one, **`20260723001300 qf_mvp_vendor_campaign_foundation`**, recorded **exactly once** → final **14**. Applied **transactionally via the authorised Supabase Management API** with fail-closed preflight, exact version/name preservation and the migration's own self-verification. **Post-state:** the three campaign objects exist — `public.vendor_campaigns`, `public.vendor_campaign_audience_members`, `public.vendor_campaign_events` — all with **RLS enabled and no untrusted policy**; `service_role` holds SELECT+INSERT+UPDATE on the head and **SELECT+INSERT only** on audience + events, with **no DELETE/TRUNCATE anywhere**; both RPCs exist and are service_role-only (`qf_prepare_vendor_campaign_v1`, `qf_approve_vendor_campaign_v1`); `communication_templates.category` now permits `authentication`, `business`, **`marketing`**; **`communication_intents.aggregate_type` was NOT widened**; campaign/audience/event rows before fixtures **0**; no provider activation; no campaign communication intent; **no Core financial, assignment, consent, suppression or package mutation**. **Verifier** `supabase/staging-verification/verify_qf_mvp_30_4.sql` run **exactly once after application and before fixtures — 32/32 PASS (C01–C32)**. **The zero-row verifier must not be re-run once fixtures exist**, and `verify_qf_mvp_30_3.sql` is likewise not re-run. **Corrected bookkeeping incident (transparent):** after the successful 32/32 verification an accidental connector invocation briefly created one migration-history entry (version `20260725134825`, name `noop_do_not_apply`, sole statement `select 1;`) which produced **no schema or data change**; the exact row was removed only after proving exactly one matching record existed, containing exactly one statement, and that the statement was exactly `select 1;`. Final verified state: migration count **14**, accidental no-op count **0**, campaign migration count **1**, three campaign tables present, campaign/audience/event rows **0**, `vendor_credit_logs` **0**, `lead_assignments` **0**. Corrected tooling/bookkeeping incident — **not** an application or architecture defect. **Intentional advisor posture:** `rls_enabled_no_policy` may appear at INFO for the three server-only tables — RLS on, no anon/authenticated policy, untrusted roles zero privileges, `service_role` the only application data-access role; **do not add untrusted policies to silence it**. **Next: QF-MVP-30.4C — admin campaign management runtime.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §25.
- **30.4A Vendor campaign management foundation (GENERATED, NOT APPLIED)** — **`QF_MVP_30_4A_VENDOR_CAMPAIGN_FOUNDATION_GENERATED_READY_FOR_STAGING_APPLICATION_REVIEW`**. Generated at HEAD `b742d3ff…` (origin identical, **0/0**, clean). Architecture source: the accepted preflight `qf-staging-workspace/QF-MVP-30.4-ARCHITECTURE-PREFLIGHT-20260725T124958Z/` (SHA-256 `4A886CD2…`). **Nothing applied to staging; no DB/provider/Jarvis access; no runtime route/action/service/UI; no campaign fixture; no communication intent; no push.** **Owner decisions LOCKED:** (1) **freeze at PREPARE, not approval** — prepare evaluates the current segment against current Core/CRM facts, resolves an immutable recipient set, writes immutable audience rows, stores segment id/version/fingerprint + evaluation timestamp + recipient count + snapshot fingerprint + sanitized exclusion summary, and atomically moves `draft → ready_for_review`; the approver authorises an **already-frozen** audience and approval never resolves or alters recipients; editing a ready campaign requires an explicit **return to draft** and a later prepare creates a **new snapshot revision** (earlier snapshots/events are never rewritten); (2) **approval fails closed** on segment missing/archived, segment version/fingerprint drift, missing/incomplete snapshot, snapshot count/fingerprint mismatch, or missing/changed template evidence — remedy is return-to-draft and re-prepare, and after approval the **frozen audience is the authority** so later segment edits never expand or rewrite it; (3) **atomic narrow SECURITY DEFINER RPCs** `qf_prepare_vendor_campaign_v1` + `qf_approve_vendor_campaign_v1` — `service_role`-only EXECUTE, fixed `search_path`, no dynamic SQL, no provider call, no intent, no Core write, deterministic idempotent replay, fail-closed optimistic revision/fingerprint checks, sanitized codes, **no generic transition RPC**; (4) **`communication_intents.aggregate_type` NOT widened** — 30.4 creates no intent and performs no dispatch; **QF-MVP-30.5** owns the widening, intent creation, dispatch, delivery outcomes and uncertain-outcome handling; (5) **`communication_templates.category` minimally widened to `('authentication','business','marketing')`** — existing values preserved, no row rewritten, no template inserted, no provider mapping activated; a `consent_scope='marketing'` campaign may only pin a **marketing** template (enforced by CHECK + both RPCs + the pure validator); `communication_automation_catalog` already recognises marketing and is unchanged; **this is the only authorised alteration to an existing communication authority**; (6) **frequency policy OUT of 30.4** — none exists and none is created; preview/prepare/approval perform **no** frequency enforcement and make no such claim; **QF-MVP-30.5 must define and enforce a minimal fail-closed frequency rule before any dispatch is enabled, and until that gate exists no campaign may send** (this supersedes the §9 Core-recheck wording); (7) **exactly three tables**; (8) **no plaintext destination anywhere**. **Migration `20260723001300_qf_mvp_vendor_campaign_foundation.sql`** (generated only): `public.vendor_campaigns` (mutable head + frozen evidence + full provenance, `segment_id` → `vendor_segments` **RESTRICT**, six actor FKs **SET NULL**, lifecycle/timestamp consistency, sha256 evidence shapes, `vcm_prepared_evidence_complete`, `vcm_marketing_requires_marketing_template`); `public.vendor_campaign_audience_members` (**immutable** frozen snapshot — one row per **included** vendor per revision with `ordinal` + enum-coded consent evidence, unique `(snapshot_id, vendor_id)` and `(snapshot_id, ordinal)`; **excluded vendors are never stored as identities**, only sanitized reason-code counts); `public.vendor_campaign_events` (**append-only** provenance with unique `event_idempotency_key`). **RLS enabled on all three, no untrusted policy, `revoke all`, then `select,insert,update` on the head and `select,insert` ONLY on snapshot + events — no role receives DELETE/TRUNCATE anywhere**; immutability triggers block UPDATE/DELETE on snapshot + events for **every** role including `service_role`; a lifecycle guard enforces the legal transition table, monotonic revisions and post-approval evidence immutability; a delete guard makes campaign hard-delete impossible. **No Core table altered, no backfill, no template row inserted, no row created, no `audit_logs`/`admin_notifications`.** `length(x::text)` used for size CHECKs (`pg_column_size()` is STABLE and rejected in a CHECK). **Pure contracts** `lib/crm/campaignContracts.ts` + `campaignValidation.ts` lock the transition table, closed vocabularies, sanitized failure codes, bounds, the **ordered-snapshot fingerprint** (reordering changes it — ordinal is part of frozen identity) and a prohibited-field registry refusing destination/provider/execution/frequency/AI fields **with reasons**. **Gates:** new 30.4 validator **113/113** (13 migration + 2 contract one-defect fixtures, campaign rules executed), 30.3C 64/64, 30.3A 119/119, 30.2 62/62, 30.1B 46/46, blueprint 36/36, `test:mvp` 66/66, R1 62, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, `verify:mvp` exit 0, typecheck/lint clean, clean `.next` rebuild with **zero-hit** prohibited-ref scan, `git diff --check` exit 0. **New verifier** `supabase/staging-verification/verify_qf_mvp_30_4.sql` (32 SELECT-only rows, one pre-fixture run after 30.4B); `verify_qf_mvp_30_3.sql` and `verify_qf_mvp_30_1b.sql` **unchanged**. **Next: QF-MVP-30.4B — controlled staging application of `20260723001300`; not started.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §24.
- **30.3D Deterministic vendor segment staging smoke (COMPLETE)** — **`QF_MVP_30_3_DETERMINISTIC_VENDOR_SEGMENTS_COMPLETE_READY_FOR_CAMPAIGNS`**. Pushed commits under test: **`3b15bb00…`** (30.3B staging-application record) + **`73b913a7…`** (30.3C runtime); local == origin, **0/0**, clean at smoke start. **Authorized staging `uckafzuochmbvtiodmcl` only; production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` never contacted; no provider call; no migration applied; `verify_qf_mvp_30_3.sql` NOT re-run (zero-row verifier, already run once pre-fixture).** **Smoke 122/122 PASS**, namespace `QF_STAGING_VENDOR_SEGMENT_SMOKE_V1` (no ids recorded), driving the **real Next.js server actions** over HTTP with a genuine `@supabase/ssr` session against the clean production build on a non-default port (PID recorded, stopped and proven down; ports closed). **Authorization:** unauthenticated → `/admin/login` on directory + editor; live `profiles.role` downgrade → `?error=unauthorized`; Superadmin allowed; a state-changing action **independently refused while demoted**. **Directory:** every status filter, invalid status, negative page and past-the-end page → HTTP 200 with **no raw DB detail**. **Create:** draft, v1, schema_version 1, actor server-derived, timestamps server-maintained, server-computed sha256 fingerprint; duplicate live name refused via the DB unique index; invalid definition rejected; **client-supplied fingerprint/version IGNORED — server recomputes** (verified with deliberately spoofed values). **Version:** metadata-only edit does not burn a version; reordered-equivalent keeps the same fingerprint and version; semantic change increments exactly once; revert yields **v3 with the original fingerprint** (never decreases or reuses). **Activate/archive:** valid draft activates; archive sets status/`archived_at`/`archived_by` with the row retained (**no hard delete**); archived segment can be neither edited nor activated (never becomes campaign authority); archived name reusable per the partial-unique contract; built action manifest contains **no delete/send/dispatch/campaign action**. **Rules:** 12 permitted predicates evaluate (8 Core read-only + 4 CRM); **25 prohibited inputs refused** (unknown field/operator, wrong type, group/rule bounds, token length, free-text operator, raw PostgREST grammar, comma, parens, quote, backslash, percent, underscore, star, package expiry, package-order activity, consent, suppression, communication authorization, assignment eligibility, campaign field, AI/ranking, contact PII, note content). **Open-vocabulary:** `Modular Kitchen & Wardrobe` usable while **6 injection payloads refused**. **Preview:** count and rows share one canonical definition; matching rule returns the synthetic vendor with consistent total, non-matching returns zero; page size clamps to **100**; invalid paging clamps to page 1; page 2 stable via id tie-breaker; exposes only `vendor_id, business_name, city, status, is_active`; carries a server `evaluatedAt`. **Dynamic, not persisted:** preview on the vendor's *current* CRM `onboarding_stage` includes it and a different stage excludes it, with **no membership row written**; `vendor_segments` has no vendor/member/recipient/audience column; all six membership/campaign/audience/engagement tables **still absent (PGRST205)**. **Containment:** all **15** Core vendor fields byte-identical before/after; zero rows in package orders, credit logs, lead assignments, consent events, suppressions; no segment data on public/vendor surfaces; clean build with **zero** prohibited refs and zero `service_role`/`SUPABASE_SERVICE`/`vendor_segments`/JWT strings across 52 client bundles. **Cumulative fixture posture (by design — archive-only):** 7 namespace segments (6 archived, 1 active); prior-run leftovers retired via the app's own archive action; rows created in Core/financial/consent tables **0**; Core authoritative field changes **0**. **QF-MVP-30.3 COMPLETE. Next: QF-MVP-30.4 — Campaign Management (frozen audiences + send approval); not started.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §23.
- **30.3C Deterministic vendor segment runtime (runtime/UI, no migration/DB change)** — **`QF_MVP_30_3C_DETERMINISTIC_SEGMENTS_IMPLEMENTED_READY_FOR_STAGING_SMOKE`**. Implemented on top of the applied `20260723001200` foundation. **No migration created or modified; no new table; no membership persistence; no campaign/audience/provider code; no Core mutation; no `audit_logs`/`admin_notifications`; no staging or production access during implementation; no segment fixture; not pushed.** **Routes (preflight-locked):** `/admin/vendor-crm/segments` (directory) + `/admin/vendor-crm/segments/[segmentId]` (editor + dynamic preview; `new` creates) — static, so they win over `[section]`; a "Vendor Segments" entry added to the Command Center nav. **No public/vendor route; no segment field on any public projection.** **Authorization:** routes `getAdminSession()` + `isSuperadmin` → redirect; all **7** server actions independently call `requireCrmAdmin()` (canonical `profiles.role='admin'` AND server-owned `app_metadata.admin_role='Superadmin'`) before any service-role client; actor always session-derived, never from input or client metadata. **Modules:** `services/vendorSegmentService.ts` (server-only), `app/actions/vendorSegmentActions.ts`, `lib/crm/segmentQueryPlan.ts` (pure planner), `components/admin/crm/segments/{VendorSegmentDirectory,VendorSegmentEditor}.tsx`, `scripts/mvp/crm/validate-qf-mvp-30-3c.mjs`. **Lifecycle:** list (bounded ≤100, `updated_at desc, id asc`, status filter) · create draft · update · activate · archive. **No hard delete** — no method, action or control, and `service_role` holds no DELETE/TRUNCATE; archived segments cannot be edited or activated; invalid/unknown id → `notFound()`; fixed `SEGMENT_*_ERROR` constants with class-only server logs. **Provenance/versioning:** canonical definition, fingerprint and version computed **server-side**; client-supplied fingerprint/version ignored; `definition_version` increments **only when the canonical fingerprint changes** (metadata-only edits never burn a version, never decrease or reuse); activation re-parses stored definition and refuses on fingerprint drift; duplicate live name fails safely; duplicate definition surfaced not blocked. **Dynamic evaluation:** compiled at request time and discarded — **no membership row, no vendor id written back, no recipient list, no campaign object, no communication authorization inferred, no Core fact copied**; count and rows share one query and one canonical definition; all relative windows resolve from a single `evaluatedAt`. **No N+1:** one batched pre-resolution per distinct CRM predicate (bounded 2000, fails closed — never silent truncation) + one paged `vendors` query — constant in vendor count. **Injection posture:** registry-only column names; enum/uuid/integer/ISO literals, all double-quoted; data-driven vocabularies (`service_categories`, `areas_covered`) additionally **charset-restricted at validation time**, closing the last path for raw PostgREST grammar. **Privacy:** preview returns vendor id, business name, city, verification status, enabled flag only — no contact PII, note content, consent/suppression internals, service key or raw DB error. **Gates:** 30.3C runtime validator **64/64**, 30.3A foundation **119/119**, 30.2 **62/62**, 30.1B 46/46, blueprint 36/36, `test:mvp` 66/66, R1 62, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, `verify:mvp` exit 0, typecheck/lint clean, clean `.next` rebuild with **zero-hit** prohibited-ref scan, `git diff --check` exit 0. **Next: QF-MVP-30.3D — staging runtime smoke; not started.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §22.
- **30.3B Vendor segment foundation staging APPLICATION + verification** — **`VENDOR_SEGMENT_FOUNDATION_APPLIED_AND_VERIFIED_ON_STAGING`**. Applied from foundation commit `63f1adea…` (`feat(mvp): add deterministic vendor segment foundation`, parent `e8b2e738…`, origin identical, **0/0**, clean). **Authorized staging `uckafzuochmbvtiodmcl` only**; production `yqpgcsduqbxulrlzwzap` and QF-Jarvis `coilipywdvxklewquqvv` never contacted; **no provider call; no fixture created; this record is documentation only — no runtime implementation.** **Application:** pre-state **12** migrations (latest `20260723001100 qf_mvp_vendor_crm_foundation`) → applied exactly one, **`20260723001200 qf_mvp_vendor_segment_foundation`**, recorded **exactly once** → post-state **13**. **Method recorded transparently:** applied **transactionally via the authorized Supabase Management API** with fail-closed preflight and exact migration-history version/name preservation, because the available generic migration action could not pin the repository's locked numeric version; resulting history is exact (`20260723001200 qf_mvp_vendor_segment_foundation`). **Not an application defect — do not reapply.** **Post-state:** `public.vendor_segments` exists, **RLS enabled**, **0 segment rows before fixtures**; six prior CRM tables intact; `vendor_credit_logs` **0**; `lead_assignments` **0**; **no membership / segment-member / version-history / campaign / campaign-audience / engagement / provider table created**; public/vendor projection exposes no segment or private CRM field. **Verifier** `supabase/staging-verification/verify_qf_mvp_30_3.sql` run **exactly once after application and before fixtures — 22/22 PASS (S01–S22)**: migration history, table existence, RLS/default-deny, zero untrusted privileges, `service_role` SELECT+INSERT+UPDATE only with **no DELETE/TRUNCATE**, expected columns/constraints/FKs/indexes/trigger, zero segment rows, zero Core financial/assignment mutation, no public projection exposure, Core assignment authority intact. **The zero-row verifier must not be re-run once fixtures exist**; `verify_qf_mvp_30_1b.sql` stays the locked historical pre-segments verifier. **Intentional advisor posture:** Supabase may report `rls_enabled_no_policy` (INFO) for `vendor_segments` — designed: RLS on, no untrusted-role policy, `anon`/`authenticated` zero privileges, `service_role` the only runtime data-access role. **Do not add an anon/authenticated policy to silence it.** **Migration-006 divergence** unchanged (`audit_logs`/`admin_notifications` absent, neither created nor referenced; provenance on `vendor_segments` itself). **Next: QF-MVP-30.3C — deterministic segment admin runtime.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §21.
- **30.3A Deterministic vendor segment foundation (GENERATED, NOT APPLIED)** — **`QF_MVP_30_3A_DETERMINISTIC_SEGMENT_FOUNDATION_GENERATED_READY_FOR_STAGING_APPLICATION_REVIEW`**. Generated at HEAD `e8b2e738…` (origin identical, **0/0**, clean). Architecture source: the accepted preflight `qf-staging-workspace/QF-MVP-30.3-ARCHITECTURE-PREFLIGHT-20260725T073538Z/` (SHA-256 `D0E53559…`). **Nothing applied to staging; no DB/provider/Jarvis access; no runtime/UI/route/action/service implemented; no push.** **Owner decisions LOCKED:** dynamic definition evaluation with **no membership persistence** (no `vendor_segment_memberships`, no audience-member/cached-membership/recipient/campaign-audience/provider table — 30.4 owns immutable frozen audience evidence); the 30.2 validator's global "no migration newer than `20260723001100`" ceiling **removed** (it would break every future phase) and replaced by phase-scoped invariants; `verify_qf_mvp_30_1b.sql` **NOT edited** (locked point-in-time pre-segments verifier, W18 intentionally historical) and superseded by a new 30.3 verifier; **no package-expiry/package-order predicate** and no invented "active package" (`remaining_credits`/`total_credits` permitted per preflight §15 — denormalized onto `vendors`, read-only, no join); **no free-text predicate** (removes PostgREST pattern injection structurally); no AI/provider/consent/assignment authority. **A segment is a saved question, never an authorization.** **Migration `20260723001200_qf_mvp_vendor_segment_foundation.sql`** (generated only): ONE table `public.vendor_segments` — `id, name, description, status, schema_version, definition jsonb, definition_version, definition_fingerprint, created_at, updated_at, archived_at, created_by, updated_by, archived_by`; `vsg_pkey`; three actor FKs → `profiles` **ON DELETE SET NULL** (never CASCADE); checks `vsg_name_nonempty/_len`, `vsg_status_check(draft|active|archived)`, `vsg_archived_consistency`, `vsg_schema_version_check(=1)`, `vsg_definition_version_check(>=1)`, `vsg_definition_is_object`, `vsg_definition_size(<=8KB)`, `vsg_fingerprint_shape(^[0-9a-f]{64}$)`; **partial unique** `uq_vendor_segments_live_name` on `lower(btrim(name)) where status <> 'archived'`; indexes `ix_vendor_segments_status_updated`, `ix_vendor_segments_fingerprint`; trigger `trg_vsg_touch` reusing `qf_crm_touch_updated_at()`; **RLS enabled, no untrusted policy, `revoke all` then `grant select, insert, update` to `service_role` only — NO DELETE/TRUNCATE** (hard delete impossible at the privilege layer); prohibited-column fence (no Core copy, no `member_count`/`recipients`/`approved_audience`); **no Core table altered, no backfill, no destructive DDL, no row created**; `audit_logs`/`admin_notifications` neither created nor referenced (006 divergence explicit — provenance lives on the table itself). **Typed AST** `lib/crm/segmentRuleContracts.ts` + `lib/crm/segmentRuleValidation.ts` (both **pure** — no DB, no `server-only`, directly executable by tests): 10 permitted Core read-only fields, 12 permitted CRM fields, explicit prohibited-field registry (package expiry/order, consent, suppression, communication authorization, campaign eligibility, lead/assignment content, contact PII, note bodies, unbounded free text, AI score) refused with a **reason**; 17 operators, 9 value kinds, field is always a registry key never a raw column; canonical JSON with fixed key order + sorted/de-duplicated values + collapsed duplicate predicates; **sha256 fingerprint** matching the DB check; bounds ≤3 groups/≤8 per group/≤24 total/≤25 array/≤8KB; NULL evaluates **false** (only `is_null` matches); single `evaluatedAt`, **Asia/Kolkata**; stable `created_at DESC, id ASC`. **30.4 handoff:** campaign records `segment_id + definition_version + definition_fingerprint` and freezes recipients separately, so an edit changes the fingerprint and an approved campaign **fails closed** — an approved audience can never silently change (no second version table needed). **Gates:** new 30.3 validator **118/118** (10 migration + 2 contract one-defect fixtures, real rule engine executed), corrected 30.2 **62/62**, 30.1B 46/46, blueprint 36/36, `test:mvp` 66/66, R1 62, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, `verify:mvp` exit 0, typecheck/lint clean, clean `.next` rebuild with **zero-hit** prohibited-ref scan, `git diff --check` exit 0. **New verifier** `supabase/staging-verification/verify_qf_mvp_30_3.sql` (22 SELECT-only rows, for exactly one pre-fixture run after 30.3B). **Next: QF-MVP-30.3B — staging preflight + application of `20260723001200`; not started.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §20.
- **30.2C1 + 30.2S4 Vendor CRM bounded security correction + direct staging smoke (no migration/env)** — **`VENDOR_CRM_DIRECTORY_AND_PROFILE_STAGING_SMOKE_COMPLETE_READY_FOR_SEGMENTS`**. Implementation SHA `d11a348…` (13 paths, unchanged); **correction commit `fc81103…`** (6 paths, parent = `d11a348…`; `services/vendorCrmService.ts`, both `/admin/vendor-crm` route files, `lib/crm/vendorCrmValidation.ts`, `scripts/mvp/crm/validate-qf-mvp-30-2.mjs`, `package.json`). **No migration, no Supabase SQL, no environment file, no unrelated runtime code; no push; no PR; no deployment.** **Two real defects fixed. (1) Unsafe PostgREST `.or()` search interpolation** — a crafted term could append an OR condition and widen an admin's result set; `sanitizeDirectorySearch()` now NFKC-normalizes, strips control/format characters, allow-lists Unicode letters/marks/digits + space `' - . + @ &`, collapses whitespace and caps at 80 chars, so `, ( ) " \` and `% _ *` cannot survive (stripping chosen over escaping to remove the PostgREST-quoting × SQL-LIKE double-escaping ambiguity); the service **also** double-quotes each `ilike` value. *Recorded consequence:* `_`/`%` are not searchable literals. **(2) Raw error message exposure** — both routes assigned `e.message` to the rendered error state; they now render a fixed `CRM_*_LOAD_ERROR` constant and log only error `name`+`code`, and neither module references `.message`. Server-action `fail()` path unchanged. **Validator 32 → 59** (`S09_safe_search` + fixture H; `R01`/`R02` route rules + fixtures RF1/RF2; **20 executed** sanitizer cases + 2 wiring assertions via the type-stripping loader). **Foundation baseline INHERITED, not re-run** (prior block classified a *procedural access block*): `qf-staging-workspace/QF-MVP-30.1BA-APPLICATION-20260724T185637Z/` — staging `uckafzuochmbvtiodmcl`, **12** migrations (all `local == remote`) ending `20260723001100`, locked verifier **25 PASS / 0 FAIL** (machine-counted), six CRM tables, no Core mutation; link asserted mechanically (verifier `e10caa56…` + migration `9212f746…` still match; no newer migration). **Live runtime contract re-proved** via wrapper-mediated service-role HTTP: six tables reachable, exact application column contract accepted (7/11/6/7/7/14), and **`service_role` cannot DELETE any of the six (all refused `42501`)** nor UPDATE `vendor_internal_notes` — hard delete impossible at the privilege layer. **Staging smoke 127/127 PASS**, namespace `QF_STAGING_VENDOR_CRM_SMOKE_V1`, one synthetic Superadmin + one synthetic disabled/unverified vendor (`@example.invalid`, no real PII), driving the **real Next.js server actions** over HTTP with a genuine `@supabase/ssr` session against the built app on a non-default port (PID recorded, stopped and proven down). Covered: authorization (unauthenticated → `/admin/login`, non-admin → `error=unauthorized`, Superadmin allowed); directory/search-by-name-owner-phone/bounded pagination/filter state; **10 injection-shaped payloads proved unable to widen results**, 4 wildcard-only terms degrade to *no filter* not a wildcard match; profile load, invalid + unknown id → 404, **no database/secret detail rendered**; CRM profile create/update (server-derived actor); contact create → primary → update → archive with **no consent row**; tag create/normalized_name/duplicate-variant rejection/assign/duplicate-active protection/lifecycle removal; append-only notes (+2 exactly, nothing replaced, no note mutation action exists); task idempotency dedupe, forced-`done` rejected, complete with result/actor/time, cancel without hard delete. **All 15 Core vendor facts byte-identical before/after**; zero rows in package orders, credit logs, lead assignments, consent, suppression; no segment/campaign object; **no Meta/n8n/Jarvis/WhatsApp/SMS/email/webhook request; no production or QF-Jarvis contact.** **Gates:** 30.2 **59/59**, 30.1B 46/46, blueprint 36/36, `test:mvp` 66/66, R1 62, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, `verify:mvp` exit 0, typecheck/lint clean, clean `.next` rebuild with a **zero-hit** prohibited-ref scan (cache/static/server/manifests/maps), `git diff --check` exit 0. **Next: QF-MVP-30.3 — Deterministic segments; not started.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §19.
- **30.2 Vendor CRM directory + combined vendor profile (runtime/UI, no migration/DB)** — **`VENDOR_CRM_DIRECTORY_AND_PROFILE_IMPLEMENTED_REVIEWED_READY_FOR_STAGING_SMOKE`**. Runtime/UI implementation at HEAD `6bfbb7e…` (origin identical, **0/0**, clean; previous commit=2 docs; migration `20260723001100` unchanged). **No migration created/edited; no managed DB access; no RLS/grant/policy/function change; no `vendor_public_v` change; no Core authority change; no segments/campaigns/Meta/n8n/Jarvis/owner-binding.** **Canonical route (static, wins over `[section]`):** `/admin/vendor-crm` (directory) + `/admin/vendor-crm/[vendorId]` (combined profile); a "Vendor CRM" nav entry added to `adminConfig` Command Center; the lead-ops `/admin/crm` dashboard is untouched (no collision). **Access model A (server-only):** routes self-guard via `getAdminSession()` + `isSuperadmin` (redirect otherwise); every server action runs through `lib/crm/crmAuth.requireCrmAdmin()` (profiles.role='admin' AND app_metadata.admin_role='Superadmin') before any service_role client; **client components never import service-role/`server-only`/the CRM service** (type-only imports allowed); actor id always from the session, never request input. **Canonical CRM service `services/vendorCrmService.ts` (server-only, `import "server-only"` + adminClient):** listVendorCrmDirectory + 6 reads + 14 mutations (upsert profile, contact create/update/archive, tag create/update/archive/assign/remove, note create, task create/update/complete/cancel) — **writes only the six CRM tables**, no Core mutation, **no note update/delete** (append-only), **no hard-delete** (archive via `is_active`/`removed_at`/`status`), deterministic server-side tag normalization, normalized phone, task idempotency honored. **Directory read model:** server-paged (`.range`, bounded ≤100, deterministic `created_at desc,id` sort), batch-loaded CRM extensions per page (profiles/tags/tasks/primary-contact in a fixed handful of queries — **no N+1**), Core+CRM filters (search/category/city/verification/enabled/stage/relationship/tag/task-state) via URL params (all fetching server-side). **Combined profile:** Overview (Core read-only + CRM edit), Contacts (create/archive, primary, "contact ≠ consent"), Tags (assign/remove/create, no duplicate active), Notes (append-only, no edit/delete UI), Tasks (create/complete/cancel, overdue, idempotency), Core context (package/credit/lead/consent read-only, Core-owned). **Validation** `lib/crm/vendorCrmValidation.ts` (hand-rolled, closed enums, rejectUnknownKeys/mass-assignment defence, phone/email, page-size Math.min bound). **Migration-006 divergence honored:** no `audit_logs`/`admin_notifications` dependency — CRM actor/timestamp fields are the V1 mutation audit evidence. **Empty/error/zero-state** handled throughout (staging has 0 vendors). **Phase validator** `scripts/mvp/crm/validate-qf-mvp-30-2.mjs` **32/32** (7 service + 2 client one-defect fixtures rejecting Core mutation, note update, hard delete, server-only removal, 006 dependency, campaign scope, unbounded pagination, client service-role import). **Gates:** 30.2 32/32, 30.1B 46/46, blueprint 36/36, B1/G 165, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, R1 62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Next: a bounded QF-MVP-30.2 staging smoke/integration review before QF-MVP-30.3 (Deterministic segments); not started.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §18.

- **30.1BA Vendor CRM foundation staging APPLICATION + verification** — **`VENDOR_CRM_FOUNDATION_APPLIED_AND_VERIFIED_ON_STAGING`**. Applied at HEAD `51126e0…` (parent `d5ee7cf…`, origin identical, **0/0**, clean; preflight commit=2 docs; no later application/correction). **Migration `20260723001100` APPLIED to authorized staging `uckafzuochmbvtiodmcl`** via exactly one `npx supabase db push --linked` (18:57:24→18:57:34 UTC, **exit 0**); exactly that migration, no earlier migration, no repair/reset, no second push. **ABSENT path executed** — pre-state proved `vendor_internal_notes` absent; apply log shows 16 benign `drop … if exists … skipping` NOTICEs (converge block on the freshly-created table) + the `$verify$` NOTICE (six tables, sole append-only notes authority, RESTRICT vendor FKs + SET NULL actor FKs, no Core-truth cols, no segment/campaign, A–E/20.4C/20.5A + vendor_public_v + owner-binding intact); trailing `pgdelta-target-ca.crt ENOENT` known non-blocking artifact, exit 0. **Hashes exact** (migration `9212f746…`, verifier `e10caa56…`); applied A…20.5A byte-unchanged. **Workspace (outside Git):** 12 SQL (all repo-backed byte-identical). **Pre-state (safe, to_regclass):** all six CRM tables ABSENT, `vendor_internal_notes` ABSENT, remote 11, 68 tables → 0 rows, register empty, A–E intact, no segment/campaign. **History: before 12 local / 11 remote (sole pending) → after 12 local / 12 remote**, paired once, applied once. **Locked verifier `verify_qf_mvp_30_1b.sql` (`e10caa56…`) ran once: 25 rows, 25 PASS / 0 FAIL** — six tables; no rival vendor_notes; RLS all six; untrusted zero; service_role SI-notes / SIU-lifecycle, no DELETE/TRUNCATE; notes append-only triggers; vendor FKs RESTRICT; **W25 notes final contract (7 cols, created_by SET NULL, PK, legacy policy absent) — ABSENT bootstrap converged**; tag/profile/contact/task uniqueness+idempotency; no Core-truth cols; no segment/campaign; vendor_public_v intact; A–E/20.4C/20.5A + owner-binding preserved. **Zero-data-write (post SELECT-only):** all six foundation tables 0 rows; auth.users/profiles/vendors/lead_assignments/vendor_credit_logs/register all 0; all public rows 0; public table count **68→74** (exactly +6 CRM tables — no unrelated 006 table created); no note content, no Core backfill, no RPC. **Gates:** 30.1B 46/46, blueprint 36/36, B1/G 165, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, R1 62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Migration-006 divergence:** only vendor_internal_notes created; audit_logs/admin_notifications/… NOT created (must revalidate before reuse; QF-MVP-30.4 must not assume they exist). Transcript + captures outside Git in `qf-staging-workspace/QF-MVP-30.1BA-APPLICATION-20260724T185637Z/`. **Next: QF-MVP-30.2 — Vendor CRM Directory + Combined Vendor Profile (not started).** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §17.

- **30.1BP2 Vendor CRM foundation corrected staging preflight (dry-run only, NOT applied)** — **`VENDOR_CRM_FOUNDATION_PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. At HEAD `d5ee7cf…` (parent `6e3670b…`, grandparent `bf432800…`, origin identical, **0/0**, clean; correction commit=6 paths, foundation commit=9 paths). **Nothing applied** — one `npx supabase db push --linked --dry-run` + catalog-SAFE SELECT-only checks + `migration list`; no push-without-dry-run, no second dry run, no up/repair/reset, no hand-executed SQL, no vendor_internal_notes/CRM/Core change, no RPC. **Full hashes exact** (migration `9212f746…`, validator `5935a881…`, verifier `e10caa56…`, manifest `26bd3ab3…`); applied 11 byte-unchanged. **Corrected source re-proved:** `create table if not exists public.vendor_internal_notes` precedes the first dependent ALTER; both ABSENT + LEGACY_MINIMAL converge; validator **46/46**, verifier 25 rows. **Linked `uckafzuochmbvtiodmcl`** (production/QF-Jarvis not contacted). **Workspace (outside Git):** no seed/functions, 11 SQL (repo-backed identical) → 30.1B absent → copied corrected (`9212f746…`) → **12**. **Safe pre-state (to_regclass, no error on absent):** `20260723001100` absent; remote **11**; **all six CRM tables ABSENT** (vendor_internal_notes=ABSENT — the create-if-absent path); 006 omissions recorded (audit_logs/admin_notifications/lead_internal_notes absent, informational); all public rows 0; auth.users/profiles/vendors/lead_assignments/vendor_credit_logs=0; register present+empty; vendor_public_v present; A–E intact; admin_role absent; no segment/campaign; owner binding deferred; 68 tables. **History: 12 local / 11 remote**, `20260723001100` sole pending. **Dry run (once, exit 0):** `DRY RUN: migrations will *not* be pushed`; **exactly one** proposed — `20260723001100_qf_mvp_vendor_crm_foundation.sql`. **No-write proof:** post-dry-run history + safe pre-state re-run **identical** (remote 11, six CRM objects absent, data 0). **Gates:** 30.1B 46/46, blueprint 36/36, B1/G 165, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, R1 62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Migration-006 divergence:** only vendor_internal_notes handled; audit_logs/admin_notifications/… NOT created/assumed — QF-MVP-30.4 must revalidate before reuse. **Next: QF-MVP-30.1B staging application review.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §16.

- **30.1B1 Vendor CRM foundation notes-bootstrap correction (offline, still NOT applied)** — **`VENDOR_CRM_FOUNDATION_NOTES_BOOTSTRAP_CORRECTED_READY_FOR_PREFLIGHT`**. Correction commit at parent `6e3670b…` (the 30.1B foundation commit is NOT amended; branch ahead of origin by **1**). **No DB/Supabase/dry-run/application; no new migration version; no runtime/UI/API/type/Core change.** **Proved blocker (from 30.1BP preflight):** the staging baseline squash `269c9265` **omits the entire migration-006 table set** — `vendor_internal_notes` (+ `lead_internal_notes`/`lead_timeline_events`/`audit_logs`/`admin_notifications`/`reviews`/`ai_agents`/`localities`) is **absent** on staging but **present** (minimal shape) on production; the original unconditional `alter table vendor_internal_notes …` would have failed `42P01` on staging. **Correction (migration `20260723001100` edited in place, still unapplied):** §5 is now a **presence-idempotent two-path bootstrap** — `create table if not exists public.vendor_internal_notes (…legacy base shape…)` **before** any dependent ALTER, converging **ABSENT** (staging→create) and **LEGACY_MINIMAL** (production→no-op) to one exact final contract: cols `id, created_at, vendor_id, note, created_by, category, supersedes_note_id`; PK id; vendor FK **RESTRICT**; created_by **SET NULL**; self-FK **RESTRICT**; **NOT-VALID** required/format checks (lossless — no legacy row validated/rejected/rewritten); legacy "vendor notes admin all" policy dropped; append-only triggers; server-only grants. **No second notes table; no note DML/rewrite.** Self-verify strengthened (9.6b: exact column set, PK, created_by SET NULL, self-FK non-cascading, required checks, legacy policy absent — **not assuming pre-existence**). **Migration-006 divergence follow-up recorded:** the other omitted 006 tables (audit_logs/admin_notifications/…) are NOT created here and must be revalidated on staging before reuse — **QF-MVP-30.4 must not assume audit_logs/admin_notifications exist.** **Corrected hashes:** migration `9212f746…` (was `599d4dd0…`), validator `5935a881…`, verifier `e10caa56…`, manifest `26bd3ab3…`. **Validator** `scripts/mvp/crm/validate-qf-mvp-30-1b.mjs` **46/46** (28 migration + 3 verifier fixtures; new bootstrap fixtures W/X/Y/Z/AA/BB reject: missing create-if-absent, create-after-ALTER order defect, incomplete base shape, created_by CASCADE, legacy policy retained, absent-path self-check omitted; + manifest 11b two-path). **Verifier** `verify_qf_mvp_30_1b.sql` (**25 rows**, adds path-agnostic W25 notes-final-contract). **Manifest** adds `notes_bootstrap_mode: CREATE_IF_ABSENT_THEN_CONVERGE` + `supported_start_states: [ABSENT, LEGACY_MINIMAL]` + lossless flags. **Gates:** blueprint 36/36, 30.1B1 validator 46/46, B1/G 165, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, R1 62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Next: a fresh QF-MVP-30.1B staging preflight.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §15.

- **30.1B Vendor CRM foundation schema + security (generated, NOT applied)** — **`VENDOR_CRM_FOUNDATION_GENERATED_REVIEWED_READY_FOR_PREFLIGHT`** (superseded by 30.1B1 — notes bootstrap made presence-idempotent). Offline generation at HEAD `bf43280…` (origin identical, **0/0**, clean; blueprint commit=4 paths; no collision at `20260723001100`; `20260723001000` was prior highest). **No DB/Supabase/provider/Jarvis access; migration generated but unapplied; no UI/API/route; no segments/campaigns; no Core authority change.** **Canonical notes decision:** `vendor_internal_notes` (migration 006, **zero runtime call sites**) is **evolved in place** into the SINGLE append-only notes authority (+category, +supersedes_note_id, NOT-VALID non-empty body, vendor FK CASCADE→RESTRICT, legacy authenticated policy dropped, immutability triggers); a rival `vendor_notes` was **rejected** (would be a competing writer). **Access model A (server-only):** PUBLIC/anon/authenticated hold **zero** direct privilege on all six CRM tables (RLS default-deny); `service_role` (the existing `adminClient` path) is the only writer — SELECT+INSERT on notes (append-only), SELECT+INSERT+UPDATE on the 5 lifecycle tables, and **never** DELETE/TRUNCATE/REFERENCES/TRIGGER/MAINTAIN. **History-preserving FKs:** every CRM→vendors FK is **ON DELETE RESTRICT** (no runtime vendor hard-delete; soft-delete by status), actor→profiles **SET NULL**; tags/tasks/contacts/assignments archive via state, notes append-only — no hard delete for app roles. **Six tables:** `vendor_crm_profiles`(PK vendor_id, 1/vendor), `vendor_contacts`(PII, partial-unique active primary + active phone), `vendor_tags`(normalized_name unique), `vendor_tag_assignments`(partial-unique active per vendor,tag), `vendor_internal_notes`(canonical, append-only), `vendor_tasks`(type/priority/status/source CHECKs, idempotency_key partial-unique). **Core non-duplication proof:** machine-checked prohibited-column list (verification/enabled/city/service-area/categories/package/credits/eligibility/consent/suppression) enforced by migration self-verify + validator + verifier — **zero** authoritative Core copies. **Migration** `supabase/migrations/20260723001100_qf_mvp_vendor_crm_foundation.sql` (`599d4dd0…`): schema/ACL only, no DML/backfill/txn/history-write/default-priv/broad grant/secret; catalog-fact `$verify$`; `vendor_public_v`/A–E/20.4C/20.5A/owner-binding-deferral all fenced. **Validator** `scripts/mvp/crm/validate-qf-mvp-30-1b.mjs` **(22 migration + 3 verifier one-defect fixtures)** via a comment/string/dollar-body-aware tokenizer; real artifacts graded by the same evaluators. **Verifier** `supabase/staging-verification/verify_qf_mvp_30_1b.sql` (24 rows, SELECT-only). **Contract manifest** `scripts/mvp/crm/qf-mvp-30-1b-foundation-contract.json` (no production rows). **Domain types** `lib/crm/vendorCrmContracts.ts` (closed enums, no secret/client). **Gates:** blueprint 36/36, 30.1B validator PASS, B1/G 165, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, R1 62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Next: QF-MVP-30.1B staging preflight.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md) §15.

- **30.1A Vendor CRM V1 blueprint (audit + design, no runtime/migration/DB)** — **`VENDOR_CRM_V1_BLUEPRINT_FROZEN_READY_FOR_FOUNDATION_IMPLEMENTATION`**. First QF-MVP-30 phase on new branch `mvp/qf-mvp-30-vendor-crm-v1` at HEAD `dcead0b…` (= QF-MVP-20 closeout commit; origin identical, **0/0**, clean; no QF-MVP-30 implementation commit). **No DB/provider/Jarvis access; no migration; no runtime/UI/API/type change; no Core authority change.** **Locked boundary:** Core decides · Vendor CRM organizes · Jarvis recommends · n8n executes · Meta delivers · Admin approves — CRM owns relationship/enrichment extensions, never authoritative Core truth (identity/verification/enabled/capability/geo/package/credit/eligibility/consent/suppression/campaign-recipient-authorization). **Current-state audit:** the existing `/admin/crm` (`CRMDashboard.tsx`) is a **lead-ops** CRM (out of scope); reuse the AdminShell UI, `audit_logs`, vendor comms/support tables, and the communication/consent authority (`communicationConsentDecisionService`) as the **Core recheck** source; `vendor_internal_notes` exists (minimal) → superseded by richer `vendor_notes`; **no** vendor tags/segments/campaigns/contacts/CRM-profile/vendor-tasks exist yet. **Core-truth reference map frozen** (vendors/vendor_packages/vendor_credit_logs/lead_assignments/communication_consent_events/communication_suppressions/communication_templates) — read-only, zero authoritative copies. **Selected V1 model (additive):** `vendor_crm_profiles`, `vendor_contacts`, `vendor_tags`(+assignments), `vendor_notes`(append-only), `vendor_tasks`, `vendor_segments`(typed JSON rule AST, no arbitrary SQL/AI), `vendor_campaigns`(+`_audiences` immutable RESTRICT, `_events`, `vendor_engagement_events`) — RLS + service_role writer + admin read, no anon/authenticated-generic, private contacts/notes never public and never on `vendor_public_v`. **Campaign lifecycle** DRAFT→…→SNAPSHOT_FROZEN→APPROVED→EXECUTION_REQUESTED→… with a **fail-closed Core recheck** (state/consent/suppression/frequency/authorization/template/approval) before an immutable snapshot; n8n receives only the frozen approved audience; **no service-role key reaches Jarvis/n8n**; CRM cannot send directly to Meta. **Implementation sequence:** 30.1B foundation schema → 30.2 directory+profile → 30.3 segments → 30.4 campaigns → 30.5 staging closeout (**4–6 focused days**). **No genuine blockers.** **Blueprint validator** `scripts/mvp/crm/validate-qf-mvp-30-blueprint.mjs` (`test:crm:blueprint`, standalone — not in verify:mvp) **36/36** (required sections + locked boundaries + selected tables). **Gates:** B1/G 165, B2 61, C 83, D 110, E 51, 20.4A 39, 20.4C 42, 20.5A 40, R1 62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Next: QF-MVP-30.1B — CRM foundation schema + security (staging-first); not started.** Record: [`QF-MVP-30-VENDOR-CRM-BLUEPRINT.md`](QF-MVP-30-VENDOR-CRM-BLUEPRINT.md).

- **20.6 Marketplace Engine V1 FINAL staging closeout audit** — **`MARKETPLACE_ENGINE_V1_STAGING_COMPLETE_READY_FOR_VENDOR_CRM`**. Read-only audit at HEAD `19572eb…` (parent `e37d6d2…`, origin identical, **0/0**, clean; previous commit=2 docs; HEAD is tip). **No migration created/applied, no runtime change, no DML/DDL/DCL/DO/RPC, no data written, no production/QF-Jarvis access, no owner binding, no exception insert.** **Authority map (current code):** sole executable assignment writer `services/canonicalAssignmentAuthority.ts` → `qf_assign_lead_vendors_v2` (service_role); sole credit writer `services/vendorCreditWalletService.ts` → `qf_apply_vendor_credit_delta` (service_role, writes vendor_credit_logs); **zero** executable runtime call sites for any of the 6 legacy assignment / 3 legacy credit RPCs (comments confirm not called/no-fallback); Jarvis/n8n only in the communication layer — **QuickFurno Core is the sole marketplace authority**. **Migration integrity:** 10 repo migrations hash-match (A `b6307094…`→20.5A `5cf12b72…`); `001000` is highest, no pending; 20.4C/20.5A locked hashes match; no applied migration changed. **Offline gates:** B1/G 165/165, R1 62/62, B2 61/61, C 83/83, D 110/110, E 51/51, 20.4A 39/39, 20.4C 42/42, 20.5A 40/40, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Linked `uckafzuochmbvtiodmcl`; migration list 11 local / 11 remote, all paired once, no pending/unexpected.** **SELECT-only verifiers (each once):** D **37/37**, E **21/21**, 20.4C **22/22**, 20.5A **23/23** — ALL PASS. B1/G 57/62, B2 32/34, C 22/23 — the 8 non-pass rows are **forward-looking "later migration not yet applied" temporal scope-fences** (self-documenting `details` e.g. "Migration D not started"), correctly inverted now that the full sequence is applied — **not defects**. **Final staging state matrix 26/26 (affirmative):** 11 migrations; all public tables + auth.users + profiles + lead_assignments + vendor_credit_logs + vendors + vendor_packages + register **0 rows**; assignment authority ×1 (no dup); credit-ledger writer present; 4 B2 triggers; vendor_public_v present + no private cols; PUBLIC/anon no vendors priv; PUBLIC/anon/authenticated no leads priv; D trigger+handle_new_user+is_admin present; 6 legacy RPCs untrusted-EXECUTE=0; profiles RLS on + authenticated SELECT-only + service_role SELECT/INSERT/UPDATE-only; admin_role absent; owner-binding cols absent; register 2 immutability triggers. **All 19 closeout-standard contracts + 26 state-matrix invariants + authority map hold; no launch-critical security/money/data-integrity/runtime defect.** **Deferred fail-closed (non-blocking):** client-selected owner binding (`R1_BLOCKED_PENDING_OWNER_BINDING`, cols absent, path not called), production rollout, historical exception population (register empty), destructive legacy cleanup (6 RPCs retained but revoked). **Production not migrated by this branch** (every `db push` targeted staging; production read once in 20.4B under separate authorization, SELECT-only). Evidence pack outside Git: `qf-staging-workspace/QF-MVP-20.6-MARKETPLACE-CLOSEOUT-20260724T162126Z/`. **Next authorized phase: QF-MVP-30 Vendor CRM (not started).** Record: [`QF-MVP-20-AUTHORITY-REPAIR-DESIGN.md`](QF-MVP-20-AUTHORITY-REPAIR-DESIGN.md) §Z.

- **20.5AA Profiles privilege + admin_role cleanup staging APPLICATION + verification** — **`PROFILES_PRIVILEGE_AND_ADMIN_ROLE_CLEANUP_APPLIED_AND_VERIFIED_ON_STAGING`**. Applied at HEAD `e37d6d2…` (parent `a863c8b…`, origin identical, **0/0**, clean; preflight commit=2 docs; no later application/correction). **Migration `20260723001000` APPLIED to authorized staging `uckafzuochmbvtiodmcl`** via exactly one `npx supabase db push --linked` (16:04:06→16:04:16 UTC, **exit 0**); exactly that migration, no earlier migration, no repair/reset, no second push. In-transaction `$verify$` NOTICE fired (authenticated SELECT-only no-escalation; anon/PUBLIC zero; **service_role SELECT+INSERT+UPDATE only, no DELETE/TRUNCATE/REFERENCES/TRIGGER/MAINTAIN**; admin_role removed; `profiles.role`+`is_admin()`+D trigger intact; 20.4C register + owner-binding untouched). `column "admin_role" … does not exist, skipping` = `drop column if exists` no-op on the staging squash; trailing `pgdelta-target-ca.crt ENOENT` = known non-blocking cache artifact, exit 0. **Locked hashes exact** (migration `5cf12b72…`, validator `2458f7f8…`, verifier `fb582dbd…`); applied A…E + 20.4C byte-unchanged. **Workspace (outside Git):** no seed/functions, 11 SQL (all 10 repo-backed byte-identical). **Pre-state (SELECT-only):** `20260723001000` absent (10 remote); profiles present RLS-on **0 rows**; auth.users 0; admin_role absent; role present; **authenticated 0 privileges** (drift); anon/PUBLIC 0; **service_role FACT = all 7** (`DELETE,INSERT,REFERENCES,SELECT,TRIGGER,TRUNCATE,UPDATE`); 3 policies; D+is_admin+handle_new_user; no owner-binding; 20.4C register present + **0 rows**; A…E intact; 68 tables → **0 rows**. **History: before 11 local/10 remote (sole pending) → after 11 local/11 remote**, paired once, applied once, no duplicate. **Locked verifier `verify_qf_mvp_20_5a.sql` (`fb582dbd…`) ran once: 23 rows, 23 PASS / 0 FAIL** — profiles present RLS-on; **authenticated SELECT + no INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER/MAINTAIN**; role-escalation impossible (no authenticated UPDATE); anon/PUBLIC zero; **service_role exactly SELECT/INSERT/UPDATE**; admin_role absent; role present; 3 policies preserved no anon target; is_admin+D trigger+handle_new_user preserved; 20.4C register intact+empty; A…E + owner-binding untouched. **Final matrix (live): PUBLIC none · anon none · authenticated SELECT only · service_role SELECT/INSERT/UPDATE only.** **No-data-write (post SELECT-only):** the ONLY change was the ACL — authenticated 0→SELECT, service_role all-7→SELECT/INSERT/UPDATE (FACT `INSERT,SELECT,UPDATE`); every data table still 0 rows (profiles/auth.users/all 68 public/register/vendor_credit_logs/lead_assignments), no role changed, no assignment/package/credit/lead mutation, no exception inserted, no RPC. **Gates:** 20.5A 40/40, 20.4C 42/42, 20.4A 39/39, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. Transcript + captures outside Git in `qf-staging-workspace/QF-MVP-20.5AA-APPLICATION-20260724T160323Z/`. **Next: Marketplace Engine final closeout review** — not another cleanup subphase; owner binding deferred; 20.4 register stays empty. Record: [`QF-MVP-20-5A-PROFILES-PRIVILEGE-ADMIN-ROLE-CLEANUP.md`](QF-MVP-20-5A-PROFILES-PRIVILEGE-ADMIN-ROLE-CLEANUP.md) §12.

- **20.5AP Profiles privilege + admin_role cleanup staging preflight (dry-run only, NOT applied)** — **`PROFILES_PRIVILEGE_AND_ADMIN_ROLE_CLEANUP_PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. At HEAD `a863c8b…` (parent `3b2c51d…`, grandparent `5fc5ba4…`, origin identical, **0/0**, clean; correction commit=5 paths, generation commit=10 paths). **Nothing applied** — one `npx supabase db push --linked --dry-run` + SELECT-only checks + `migration list`; no push-without-dry-run, no second dry run, no `up/repair/reset`, no hand-executed SQL, no profile/auth/role change, no manual grant/drop, no RPC. **Linked `uckafzuochmbvtiodmcl`** (production `yqpgcsduqbxulrlzwzap` + QF-Jarvis `coilipywdvxklewquqvv` not the target, not contacted). **Locked hashes exact** (migration `5cf12b72…`, validator `2458f7f8…`, verifier `fb582dbd…`); applied A/A2/B1/G/B2/C/D/E/20.4C + profiles-source (`20260620000001`/`2`/`20260621000006`) + 20.4A/20.4C artifacts byte-unchanged. **External apply workspace (outside Git):** no seed/functions; 10 SQL (baseline+A…E+20.4C, byte-identical) → 20.5A absent → copied ONLY the locked migration (`5cf12b72…`) → **11 SQL**. **Live pre-state (SELECT-only):** `20260723001000` absent (10 remote); profiles present, RLS on, **0 rows**; auth.users 0; **`profiles.admin_role` absent** on staging; `profiles.role` present; **`authenticated` holds 0 profiles privileges** (the drift the migration fixes by adding SELECT); anon/PUBLIC 0; **`service_role` currently holds all 7** (`DELETE,INSERT,REFERENCES,SELECT,TRIGGER,TRUNCATE,UPDATE`, Supabase defaults) — the migration tightens to SELECT/INSERT/UPDATE only; 3 profiles policies present; D trigger+`handle_new_user`+`is_admin` present; no owner-binding columns; 20.4C register present + **0 rows**; A…E intact; 68 tables → **0 rows**. **History: 11 local / 10 remote**, `20260723001000` local-only + **sole pending**. **Dry run (once, exit 0):** `DRY RUN: migrations will *not* be pushed`; **exactly one** proposed — `20260723001000_qf_mvp_profiles_privilege_admin_role_cleanup.sql`; no earlier migration, no application claim. **No-write proof:** post-dry-run history + pre-state re-run **identical** (remote 10, 20.5A local-only, grants unchanged, admin_role absent, register empty). **Gates:** 20.5A 40/40, 20.4C 42/42, 20.4A 39/39, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. Transcript + captures outside Git in `qf-staging-workspace/QF-MVP-20.5AP-PREFLIGHT-20260724T153254Z/`. **Next: application review** (one `db push` applying exactly `20260723001000`, then `verify_qf_mvp_20_5a.sql`); owner binding deferred; 20.4 register stays empty. Record: [`QF-MVP-20-5A-PROFILES-PRIVILEGE-ADMIN-ROLE-CLEANUP.md`](QF-MVP-20-5A-PROFILES-PRIVILEGE-ADMIN-ROLE-CLEANUP.md) §11.

- **20.5A1 Profiles service_role least-privilege correction (offline, migration still NOT applied)** — **`PROFILES_SERVICE_ROLE_LEAST_PRIVILEGE_CORRECTION_COMPLETE_READY_FOR_PREFLIGHT`**. Correction commit at parent `3b2c51d…` (branch now ahead of origin by **2**, behind 0; the 20.5A commit `3b2c51d` is NOT amended). **No managed DB access; migration `20260723001000` remains unapplied.** **Proved defect:** as-generated 20.5A granted `select, insert, update, delete` on profiles to `service_role`, but no runtime deletes profiles (bootstrap upserts; dashboard reads) — an unnecessary privilege the self-verify/verifier/validator did not reject. **Correction:** the migration now `revoke all … from service_role` then grants **only `select, insert, update`**; the `$verify$` proves SELECT+INSERT+UPDATE present and DELETE/TRUNCATE/REFERENCES/TRIGGER absent (+MAINTAIN absent on PG17, `server_version_num`-guarded), failing closed on any over-grant (incl. via membership, since `has_table_privilege` is effective). Corrected migration hash `5cf12b726b40aea6cac1d7f97eb2b289d6a46db39f9adc197249a37939a16592` (was `5ca87813…`); verifier `fb582dbd…` (was `e4b3fec9…`); validator `2458f7f8…` (was `c061d488…`). Verifier `Y16` now asserts the exact matrix (still 23 rows). Validator adds rule **G19** (rejects service_role DELETE/TRUNCATE/REFERENCES/TRIGGER/MAINTAIN/ALL grant, missing deterministic revoke, or a self-check omitting the DELETE-absence proof) with 3 one-defect fixtures → **23 migration + 3 verifier fixtures, PASS**. **Final service_role matrix: SELECT/INSERT/UPDATE only** (no DELETE/TRUNCATE/REFERENCES/TRIGGER/MAINTAIN); authenticated SELECT-only, anon/PUBLIC zero — unchanged. **Gates:** 20.5A1 validator PASS, 20.4C 42/42, 20.4A 39/39, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Next: staging preflight.** Record: [`QF-MVP-20-5A-PROFILES-PRIVILEGE-ADMIN-ROLE-CLEANUP.md`](QF-MVP-20-5A-PROFILES-PRIVILEGE-ADMIN-ROLE-CLEANUP.md) §10.

- **20.5A Profiles base-table privilege + admin_role drift cleanup (generated, NOT applied)** — **`PROFILES_PRIVILEGE_AND_ADMIN_ROLE_CLEANUP_GENERATED_REVIEWED_READY_FOR_PREFLIGHT`** (service_role matrix superseded by 20.5A1 — DELETE removed). Generation + offline-review only at HEAD `5fc5ba4…` (origin identical, **0/0**, clean; no collision at `20260723001000`; `20260723000900` was the prior highest). **No managed database accessed; migration generated but not applied.** Closes the two mandatory profiles/auth closeout follow-ups. **Inventory (repo-proved):** `public.profiles(id, full_name, phone, role check(admin/vendor), is_active)` with RLS + 3 `authenticated` policies (self read / self update / admin all); `is_admin()` reads `profiles.role` (never admin_role); **no explicit table GRANT exists in any migration** (drift — on the staging squash `authenticated` holds 0 privilege). Every runtime profiles read is `SELECT role` (login/access guards via authenticated session; admin dashboard + bootstrap via service_role); **zero authenticated writes** anywhere. **Authenticated privilege decision: SELECT only** (anon/PUBLIC none; service_role SELECT/INSERT/UPDATE for the bootstrap — DELETE removed in 20.5A1). **Role-escalation (mandatory):** the `profiles self update` policy checks only `id=auth.uid()` (not columns), so a table UPDATE grant would allow `set role='admin'`; since no runtime needs UPDATE, the migration grants SELECT-only and revokes UPDATE — escalation closed at the grant layer; self-check + verifier assert no authenticated UPDATE. **admin_role drift = State B (production drift):** the `20260621000006` `profiles.admin_role` column is non-authoritative + unused (only a dead type field + a dead display fallback; the real authority marker is the untouched `app_metadata.admin_role` JWT claim) → `drop column if exists admin_role` (no-op on staging squash, clean removal on production); the two dead runtime refs removed + a stale bootstrap log corrected. **Migration** `supabase/migrations/20260723001000_qf_mvp_profiles_privilege_admin_role_cleanup.sql` (`5ca87813…`): re-assert RLS; revoke-then-grant least-privilege; drop admin_role; catalog-fact `$verify$` (RLS, authenticated SELECT + no write/escalation, anon/PUBLIC zero, service_role bootstrap, admin_role absent, role/is_admin/D-trigger present, 20.4C register present, no owner-binding) — no DML/backfill/role-rewrite/txn/history-write/default-priv/broad grant; auth trigger + assignment/package/credit/lead untouched. **Verifier** `supabase/staging-verification/verify_qf_mvp_20_5a.sql` (`e4b3fec9…`, 23 rows, SELECT-only). **Validator** `scripts/mvp/auth/validate-qf-mvp-20-5a.mjs` (20 migration + 3 verifier one-defect fixtures) via a comment/string/dollar-body-aware tokenizer; real artifacts graded by the same evaluators. **Runtime cleanup:** `adminTypes.ts` admin_role field removed; `AdminSectionPage.tsx` renders constant "Superadmin"; `grant-superadmin.mjs` log corrected. **Gates:** 20.5A validator PASS, 20.4C 42/42, 20.4A 39/39, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Next: staging preflight** (one `db push --linked --dry-run` + the verifier) before any application; owner binding still deferred; 20.4 exceptions remain NO_FINANCIAL_CHANGE and are not inserted. Record: [`QF-MVP-20-5A-PROFILES-PRIVILEGE-ADMIN-ROLE-CLEANUP.md`](QF-MVP-20-5A-PROFILES-PRIVILEGE-ADMIN-ROLE-CLEANUP.md).

- **20.4CA Immutable exception-register staging APPLICATION + verification** — **`HISTORICAL_LEDGER_EXCEPTION_REGISTER_APPLIED_AND_VERIFIED_ON_STAGING`**. Applied at HEAD `06b900a…` (origin identical, **0/0**, clean; preflight commit changed exactly its 2 docs; no later application/correction). **Migration `20260723000900` APPLIED to authorized staging `uckafzuochmbvtiodmcl`** via exactly one `npx supabase db push --linked` (13:54:46→13:54:53 UTC, **exit 0**); exactly that migration applied, no earlier migration, no repair/reset, no second push. The migration's in-transaction `$verify$` NOTICE fired (immutable append-only table, RLS on, service_role SELECT+INSERT only, U/D/T trigger-blocked for every role, locked to INSUFFICIENT_EVIDENCE / EXCEPTION_RECORD_ONLY / NO_FINANCIAL_CHANGE, all mutation flags false, no `vendor_credit_logs` backfill, non-cascading self-FK, ZERO rows, A…E intact). Trailing `pgdelta-target-ca.crt ENOENT` = known non-blocking edge-runtime cache artifact (as in E), exit 0. **Locked hashes exact** (migration `75b6faf2…`, validator `0a4256a1…`, verifier `1cfd9104…`, manifest `bafe0951…`); A/A2/B1/G/B2/C/D/E + 20.4A byte-unchanged. **External apply workspace (outside Git):** no seed.sql, no Edge Functions, 10 SQL (baseline+A…E+register), 9 repo-backed files byte-identical, register `75b6faf2…`. **Pre-state (SELECT-only):** register absent, `20260723000900` absent from 9 remote, A…E preserved, 67 tables→0 rows, all operational + auth.users/profiles=0, no candidate rows, owner-binding/grant/admin_role un-implemented. **History: before 10 local / 9 remote (sole pending) → after 10 local / 10 remote**, all paired once, applied once, no duplicate. **Locked verifier `verify_qf_mvp_20_4c.sql` (`1cfd9104…`) ran once: 22 rows, 22 PASS / 0 FAIL** — register present with exact 20-column contract + 0 rows, RLS on, PUBLIC/anon/authenticated zero privilege, service_role SELECT+INSERT only, immutable trigger (tgtype 27) + no-truncate trigger (tgtype 34) both bound, trigger fns not untrusted-executable, 6 financial locks + 10 integrity constraints (incl. unique idempotency), only non-cascading self-FK, `vendor_credit_logs` untouched, A/A2/B1/G/B2/C/D/E intact; owner=`postgres` (informational). **No-financial-write (post SELECT-only):** register 0 rows, sum of ALL public rows 0, vendors/vendor_packages/lead_assignments/vendor_credit_logs/auth.users/profiles all 0, public tables 67→68 (only the register added); no exception row, no balance/package/assignment/ledger change, no RPC, no candidate data. **Gates:** 20.4C 42/42, 20.4A 39/39, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. Transcript + queries + before/after captures outside Git in `qf-staging-workspace/QF-MVP-20.4CA-APPLICATION-20260724T135333Z/`. **Next: founder-approved exception-population design/review OR Marketplace closeout — NOT automatic candidate insertion** (the 27-row population stays a separate founder-authorized plan, still not authorized). `profiles`-GRANT, `admin_role`, owner binding remain separate. Record: [`QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md`](QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md) §0-D.

- **20.4CP Immutable exception-register staging preflight (dry-run only, NOT applied)** — **`HISTORICAL_LEDGER_EXCEPTION_REGISTER_PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. Executed at HEAD `729abc4…` (origin identical, **0/0**, clean; generation commit touched exactly its 8 authorized paths; no later 20.4C correction). **Nothing applied** — one `npx supabase db push --linked --dry-run` + SELECT-only catalog checks + `migration list`; no push-without-dry-run, no `up/repair/reset`, no hand-executed SQL, no register row, no RPC, no balance/package/assignment/ledger change. **Linked target `uckafzuochmbvtiodmcl`** (production `yqpgcsduqbxulrlzwzap` + QF-Jarvis `coilipywdvxklewquqvv` not the target, not contacted). **Phase-locked full hashes** (match 20.4C prefixes): migration `75b6faf2f7ed52007b79b9036dd5998f00eb67d88e62ffa34b3d9d1343c5039d`, validator `0a4256a1…`, verifier `1cfd9104…`, manifest `bafe0951…`; applied A/A2/B1/G/B2/C/D/E + 20.4A artifacts byte-unchanged. **External apply workspace (`qf-staging-apply`, outside Git):** no seed.sql, no Edge Functions; 9 SQL (baseline+A…E, byte-identical to repo) → register absent → copied ONLY the locked migration (byte-identical, `75b6faf2…`) → **10 SQL**. **Live pre-state (SELECT-only):** register table/functions/triggers **absent**; `20260723000900` **absent** from remote; **9** earlier present; A…E preserved (B1 authority, 4 B2 triggers, C `vendor_public_v`, D `on_auth_user_created`, E 6-RPC untrusted-EXECUTE=0); **67** public tables → **0** rows; auth.users/profiles/lead_assignments/vendor_credit_logs/vendors/vendor_packages all **0** (no candidate rows anywhere); no owner-binding column on leads; `profiles.admin_role` absent + `authenticated` holds no `profiles` grant (follow-ups un-implemented). **History: 10 local / 9 remote**, baseline…E paired, `20260723000900` local-only + **sole pending**. **Dry run (once, exit 0):** `DRY RUN: migrations will *not* be pushed`; **exactly one** proposed — `20260723000900_qf_mvp_credit_ledger_reconciliation_exception_register.sql`; no earlier migration, no application claim. **No-write proof:** post-dry-run history + pre-state re-run **identical** (remote still 9, 20.4C still local-only, register still absent, all tables still empty). **Gates:** 20.4C **42/42**, 20.4A 39/39, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. Transcript + query + before/after captures outside Git in `qf-staging-workspace/QF-MVP-20.4CP-PREFLIGHT-20260724T132916Z/`. **Next: application review** (one `db push` applying exactly `20260723000900`, then `verify_qf_mvp_20_4c.sql`); 27-candidate population stays a separate founder-authorized insertion plan. Record: [`QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md`](QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md) §0-C.

- **20.4C Immutable historical credit-ledger exception register (migration generated, NOT applied)** — **`HISTORICAL_LEDGER_EXCEPTION_REGISTER_GENERATED_REVIEWED_READY_FOR_PREFLIGHT`**. Generated at HEAD `854709d…` (origin identical, **0/0**, clean). **No managed database accessed — no `db push`, no dry run, no application, no data written; no candidate UUID/PII in Git.** **Founder ruling (LOCKED):** on the 20.4B evidence (all **27** candidates `INSUFFICIENT_EVIDENCE`, zero canonical/legacy ledger evidence, zero conflicts, zero arithmetic violations) the founder approved **NO_FINANCIAL_CHANGE** — **zero debit, zero refund, zero package change, no `vendor_credit_logs` backfill**. **Deliverable:** one forward-only migration `supabase/migrations/20260723000900_qf_mvp_credit_ledger_reconciliation_exception_register.sql` (`75b6faf2…`) creating the immutable, append-only table `public.credit_ledger_reconciliation_exceptions` — **separate from `vendor_credit_logs`** (per §8: the arithmetic ledger cannot honestly hold evidence-only rows), carrying **no credit-mutation semantics** and touching **no** `vendors`/`vendor_packages`/`lead_assignments`/`vendor_credit_logs` row. **Schema-pinned outcome:** CHECK-locks `classification='INSUFFICIENT_EVIDENCE'`, `correction_mode='EXCEPTION_RECORD_ONLY'`, `founder_decision='NO_FINANCIAL_CHANGE'`, and `balance_mutation=package_mutation=vendor_credit_logs_backfill=false`. **Immutable/append-only:** RLS enabled; `UPDATE`/`DELETE`/`TRUNCATE` blocked for **every** role incl. `service_role` by `trg_clre_immutable` (BEFORE UPDATE|DELETE ROW, tgtype 27) + `trg_clre_no_truncate` (BEFORE TRUNCATE STATEMENT, tgtype 34) with matching revokes; future resolution is append-only via `supersedes_record_id` (self-FK `ON DELETE RESTRICT`). **Least privilege:** PUBLIC/anon/authenticated get **nothing**; `service_role` granted **only SELECT+INSERT**; no policy targets an untrusted role. **Evidence-preserving keys:** `assignment_id`/`vendor_id` are plain UUIDs with **no** FK to operational tables (no cascade can erase history); each row carries `audit_run_id`, `audit_sql_sha256`/`evidence_manifest_sha256` (64-hex CHECK), `reviewer_actor` (FOUNDER/AUTHORIZED_ADMIN), `reviewed_at`, `reason`, and a **unique** `idempotency_key`. Migration ships **empty** (expected post-application **0 rows**); populating it is a separate founder-authorized insertion plan. **Offline validator** `scripts/mvp/reconciliation/validate-qf-mvp-20-4c.mjs` (`0a4256a1…`) **42/42** with **21 migration + 3 verifier one-defect fixtures** (rejects INSERT/backfill, production UUID, PII column, `vendor_credit_logs`-as-register, `vendors`/ledger mutation, `service_role` UPDATE + missing U/D/T revoke, `authenticated` grant, missing RLS, dropped immutability trigger, cascade FK, removed false-flag/classification/correction/decision/actor/hash/idempotency locks, ALTER DEFAULT PRIVILEGES, owner-binding column, removed self-verification, verifier DML, dropped required assertions — all caught) via a comment/string/dollar-body-aware tokenizer; the real migration and verifier are graded by the **same** evaluators. **SELECT-only staging verifier** `supabase/staging-verification/verify_qf_mvp_20_4c.sql` (`1cfd9104…`, 22 rows). **Contract manifest** `scripts/mvp/reconciliation/qf-mvp-20-4c-register-contract.json` (`bafe0951…`) — schema contract only, **zero production rows**. `verify:mvp` now also runs `test:mvp:20-4c`; R1 declared-later set now lists `20260723000900` (harness still **62/62**). **Gates:** 20.4C **42/42**, 20.4A 39/39, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **Next: staging preflight** (one `db push --linked --dry-run` + the SELECT-only verifier) before any application. `profiles`-GRANT, `admin_role` and owner binding remain separate. Record: [`QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md`](QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md) §0-B.

- **20.4B Production historical credit-ledger evidence audit (SELECT-only)** — **`PRODUCTION_LEDGER_EVIDENCE_AUDIT_COMPLETE_READY_FOR_FOUNDER_REVIEW`**. Founder-authorized one-time SELECT-only audit executed against **production `yqpgcsduqbxulrlzwzap`** ('QuickFurno', ap-southeast-1, PostgreSQL 17.6) on 2026-07-24. **Facts only — no classification approved, no correction authorized, no data changed, nothing pushed.** **Connection truth:** `transaction_read_only=off` (technically writable); SELECT-only was **process-enforced** via the exact R00-R11 allowlist, not connection-enforced. Staging/QF-Jarvis not contacted. **Locked artifacts exact** (audit `615d3712…`, validator `3f660420…` 39/39, manifest schema `a63d04a7…`); the identity gate required local==origin==`44247f0` (0/0) and passed only after the 20.4A commit was pushed (a prior run correctly HALTED fail-closed on the unpushed origin). **R02 fingerprint gate PASS** — `qf_apply_vendor_credit_delta` def MD5 `45ad58beb9cb1dd8ea4f77466909cc0e` matches the QF-MVP-10 record; R01 shows 4 migration rows (HISTORY_DRIFT, reported not repaired). **Aggregate (no UUIDs/PII):** **46** total assignments, **46** credit_deducted, **19** canonical assignment-debit ledger rows, 28 logs without reference_id, **27 candidates** (R04) split **admin 5 / automatic 16 / client-selected 6** (R05) — **identical to the historical 46/19/27** (2026-07-22): production UNCHANGED. R06 legacy signals: **0** unreferenced lead_assignment_debit logs for every candidate vendor (no legacy evidence to link). R07 reference conflicts **0**; R08 arithmetic violations **0**; R10 unreconcilable **0**; R09 3 distinct candidate vendors (current balances, PROHIBITED as sole proof). **R11 conservative class: all 27 → `INSUFFICIENT_EVIDENCE`** (0 PROVEN_*, 0 DATA_INVARIANT, 0 DUPLICATE). **No strong-evidence path exists in the current data for any candidate**, so no PROVEN_DEBIT/PROVEN_NO_DEBIT was asserted. **Evidence outside Git:** transcript, allowlist + statement hashes, complete R00-R11 outputs + hashes, a **schema-valid** evidence-manifest instance (27 candidate UUIDs; reviewer PENDING; approval null; correction_mode NONE_PENDING_REVIEW; balance/package mutation false; execution NOT AUTHORIZED; verification EVIDENCE_CAPTURED_ONLY), and the aggregate report — in `qf-production-workspace/QF-MVP-20.4B-PRODUCTION-AUDIT-…/`. **No production UUID, row or secret entered Git.** **No-write proof:** only the identity SELECT + the exact R00-R11 SELECTs ran, each once; no write/DDL/DCL/CALL/DO/COPY, no RPC (no qf_apply_vendor_credit_delta / deduct_/restore_/increment_vendor_credit / assignment RPC), no migration command, no session change. **Deviation:** the schema lacks literal `NOT_AUTHORIZED`/`EVIDENCE_CAPTURED_ONLY` enum values, so the locked (uneditable) schema's `NOT_STARTED`/`NOT_VERIFIED` encode that intent, disclosed in the manifest + aggregate report. **Gates:** 20.4A **39/39**, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. Record: [`QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md`](QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md).

Next: **founder evidence review + a correction-plan decision** — NOT a migration application. Any correction stays gated on an approved manifest; ambiguous rows keep zero balance effect. `profiles`-GRANT, `admin_role` and owner binding remain separate. Migrations A, A2, B1, G, B2, C, D and E are applied and immutable.
- **20.4A Historical credit-ledger reconciliation (design + SELECT-only audit pack)** — **`HISTORICAL_LEDGER_RECONCILIATION_DESIGN_READY_FOR_PRODUCTION_SELECT_ONLY_AUDIT`**. Design-only; **no database accessed, no migration, nothing pushed**. Turns the committed QF-MVP-10 production evidence (46 credit_deducted assignments, 19 canonical assignment-debit ledger rows, **27 missing**: admin 5 / automatic 16 / client-selected 6 — **historical observations, NOT invariants**) into an executable, reviewable contract. **Root cause (from evidence):** the four PUBLIC/anon assignment RPCs + legacy `deduct_vendor_credit`/`restore_vendor_credit`/`increment_vendor_credits` mutate balances **without** writing assignment-linked ledger evidence; canonical `qf_apply_vendor_credit_delta` (service_role, dedup on `uq_vendor_credit_logs_reference`) is the only ledger-writing path. **Canonical debit evidence = `vendor_credit_logs` WHERE reference_type='lead_assignment' AND reference_id=assignment::text AND change_type='lead_assignment_debit'.** **No blind backfill:** missing ledger = missing *evidence*, never a missing *debit*; `credit_deducted`, current balances, package remaining and assignment existence are **PROHIBITED as sole proof**. **Closed 8-class vocabulary** (PROVEN_DEBIT_ALREADY_APPLIED / PROVEN_NO_DEBIT / ALREADY_HAS_EQUIVALENT_LEDGER / DUPLICATE_OR_REFERENCE_CONFLICT / INSUFFICIENT_EVIDENCE / DATA_INVARIANT_VIOLATION / ALREADY_RECONCILED / OUT_OF_SCOPE_NON_CREDIT) + **5 correction modes** (EVIDENCE_ONLY / STATE_CORRECTION_DEBIT [never automatic] / STATE_CORRECTION_REFUND / EXCEPTION_RECORD_ONLY / LINK_EXISTING_LEGACY_EVIDENCE). **Authority matrix:** SELECT-only collector decides nothing; classifier proposes only; **founder/authorized-admin approves**; service_role executes an approved immutable plan; **Jarvis/n8n have no authority**. **Key schema finding:** `vendor_credit_logs` is an arithmetic, reference-unique ledger and **cannot** safely hold evidence-only rows → recommend a separate immutable `credit_ledger_reconciliation` table in a LATER reviewed migration (not created here). **Deliverables:** SELECT-only audit pack `supabase/reconciliation/qf_mvp_20_4_historical_credit_ledger_audit.sql` (`615d3712…`) — one SELECT/CTE chain, R00-R11 result sets, model + schema fingerprints (R00/R02), migration-history reported-not-repaired (R01), candidate population + source breakdown (R04/R05), legacy signals SUPPORTING-only (R06), reference conflicts (R07), arithmetic violations (R08), per-vendor facts labelled prohibited-as-proof (R09), unreconcilable rows (R10), and a CONSERVATIVE `sql_proposed_class` that **never emits PROVEN_***  (R11); PII-minimised (UUIDs/timestamps/source/refs/integers only). **Offline validator** `scripts/mvp/reconciliation/validate-qf-mvp-20-4a.mjs` (`3f660420…`) **39/39** with **16 one-defect fixtures** (INSERT/UPDATE, writable CTE, BEGIN, legacy/canonical RPC invocation, header removal, PII column, hardcoded-27 gate, SQL-emitted PROVEN, class-from-credit_deducted, missing ORDER BY, embedded URL, dropped breakdown/fingerprint, weakened reference contract — all caught) + a comment/string-aware tokenizer + 4 focused classifier scenarios. **Empty evidence-manifest schema** `qf-mvp-20-4-evidence-manifest.schema.json` (`a63d04a7…`): closed enums, prohibited-evidence checks pinned false, strong-evidence references required before any PROVEN_*, **zero live rows**, placeholder environment. **Runbook:** the later phase needs explicit founder authorization, a process-enforced SELECT-only allowlist over a technically-writable connection, evidence captured **outside Git**, **no correction in the same session**, **no automatic migration application** against drifted production, ambiguous rows unresolved with zero balance effect. **Gates:** 20.4A **39/39**, E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0 (now runs the 20.4A validator too), typecheck/lint/build clean, `git diff --check` exit 0. Record: [`QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md`](QF-MVP-20-4-HISTORICAL-CREDIT-LEDGER-RECONCILIATION.md).

Next: a **separately founder-authorized production SELECT-only evidence audit** (run the audit pack under the runbook, capture evidence outside Git, populate the manifest for founder review). **No correction, no migration, no write** until an approved manifest exists. `profiles`-GRANT + `admin_role` follow-ups remain separate; owner binding deferred. Migrations A, A2, B1, G, B2, C, D and E are applied and immutable.
- **20.3EA Legacy assignment RPC staging APPLICATION + verification** — **`E_APPLIED_VERIFIED_ON_STAGING_READY_FOR_COMMIT_REVIEW`**. Migration E **APPLIED to authorized staging `uckafzuochmbvtiodmcl`** on 2026-07-24T11:18:14→11:18:22Z via the single authorized `npx supabase db push --linked` (**exit 0**); exactly `20260723000800` applied, no earlier migration, no repair, no second push. The migration's §2 self-verification NOTICE fired in-transaction (*six legacy assignment RPCs service_role-only — PUBLIC/anon/authenticated hold no EXECUTE; all present as SECURITY DEFINER jsonb; get_public_eligible_vendors anon/authenticated EXECUTE preserved; canonical B1 authority server-only and intact; A/A2/B1/G/B2/C/D preserved; owner binding + 20.4 not started*). Trailing `pgdelta-target-ca.crt ENOENT` = local edge-runtime cache artifact, non-blocking at exit 0. **History:** 9 local / 8 remote → **9 local / 9 remote**, E recorded exactly once, no pending. **Locked verifier** `verify_qf_mvp_20_3e.sql` (re-hashed `a4ab76fa…`, run byte-exact via `supabase db query --linked --file`): **21 rows, 21 PASS / 0 FAIL**, SELECT-only, no RPC invoked — per-target server-only (E03-E08), untrusted-EXECUTE=0 (E09), six secdef jsonb (E10), safe RPC preserved (E11), canonical server-only (E12), B1/B2/G/C/D preserved (E13-E17), six retained (E18), owner-binding + 20.4 fenced (E19/E21). **ACL NO-OP PROVED:** a SELECT-only catalog snapshot **before vs after** is byte-identical — every target **OID unchanged** (17490/17494/17495/17497/17499/17502 → no DROP+CREATE), every `prosrc` md5 unchanged (no body drift), untrusted EXECUTE=0 + service_role present throughout; canonical (19377) and safe public (17509) unchanged. **Empty state:** 67 public tables, **0 rows**, 0 auth users — no data written, no RPC invoked. **Preservation:** B1 5 functions, B2 4 functions + 4 triggers, G boundary, C `vendor_public_v` + anon revocation, D trigger+function all intact; six legacy retained; owner-binding deferred. **Advisors:** E introduced **zero** new security/performance advisors (identical to post-D; none references E or the six — the only ERROR is the KNOWN C `vendor_public_v`; perf 183 INFO + 44 WARN unchanged). **Honest durability contract reaffirmed:** E secured/verified the six exact objects at application time; it does NOT alter default privileges, protect a later DROP+CREATE, or rerun — any future recreation must re-apply the revokes+grant (validator R12). **Two disclosed follow-ups still open, out of scope:** `profiles` `authenticated` table-GRANT (mandatory before final closeout) and inert `profiles.admin_role` drift. Transcript `QF-MVP-20.3EA-APPLICATION-…txt` in the external evidence workspace, **not committed**. **No production/QF-Jarvis access; no dry run; no repair/reset/up; no second push; no RPC invoked; nothing pushed.** Record: [`QF-MVP-20-3E-LEGACY-ASSIGNMENT-RPC-EXECUTE-REVOCATION.md`](QF-MVP-20-3E-LEGACY-ASSIGNMENT-RPC-EXECUTE-REVOCATION.md).

Next (after commit review): **QF-MVP-20.4** — historical ledger reconciliation. Not owner binding. Migrations A, A2, B1, G, B2, C, D and **E** are applied and immutable. Never edit an applied migration, never `migration repair`, never `db reset`.
- **20.3EP Legacy assignment RPC staging preflight** — **`E_PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. On the synchronized HEAD `fc12992` (both E commits pushed; origin identical, 0/0). **Source-proof gate PASS:** validator **51/51** structurally requires the six signature-qualified targets + PUBLIC/anon/authenticated revokes + service_role retention + safe/canonical exclusions + no-DDL/no-broad/R12 current-object contract + SELECT-only verifier; the verifier reads **actual catalogs** (`to_regprocedure` ×31, `has_function_privilege` ×62, `pg_proc`/`pg_trigger`) with zero `obj_description`/`prosrc`/`pg_get_functiondef`. **Offline gates:** E 51/51, D 110/110, C 83/83, B2 61/61, B1/G 165/165, R1 62/62, `verify:mvp` exit 0, `git diff --check` exit 0. **Apply workspace** (`qf-staging-apply`, outside git, no seed/functions): 8 SQL files baseline→D hash-exact; E **absent** (case A) → copied in, **byte-identical** to repo (`94c696cd…`, 16023 bytes) → 9 SQL files. **Linked target** `uckafzuochmbvtiodmcl`; production/QF-Jarvis not linked/connected/referenced. **Live pre-state ACL matrix (SELECT-only, no RPC invoked) proves E is a no-op:** all six targets already have PUBLIC/anon/authenticated EXECUTE **absent** + service_role **present** (owner postgres, SECURITY DEFINER, jsonb, matching the manifest); `get_public_eligible_vendors` keeps anon/authenticated/service_role EXECUTE (STABLE, TABLE); `qf_assign_lead_vendors_v2` service_role-only. **Migration history before:** **9 local / 8 remote**, E (`20260723000800`) sole pending. **The one authorized `db push --linked --dry-run`** (2026-07-24T11:01:33→11:01:38Z, **exit 0**) proposed **exactly** `20260723000800` under a DRY RUN banner, no earlier migration, no application claim. **History after:** byte-identical (8 remote, E local-only); repeated ACL snapshot unchanged; `schema_migrations` rows for E = **0** → **zero write, E not recorded, no RPC invoked**. **Follow-ups reconfirmed, unchanged:** `profiles` `authenticated` table-GRANT (mandatory before final closeout), inert `profiles.admin_role` drift; 20.4 not started; owner binding deferred. Transcript `QF-MVP-20.3EP-PREFLIGHT-…txt` in the external evidence workspace, **not committed**. **E was NOT applied; production/QF-Jarvis untouched; nothing pushed.**

Next: **QF-MVP-20.3E staging application** — one authorized `npx supabase db push --linked` records exactly `20260723000800`, then the SELECT-only verifier (21 rows) confirms the posture. Not 20.4, not owner binding. Migrations A, A2, B1, G, B2, C and D are applied and immutable.
- **20.3EGR1 Legacy assignment RPC ACL durability-contract correction** — **`E_ACL_DURABILITY_CONTRACT_CORRECTED_READY_FOR_PREFLIGHT`**. **Why `26bd744` was not accepted for preflight:** the EG report claimed E is an *“idempotent re-assertion surviving any future DROP+CREATE ACL reset”* — **an impossible guarantee, now retracted**. PostgreSQL grants EXECUTE to PUBLIC by default when a function is newly created; E does not touch `ALTER DEFAULT PRIVILEGES` and applies only once, so a later `DROP FUNCTION`+`CREATE FUNCTION` mints a NEW object with default PUBLIC EXECUTE that E neither re-runs against nor re-secures. (A `CREATE OR REPLACE` on the same identity normally preserves the ACL, but E forbids body/signature changes to a target regardless.) **Honest contract:** E is a **current-object ACL re-assertion + catalog-verification milestone** that pins the ACLs of the objects existing at its application time — a no-op on the accepted current staging posture (already service_role-only). **Forward protection is a GOVERNANCE obligation, not a database mechanism:** any later migration recreating one of the six signatures MUST, in the same migration, re-`REVOKE EXECUTE FROM public, anon, authenticated` + re-`GRANT service_role` and re-validate the manifest, and the gates must reject a recreated state-changing RPC lacking that posture. **Correction was comment/docs/validator only:** the executable REVOKE/GRANT design is unchanged (verifier `a4ab76fa…` and manifest `8bfba966…` **unchanged**); the migration got a comment-only edit (new hash `94c696cd…`) and the validator a new structural rule **R12** (`2e9ef53a…`) proving E is current-object-only — no `ALTER DEFAULT PRIVILEGES`, no `ON ALL FUNCTIONS`/schema-wide grant, every EXECUTE grant/revoke signature-qualified — with 3 new fixtures (T ALTER DEFAULT PRIVILEGES shortcut, U schema-wide revoke, V unqualified target). **Independent review reconfirmed:** current staging is already at the desired six-RPC ACL posture, E reasserts the same ACLs and records the milestone, a no-op ACL result is acceptable, and no live vulnerability is newly closed. Six target signatures exact; canonical + safe-public exclusions correct; no runtime change; no function-definition change. **Gates:** E **51/51** (22 migration + 6 verifier fixtures), D **110/110**, C **83/83**, B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **No database contacted; nothing pushed.** Record: [`QF-MVP-20-3E-LEGACY-ASSIGNMENT-RPC-EXECUTE-REVOCATION.md`](QF-MVP-20-3E-LEGACY-ASSIGNMENT-RPC-EXECUTE-REVOCATION.md).

Next: **QF-MVP-20.3E staging preflight — not application**, and only after acceptance and pushing **both** local E commits (preflight origin-sync gate; EGR1 forbidden to push). A, A2, B1, G, B2, C and D are applied and immutable.
- **20.3EG Legacy assignment RPC EXECUTE revocation (generate/review)** — superseded by EGR1; **`E_ACL_DURABILITY_CONTRACT_CORRECTED_READY_FOR_PREFLIGHT`**. Generated at the synchronized HEAD `2210ac71…` (origin identical, 0/0, clean; no collision for `20260723000800`; D was the highest migration). **No database contacted, no dry run, nothing pushed.** **What E does:** ACL-only — pins the **six legacy state-changing lead-assignment RPCs** (`admin_smart_assign_lead_to_vendors`, `assign_client_selected_vendor_to_group`, `assign_lead_to_paid_vendors_phase26a`, `assign_lead_to_preferred_vendor`, `assign_lead_to_vendors`, `assign_vendor_to_requirement_group`, all SECURITY DEFINER `jsonb`, signature-qualified) to **server-owned authority only** — `REVOKE EXECUTE FROM public, anon, authenticated` + `GRANT EXECUTE TO service_role`. **HONEST RECONCILIATION:** the applied baseline ALREADY carries that exact revocation for all six, and **no applied migration re-granted them** (the D advisors confirm none of the six is anon/authenticated-executable), so E does **not** close a live hole — it is a **current-object ACL re-assertion + catalog-verification milestone** (idempotent no-op on the accepted current posture). **QF-MVP-20.3EGR1 retracted a false durability overclaim**: E pins the ACLs of the function objects that exist at its application time and does **not** survive a later `DROP FUNCTION`+`CREATE FUNCTION` (which mints a new object with default PUBLIC EXECUTE); E does not re-run and does not touch default privileges, so forward protection is a **governance obligation** — any future migration recreating a target must itself re-revoke PUBLIC/anon/authenticated (validator R12 encodes the current-object-only contract). Six reported = six targeted = six already locked. **PUBLIC is named in every REVOKE** because it is a pseudo-role every role inherits — revoking only anon/authenticated would leak EXECUTE via PUBLIC (validator R06). **Excluded (proved):** the canonical B1 `qf_assign_lead_vendors_v2` (service-role only, verify-only); the safe read-only `get_public_eligible_vendors` (keeps anon/authenticated EXECUTE — it is called with `publicClient()` at `leadService.ts:351`); and all credit/package functions (not lead-assignment, not anon-granted). **Runtime:** R1 already migrated every consumer to the canonical adapter, so there are **zero** live `.rpc()` calls to the six (comments only); the canonical path uses `adminClient()` (service_role). **No runtime change required, proved.** **Immutability manifest** (`qf-mvp-20-3e-manifest.json`, `8bfba966…`) hashes each target's baseline body; the validator re-derives every hash from the baseline, so with E's no-DDL rule (R02) E provably changes ACLs only. **Validator** `validate-qf-mvp-20-3e.mjs` (**corrected** `2e9ef53a…`) **51/51** with **22 migration + 6 verifier one-defect fixtures** (incl. rule R12 current-object-only) — leave PUBLIC/anon/authenticated EXECUTE, revoke service_role, revoke the safe RPC, omit/add/unqualify a target, DROP/CREATE/ALTER a target, ALTER DEFAULT PRIVILEGES, broad schema revoke, table/data mutation, owner-binding column, functiondef assertion, verifier DML — all caught. **SELECT-only verifier** `verify_qf_mvp_20_3e.sql` (`a4ab76fa…`) **21 rows**: per-target server_only posture (E03-E08), aggregate untrusted-EXECUTE=0 (E09), all six still SECURITY DEFINER jsonb (E10), safe RPC preserved (E11), canonical service-only (E12), B1/B2/G/C/D preserved (E13-E17), six retained (E18), owner-binding + 20.4 fenced (E19/E21); never invokes an assignment RPC. **Independent review caught one robustness defect:** verifier E20 aggregated owners with `string_agg(pg_get_userbyid(...))` — `name` vs `text` (the B2R1 class) — fixed with `::text`. **Migration** (corrected, comment-only) `94c696cd…`. **Two disclosed follow-ups still tracked, out of scope:** `profiles` `authenticated` table-GRANT (mandatory before final closeout) and inert `profiles.admin_role` drift. **Gates:** E **51/51**, D **110/110**, C **83/83**, B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0 (now runs the E validator too), typecheck/lint/build clean, `git diff --check` exit 0. Record: [`QF-MVP-20-3E-LEGACY-ASSIGNMENT-RPC-EXECUTE-REVOCATION.md`](QF-MVP-20-3E-LEGACY-ASSIGNMENT-RPC-EXECUTE-REVOCATION.md).

Next: **QF-MVP-20.3E staging preflight — not application.** E is generated and reviewed only; nothing has been applied and no dry run has been executed. Then E application → QF-MVP-20.4. Migrations A, A2, B1, G, B2, C and D are applied and immutable. Never edit an applied migration, never `migration repair`, never `db reset`.
- **20.3DA Auth-trigger staging APPLICATION + verification** — **`D_APPLIED_VERIFIED_ON_STAGING_READY_FOR_COMMIT_REVIEW`**. Migration D **APPLIED to authorized staging `uckafzuochmbvtiodmcl`** on 2026-07-24T06:47:46→06:47:57Z via the single authorized `npx supabase db push --linked` (**exit 0**); exactly `20260723000700` applied, no earlier migration, no repair, no second push. The migration's own §4 self-verification NOTICE fired in-transaction (*handle_new_user() hardened — role from the app_metadata marker, neutral by default, admin unreachable; profiles.role nullable; on_auth_user_created AFTER INSERT on auth.users tgtype 5, enabled, sole auth trigger; untrusted EXECUTE revoked; A/A2/B1/G/B2/C intact; E not started; no backfill*). The `drop trigger … skipping` NOTICE was expected (trigger absent pre-apply); the trailing `pgdelta-target-ca.crt ENOENT` was a **local** edge-runtime catalog-cache artifact, non-blocking at exit 0. **History:** 8 local / 7 remote → **8 local / 8 remote**, D recorded exactly once, no pending. **Locked verifier** `verify_qf_mvp_20_3d.sql` (re-hashed `f0e87bfb…`, run byte-exact via `supabase db query --linked --file`): **37 rows, 37 PASS / 0 FAIL**, contiguous, no skips — every installed-body row D27, D29-D37 PASS. **Live catalog facts:** `handle_new_user/0`, SECURITY DEFINER, returns trigger, `search_path=pg_catalog, public, pg_temp`, owner postgres, **body is the D form** (`qf_principal` present, old `coalesce` gone), no dynamic/external calls; trigger `tgtype=5` enabled on `auth.users`, bound to the function, no other auth trigger; **anon/authenticated EXECUTE false, service_role true**. **Empty state preserved:** 67 public tables, **0 total rows**, 0 auth users, 0 profiles — D created no row and performed no backfill; only schema change is the hardened function body + the auth.users trigger. **Preservation:** C `vendor_public_v` + anon revocation intact, B1 5 functions, B2 4 triggers, G boundary, 6 legacy RPCs RETAINED with EXECUTE untouched; E/20.4/owner-binding not started. **Advisors:** neither `handle_new_user` nor `on_auth_user_created` appears in ANY security or performance advisor (D's EXECUTE is locked to service_role, so it exposes no callable surface — the expected non-blocking SECURITY DEFINER posture); the only ERROR is the KNOWN, expected `security_definer_view` on `vendor_public_v` (Migration C), and `multiple_permissive_policies` stays 34 (unchanged from post-C). No unexpected D-related finding. **Two disclosed follow-ups still open, out of scope:** `profiles` `authenticated` table-GRANT (**mandatory** before final closeout — D does not depend on it, SECURITY DEFINER) and inert `profiles.admin_role` drift. Transcript `QF-MVP-20.3DA-APPLICATION-…txt` in the external evidence workspace, **not committed**. **No production/QF-Jarvis access; no dry run; no repair/reset/up; no second push; nothing pushed.** Record: [`QF-MVP-20-3D-AUTH-USER-ONBOARDING-TRIGGER.md`](QF-MVP-20-3D-AUTH-USER-ONBOARDING-TRIGGER.md).

Next (after commit review): **Migration E** — legacy assignment-RPC EXECUTE revocation. Migrations A, A2, B1, G, B2, C and **D** are applied and immutable. Never edit an applied migration, never `migration repair`, never `db reset`.
- **20.3DP2 Auth-trigger staging preflight (rerun)** — **`D_PREFLIGHT_COMPLETE_READY_FOR_APPLICATION_REVIEW`**. The rerun after DVR1 fixed the source-proof gap. **Verifier installed-body source-proof gate: PASS** (the gate that stopped DP) — `evaluateDVerifier` returns 0 findings, and the verifier proves the installed `pg_proc.prosrc` (comment-stripped in-SQL) via rows 27 + 29-37, not the COMMENT. **Offline gates:** D **110/110** (23 migration + 12 verifier fixtures, 5/5 body-drift sims, 9/9 scenarios, 5/5 marker mutations), C **83/83**, B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, `git diff --check` exit 0. **External apply workspace** (`qf-staging-apply`, outside git, no seed.sql, no functions): 7 SQL files baseline→C all hash-exact; D was **absent** (case A) so the corrected D was copied in and proved **byte-identical** to the repo (`8fb3c28c…`) — workspace now 8 SQL files. **Linked target** `uckafzuochmbvtiodmcl` (staging); production/QF-Jarvis not linked, connected or referenced. **Migration history before:** **8 local / 7 remote**, D (`20260723000700`) the sole pending migration. **The one authorized `npx supabase db push --linked --dry-run`** (2026-07-24T05:21:45→05:21:51Z, **exit 0**) proposed **exactly one** migration — `20260723000700_qf_mvp_auth_user_onboarding_trigger.sql` — under an explicit DRY RUN banner, no earlier migration, no application claim. **History after:** **byte-identical** to before (7 remote, D still local-only) → **zero** remote rows created; and since no migration SQL executed, no auth/profile/application row was created. **Separate follow-ups reconfirmed, not changed:** `profiles` `authenticated` table-GRANT → MANDATORY SEPARATE FOLLOW-UP before closeout (D does not depend on it — SECURITY DEFINER); `profiles.admin_role` drift → inert SEPARATE SCHEMA-CLEANUP FOLLOW-UP. Transcript `QF-MVP-20.3DP2-PREFLIGHT-…txt` in the external evidence workspace, **not committed**. **D was NOT applied; production/QF-Jarvis untouched; nothing pushed.**

Next: **QF-MVP-20.3D staging application** — one authorized `npx supabase db push --linked` records exactly `20260723000700`, then the SELECT-only verifier (37 rows) confirms the installed body. Not E, not QF-MVP-20.4. Migrations A, A2, B1, G, B2 and C are applied and immutable.
- **20.3DVR1 Auth-trigger verifier installed-body proof** — **`D_VERIFIER_INSTALLED_BODY_PROOF_CORRECTED_READY_FOR_PREFLIGHT`**. **Why:** the QF-MVP-20.3DP staging preflight **correctly halted before staging contact** (`D_PREFLIGHT_BLOCKED_VERIFIER_SOURCE_PROOF_INSUFFICIENT`) — the D verifier proved the classification contract only from the function **COMMENT** (row D27 read `obj_description()`), and no executable SQL inspected the installed body. Because `create or replace function` and `comment on function` are **separate statements**, a replaced body with an intact comment would drift undetected; proved offline against three drift mutations that the old D27 **false-passed**. **Fix (verifier + validator only; Migration D untouched):** the verifier now binds a `WITH` prelude `d_body` = the installed `pg_proc.prosrc`, comment-stripped in-SQL (block `/* */`, then line `--` with the newline-sensitive `gn` flags, then whitespace-collapse + lower-case), and `d_lits` = the exact literal set of that body. Rows **27 + 29-37** assert the frozen contract on `d_body`: trusted source `new.raw_app_meta_data->>'qf_principal'` (D29), exact `='vendor'` gate (D30), neutral `NULL` default (D31), exact insert shape with the role position an **identifier, never a literal** (D32), no `raw_user_meta_data`-fed role (D33), **no executable admin/superadmin branch** (D34), an **exhaustive literal allowlist** = `{full_name, phone, qf_principal, vendor}` (D35), `ON CONFLICT DO NOTHING` (D36), and `return new` with no dynamic SQL / external call / business-state write (D37). **This satisfies BOTH locked policies at once:** B1R2 forbade an *unstripped* negative regex (false-*fails* on body comments); DP forbade a *COMMENT-only* proof (false-*passes* on drift); a **comment-stripped `prosrc`** assertion is sound against both, and `pg_get_functiondef()` is deliberately avoided (prosrc is the narrower, non-pretty-printed source). **Independent review caught a real runtime-SQL defect in the new verifier before commit:** the literal-extraction regex `'''([^'']*)''` had **7 single-quotes** — an unbalanced SQL string that would error at PostgreSQL execution but is invisible to the offline validator (which computes literals in JS). Rewritten with dollar-quoting `$lit$'([^']*)'$lit$`; all ten body patterns were then re-verified against the real normalised prosrc (6 positives match, every negative absent, literal set exact). **Validator** `validate-qf-mvp-20-3d.mjs` (`db945481…`) **110/110** — now grades the migration (`evaluateDMigration`, 21 rules), **the verifier** (`evaluateDVerifier`, rules V01-V08, 12 fixtures: COMMENT-only proof, body inspection removed, `pg_get_functiondef`, prosrc-in-a-comment, stripping removed, any required row deleted, negative on raw prosrc, verifier DML), and an **offline installed-body simulation** (5/5 drift mutations caught incl. the 3 the old proof false-passed; a comment naming admin does **not** false-fail). **Verifier** `verify_qf_mvp_20_3d.sql` (`f0e87bfb…`) **37 rows**, SELECT-only, still never inserts a test auth user. **Migration D unchanged** (`8fb3c28c…`) and the DR1 runtime marker unchanged. **Gates:** D **110/110**, C **83/83**, B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **No database contacted, no dry run, nothing applied, nothing pushed.** Record: [`QF-MVP-20-3D-AUTH-USER-ONBOARDING-TRIGGER.md`](QF-MVP-20-3D-AUTH-USER-ONBOARDING-TRIGGER.md).

Next: **QF-MVP-20.3D staging preflight (rerun) — not application**, and only after acceptance and pushing all three local D commits (the preflight's origin-sync gate requires it; DVR1 was forbidden to push). The verifier source-proof gate that stopped DP is now satisfied by rows 27 + 29-37. Migrations A, A2, B1, G, B2 and C are applied and immutable.
- **20.3DR1 Auth principal-classification correction** — **`D_PRINCIPAL_CLASSIFICATION_CORRECTED_REVIEWED_READY_FOR_PREFLIGHT`**. **Why `e84b209` was not accepted for preflight:** DG replaced the metadata-derived role with the blanket constant `'vendor'`, which closed the escalation but introduced the opposite defect — **principal misclassification**. `auth.users` is **not a vendor-only table**: `services/clientOtpAuthService.ts` is explicitly the homeowner/client auth service, and `requestClientWhatsappOtp()` calls the **anon** SSR client's `signInWithOtp({ phone })` with first-time user creation left at its default, supplying **no vendor marker**, after which `verifyClientWhatsappOtp()` provisions `public.client_accounts`. A blanket `'vendor'` would therefore have stamped **every homeowner as a vendor profile**, and DG's “only the attack changes” claim is **retracted**. **Frozen contract:** the trigger still creates exactly one `profiles` row per new auth user (required — `vendors.user_id` FKs `public.profiles(id)`), but the **role is classified solely from the server-set Auth `app_metadata` key `qf_principal`** (read as `raw_app_meta_data`): exactly `'vendor'` → `'vendor'`, **everything else → `NULL` (neutral, unprivileged)**, and **`'admin'` is unreachable — no branch can produce it**. **Why the marker is trusted:** `signUp({options:{data}})` and `updateUser({data})` write **user_metadata only**; app_metadata is settable exclusively through the service-role Admin API, and the validator proves in-repo that no `"use client"` module writes app_metadata or holds the service-role key, and no runtime path puts the marker in user_metadata. **Why NULL is legal:** `profiles.role` has no NOT NULL and `profiles_role_check` is satisfied by a NULL-valued expression — asserted as a catalog fact in the migration and by verifier row **D26**. **Why neutral is safe:** `is_admin()` matches `role='admin'` only, `owns_vendor()` reads `vendors.user_id` and never `profiles.role`, and no policy/function/query anywhere filters on `role='vendor'` — so neutral and vendor confer identical (zero) privilege. **Outcomes:** server-created vendor → `'vendor'`; first-time client OTP → **NULL**; admin bootstrap → NULL then `grant-superadmin.mjs`'s own service-role upsert → `'admin'`; forged `{"role":"admin"}` or `{"qf_principal":"vendor"}` in user_metadata → **NULL**. **Runtime (minimal):** new pure `lib/identity/authPrincipalMarker.ts`; `submitVendorAccountRegistration` now passes `app_metadata: vendorPrincipalAppMetadata()` and **drops the untrusted `role` from user_metadata** (cleanup/`deleteUser` unchanged); `grant-superadmin.mjs` drops its inert metadata role. **Validator** `validate-qf-mvp-20-3d.mjs` (`a532aaac…`) **76/76** with **23 one-defect fixtures** — new rules **R19** (rejects an unconditional constant role, a role expression reading `raw_user_meta_data`, a missing marker gate, or a missing neutral default) and **R20** (rejects the client-writable namespace and any other app_metadata key); **9/9 focused client/vendor/admin scenarios** graded by a classifier **derived from the migration text**; **5/5 runtime↔trigger marker-divergence mutations** caught. **SELECT-only verifier** `verify_qf_mvp_20_3d.sql` (`2ca1e312…`) **28 rows** (+D26 role nullable, +D27 trusted source declared via the function **COMMENT** object — not source text, since `prosrc` retains the body's own comments and a negative lexical assertion would false-FAIL — and +D28 client principal model intact); still **never inserts a test auth user**. **Migration** `8fb3c28c…`. **Pre-existing issues investigated and classified, NOT changed:** `profiles` has **no table GRANT for `authenticated`** (confirmed — baseline revokes and nothing grants back, so browser/SSR reads in LoginForm/AdminLoginForm/`currentUser()` cannot succeed) → **SEPARATE_NON_BLOCKING_FOLLOW-UP**: D writes with SECURITY DEFINER owner rights and `is_admin()`/`owns_vendor()` are likewise definer, so D neither causes nor depends on it; and `profiles.admin_role` drift → **SEPARATE_NON_BLOCKING_FOLLOW-UP (inert)** — nothing reads that column. **Gates:** D **76/76**, C **83/83**, B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0, typecheck/lint/build clean, `git diff --check` exit 0. **No database contacted, no dry run, nothing applied, nothing pushed.** Record: [`QF-MVP-20-3D-AUTH-USER-ONBOARDING-TRIGGER.md`](QF-MVP-20-3D-AUTH-USER-ONBOARDING-TRIGGER.md).

Next: **QF-MVP-20.3D staging preflight — not application**, and only after this correction is accepted and **both** local D commits are pushed (the preflight's origin-sync gate requires it; DR1 was forbidden to push). Then D application → E → QF-MVP-20.4. Migrations A, A2, B1, G, B2 and C are applied and immutable. Never edit an applied migration, never `migration repair`, never `db reset`.
- **20.3DG Auth-user onboarding trigger (generate/review)** — **`D_AUTH_TRIGGER_GENERATED_REVIEWED_READY_FOR_PREFLIGHT`**. Generated at the synchronized HEAD `7161eea6…` (origin identical, 0/0, clean; no collision for `20260723000700`; C was the highest migration). **No database contacted, no dry run, nothing pushed.** **Why D exists:** the original contract (`20260620000003_functions.sql`) created BOTH `public.handle_new_user()` and the trigger `on_auth_user_created` on `auth.users`; the QF-MVP-20.2 staging baseline captured the **function** but **not the trigger**, because a trigger on `auth.users` lives in the managed `auth` schema that a public-schema dump excludes. Without it staging is badly broken: `profiles` has **no INSERT policy for any role** and **no application code writes it**, so the trigger is its sole writer; `is_admin()` reads `profiles` and ~all RLS policies call `is_admin()`; and `vendors.user_id` FKs **`public.profiles(id)`**, so account-linked vendor signup would fail with an FK violation. **The defect D closes:** the original function set `role` from `coalesce(new.raw_user_meta_data->>'role','vendor')`. `raw_user_meta_data` comes verbatim from the client-supplied `options.data` of a public `POST /auth/v1/signup`, and `profiles.role` permits `'admin'` — so re-attaching the trigger unchanged would let **any anonymous visitor self-register as an administrator** and unlock every `is_admin()` policy. D therefore hardens the function forward-only: **role is a trusted constant `'vendor'`, never metadata**; only the two non-privileged display keys `full_name` and `phone` are read, each by an explicit single `->>` (never a wholesale JSON copy). **Behavioural equivalence proved:** every legitimate path yields an identical final state (vendor signup passes `role:'vendor'`; phone-OTP signup passes none; `grant-superadmin.mjs` explicitly upserts `role='admin'` as its own step) — **only the attack changes**. **Design:** `AFTER INSERT ON auth.users FOR EACH ROW` (tgtype **5**) — AFTER is mandatory because `profiles.id` FKs `auth.users(id)`; INSERT-only so password resets never re-run onboarding; `ON CONFLICT (id) DO NOTHING` so it is idempotent and never overwrites; SECURITY DEFINER (required — the auth service holds no rights on `profiles`, which has RLS on) with `search_path` pinning `pg_catalog` and `pg_temp` last; EXECUTE revoked from PUBLIC/anon/authenticated, service_role retained; **no backfill** of existing auth users; signup and initialisation are atomic (a failed init rolls the auth user back). **No runtime change was required, and it is proved**: no module inserts/upserts `profiles` (no race), none requests an admin role in signup metadata, and no `"use client"` module holds the service-role key. **Validator** `validate-qf-mvp-20-3d.mjs` (`a3a7f130…`) **50/50** with **19 one-defect fixtures**; **three real-artifact mutations** all caught — reintroducing the metadata role tripped **R08+R09**, `BEFORE INSERT` tripped R07, and adding `insert into auth.users` to the verifier tripped 07/07b — with every artifact restored byte-identical. **SELECT-only verifier** `verify_qf_mvp_20_3d.sql` (`52d2384a…`) **25 rows**, proving the trigger **structurally from catalog facts** and **never inserting a test auth user**. **Migration** `16697efc…`. **Two pre-existing issues disclosed but NOT changed** (out of D scope): `profiles` has no table GRANT for `authenticated` despite browser reads in LoginForm/AdminLoginForm, and the `profiles.admin_role` column drifted between migration `20260621000006` and the production-derived baseline. **Gates:** D **50/50**, C **83/83**, B2 **61/61**, B1/G **165/165**, R1 **62/62**, `verify:mvp` exit 0 (now also runs the D validator), typecheck/lint/build clean, `git diff --check` exit 0. Record: [`QF-MVP-20-3D-AUTH-USER-ONBOARDING-TRIGGER.md`](QF-MVP-20-3D-AUTH-USER-ONBOARDING-TRIGGER.md).

Next: **QF-MVP-20.3D staging preflight — not application.** D is generated and reviewed only; nothing has been applied and no dry run has been executed. Then D application → E → QF-MVP-20.4. Migrations A, A2, B1, G, B2 and C are applied and immutable. Never edit an applied migration, never `migration repair`, never `db reset`.

### Previous active phase — QF-MVP-10 (COMPLETE)
**QF-MVP-10 — Core Architecture and Data Truth** — **`COMPLETE`**. Evidence-based repository + migration map produced (six QF-MVP-10 docs + generated JSON), and the **read-only production reconciliation is executed** (22 July 2026). Access mode: the connection was **not** technically read-only (role `postgres`, `transaction_read_only = off`); read-only behaviour was **process-enforced through an explicit SELECT-only allowlist** under founder approval (`APPROVE SELECT-ONLY PRODUCTION RECONCILIATION. STAGING_NOT_PROVISIONED. NO DATABASE CHANGES.`). No database change, migration application or provider access occurred. Production materially drifts from the repository ledger (**`HISTORY_DRIFT`**: 4 recorded migration-history rows vs 68 repository migrations); an unrecorded version does **not** prove absent objects. Results: [`QF-MVP-10-RECONCILIATION-RESULTS.md`](QF-MVP-10-RECONCILIATION-RESULTS.md).

**QF-MVP-10 subphase status:**
- **10.1 Read-only inventory tooling** — `COMPLETE`. `scripts/mvp/inventory/**` + `npm run inventory:mvp` → byte-stable `docs/generated/qf-mvp-runtime-inventory.json` + `qf-mvp-migration-ledger.json` (no DB/network/secrets; repo-relative; identical across runs).
- **10.2 Runtime inventory** — `COMPLETE`. 31 API routes · 23 pages · 64 services · 19 provider adapters · 283 lib modules · workers/cron · 51 scripts, each with activation status (EXISTS/WIRED/CONFIGURED/DEPLOYED/ACTIVE).
- **10.3 Migration ledger** — `COMPLETE`. 68 migrations; 100 tables / 50 functions / 2 triggers / 0 enums / 77 policies / 89 RLS tables; **12 `DO-NOT-AUTO-APPLY` migrations** flagged; every `staging`/`production` = `UNKNOWN_UNVERIFIED`; verification SQL recorded.
- **10.4 Core domain map** — `COMPLETE`. 14 domains + AOS; dependency direction; 8 evidence-backed boundary violations (no active bypass).
- **10.5 Authority audit** — `COMPLETE`. 16 authorities. **No BLOCKER**; 4 HIGH (eligibility duplication · no 6-lifetime cap · live credit-RPC body unverified · un-ledgered `admin_smart_assign` debit).
- **10.6 Cleanup plan** — `COMPLETE`. A–E buckets; deletion is not an MVP blocker; nothing deleted; DB-proof prerequisites recorded.
- **10.7 Database reconciliation** — **`COMPLETE` (executed 22 July 2026).** Production inspected through the connected Supabase integration under a founder-approved, **process-enforced SELECT-only** mode (connection **not** technically read-only — role `postgres`, `transaction_read_only = off`; behaviour constrained by an explicit SELECT-only allowlist). No DB change / migration / provider access. Findings: **`HISTORY_DRIFT`** (4 history rows vs 68 repo migrations, exact records in the ledger); 62 base tables / 39 functions / 33 SECURITY DEFINER; **4 assignment RPCs PUBLIC/anon-executable = BLOCKER**; canonical service_role RPCs lack the lifetime-six rule; 27/46 credit-deducted assignments lack ledger evidence; anon `SELECT` on vendor monetization columns = **HIGH**; Meta correctly inactive but ack `provider_account_id` nullable = **`QF-MVP-40_BLOCKER`**. Results in [`QF-MVP-10-RECONCILIATION-RESULTS.md`](QF-MVP-10-RECONCILIATION-RESULTS.md).
- **10.8 Execution order** — `COMPLETE`. Order 20→40→(30)→50→70→(60)→80; QF-MVP-20 opens with authority repair (20.A–20.E). QF-MVP-10 is now **COMPLETE**.

**Locked founder decisions (QF-MVP-10.7):** active cap **3**; **lifetime unique-vendor cap = 6** (the existing **9 is rejected**, corrected in QF-MVP-20); credit restoration requires founder/authorized-admin approval; **every credit mutation requires an audit ledger row**; public vendor profiles must not expose package/plan/credit/monetization; **AOS dormant scaffolding = KEEP_DISABLED**; **Jarvis (QF-MVP-60) = MVP_REQUIRED** (recommendation-only), not optional.

**Key facts established:** AOS holds no authority + **no AI active** + **no Jarvis in-repo**; **Meta non-voice only + gated OFF by DB seed**; consent has a single decision/writer/enforcer. **Production reconciliation added the critical correction:** four SECURITY DEFINER assignment RPCs are **PUBLIC/anon-executable with no in-body authorization** — an active bypass that repository-only evidence could not see — now the QF-MVP-20 20.A repair. **Reconciliation is executed; QF-MVP-10 is COMPLETE.**

---

### Previous phase (COMPLETE)
**QF-MVP-00 — Program Lock and Clean Baseline** — **`COMPLETE`**. All exit gates green; full details in [`QF-MVP-00-BASELINE.md`](QF-MVP-00-BASELINE.md).

**Subphase status:**
- **QF-MVP-00.1 Baseline verification + test-command inventory** — `COMPLETE`. Branch/HEAD/parent/ancestry/tracking verified; all `test:*` commands classified from **behavioral** inspection (not names). Finding: the 38 `scripts/phase*` harnesses vendor/inline logic or static-source-match, embed a Git working-tree/commit attestation, and several perform **real write-then-revert source mutation** (`phase5d`, `phase5f:d2c`) or shell out to **`psql`** (`phase1b:runtime`) — all `LEGACY_NON_BLOCKING`, kept on disk. `test:phase*` totals: **35** (34 `LEGACY_NON_BLOCKING` + 1 `DATABASE_INTEGRATION`).
- **QF-MVP-00.2 Focused MVP runner** — `COMPLETE`. New `scripts/mvp/**` runs offline against **real** production modules (no Git/mutation/DB/network/secrets); `marketplace` (16) + `communication` (24) = **40 checks passing**.
- **QF-MVP-00.3 Package commands** — `COMPLETE`. Added `test:mvp`, `test:mvp:marketplace`, `test:mvp:communication`, `verify:mvp`; all historical commands preserved; `test:supabase:lead` and all mutation/governance harnesses excluded from the MVP gate.
- **QF-MVP-00.4 Execution board update** — `COMPLETE`.
- **QF-MVP-00.5 Configure & close the lint gate** — `COMPLETE`. Installed `eslint@8.57.0` + `eslint-config-next@14.2.15` (exact, dev); added `.eslintrc.json` (`next/core-web-vitals`); `next lint` runs non-interactively and passes (exit 0). Two pre-existing `react/no-unescaped-entities` **errors** were surfaced and fixed with authorization (single-character apostrophe escapes in `VendorNoProfileFallback.tsx`, `LeadFunnel.tsx`); 5 non-blocking warnings remain. No Next/React/TS upgrade; `core-web-vitals` not weakened; no suppressions/ignores.

**Completed deliverables:** clean-baseline verification record; full test-command inventory + classification; `scripts/mvp/` runner + two suites + minimal `.ts` resolver + README; four MVP package scripts; ESLint toolchain + `.eslintrc.json`; `docs/QF-MVP-00-BASELINE.md`. Governance is de-blocked: **no MVP gate invokes any legacy governance harness**, and no production code was changed to satisfy a stale pin.

**Final gate results:** `test:mvp` ✅ (40/40) · `typecheck` ✅ · `lint` ✅ (non-interactive) · `build` ✅ · `verify:mvp` ✅ end-to-end · `git diff --check` ✅ · legacy governance non-blocking ✅.

**Deferred coverage (not a QF-MVP-00 blocker):** launch-critical behaviour with no safe pure seam is logged `FOCUSED_TEST_REQUIRED` in the baseline doc (assignment/credit-deduction idempotency, 6-vendor lifetime cap, replacement concurrency, stateful consent decision/writer, outbound enforcement, async ack persistence) → owned by QF-MVP-20 / QF-MVP-40.

## 3. Phase table

| Phase | Status | Depends on | Est. effort | Primary deliverable | Exit gate |
|---|---|---|---|---|---|
| QF-MVP-00 Program Lock & Clean Baseline | **COMPLETE** | — | 1–2 d | Clean branch, governance de-blocked, `verify:mvp`, green build | ✅ Focused MVP tests (40/40) + typecheck + **lint** + build + `verify:mvp` end-to-end; governance non-blocking |
| QF-MVP-10 Core Architecture & Data Truth | **COMPLETE** | 00 | 2–3 d | Runtime + DB inventory, Migration Ledger, ownership map, cleanup classification | ✅ Every route/service/migration classified + ownership map + authority audit + **production SELECT-only reconciliation executed (22 Jul 2026)** |
| QF-MVP-20 Marketplace Transaction Engine | **IN_PROGRESS** (20.0 design) | 10 | 3–5 d | Lead→qualify→eligible→assign→replace→close, deterministic + audited | Limits unbypassable; credit deduction idempotent; replacement concurrency-safe; no AI scoring |
| QF-MVP-30 Vendor CRM & Campaign Readiness | NOT_STARTED | 10, 20 | 4–6 d | CRM directory/profile/tasks/segments + consent-safe campaigns | Segments deterministic; campaigns cannot bypass consent; snapshots auditable |
| QF-MVP-40 Meta WhatsApp Production Readiness | NOT_STARTED | 10 (20 for content) | 4–6 d | Non-voice Meta inbound/outbound/delivery/consent activated on staging | Staging webhook verified; foreign callback zero-effect; STOP/START/HELP correct; no voice path |
| QF-MVP-50 n8n Workflow Automation | NOT_STARTED | 20, 40 | 3–5 d | Core→n8n→Meta execution, idempotent, Jarvis-independent | Duplicate events don't double-act; uncertain outcomes not resent; pausable; auditable |
| QF-MVP-60 Jarvis Agent Integration | NOT_STARTED | 50, 70 | 4–6 d | Sanitized context + action-request API + Riya/Anisha + kill switches | No direct DB authority; unauthorized requests rejected; Core runs Jarvis-offline |
| QF-MVP-70 Operations & Launch Control | NOT_STARTED | 20, 40, 50 | 2–3 d | Admin queues, controls, kill switches, KPIs | No failure hidden; sensitive corrections require approval; KPIs visible |
| QF-MVP-80 Staging, Canary & Pune Launch | NOT_STARTED | 20, 30, 40, 50, 70 | 2–3 d + approvals | Migration rehearsal, journeys, canaries, gated launch | All journeys + canaries pass; rollback verified; Jarvis disableable; Pune approved |

## 4. Critical path
`QF-MVP-00 → 10 → 20 → 40 → 50 → 70 → 80`.
This is the minimum spine to launch. QF-MVP-30 (CRM) and QF-MVP-60 (Jarvis) hang off the spine but are not strictly on the launch-blocking critical path for a **transactional** Pune launch — however **QF-MVP-30 is MVP_REQUIRED** per the locked scope, so it must complete before "MVP done" even though a bare transactional launch could precede full CRM campaigns. QF-MVP-60 is optional for launch (Core must run with Jarvis offline).

## 5. Parallelizable work
- **After 10 completes:** QF-MVP-20 (marketplace) and QF-MVP-40 (Meta verification/activation) can proceed largely in parallel — 20 owns business logic, 40 owns transport; they meet at template content.
- **QF-MVP-30 (CRM)** data model + directory can start once 10's vendor ownership map is done, in parallel with 20's later subphases.
- **QF-MVP-70 (ops)** queues/controls can be scaffolded in parallel with 50, then wired as 20/40/50 land.
- **QF-MVP-60 (Jarvis)** context API can be specced in parallel but must not integrate until 50/70 exist.
- Non-parallel: 50 needs 20+40; 80 needs everything upstream.

## 6. Launch blockers (must be GREEN before Pune launch)
1. QF-MVP-20 marketplace flow (limits, idempotent credits, safe replacement, auditable closure).
2. QF-MVP-40 Meta non-voice: signed inbound accepted, foreign callback zero-effect, template send + delivery lifecycle, STOP/START/HELP, no voice path.
3. QF-MVP-50 n8n: idempotent execution, uncertain-outcome-not-resent, pausable, auditable.
4. QF-MVP-70 controls: Meta sending pause + campaign pause + kill switches + approval gates for credit restoration/campaign.
5. QF-MVP-80 migration rehearsal + rollback verified; both staging journeys pass; Meta + n8n canaries pass; Jarvis disableable.
6. Consent/suppression inviolable across every path (cross-cutting).
7. QF-MVP-30 CRM (MVP_REQUIRED) for campaign readiness — blocks "MVP complete", and blocks any CRM-campaign launch specifically.

## 7. External dependencies (outside engineering control — plan lead time)
- Meta/WhatsApp: WABA + phone-number approval, **template approvals**, app-secret/webhook config.
- Hosting/deploy target for Core + webhook URL (public HTTPS).
- n8n runtime environment.
- Jarvis repository availability (optional for launch).
- Founder/admin sign-off gates: campaign approval, credit-restoration approval, Pune vendor activation, launch go/no-go.

## 8. Deferred backlog (POST_MVP — do not build now)
AI lead scoring · AI vendor ranking · predictive conversion scoring · Jarvis-controlled assignment · AI package priority · Meta voice calling · WhatsApp voice agents · voice campaigns / recording / transcription · autonomous package changes · autonomous credit changes · autonomous vendor suspension · advanced analytics warehouse · mobile apps · microservice conversion · event sourcing · RCS campaigns. (KEEP_AS_BUILT, not deferred: SMS fallback, multi-provider, advanced retry/fallback, multi-city.)

## 9. Decision log (locked)
| # | Decision | Rationale |
|---|---|---|
| D1 | QuickFurno Core is the single authoritative business system; one business DB; modular monolith | Prevents split-brain authority and duplicate data |
| D2 | Vendor CRM is an internal module using extension tables; never duplicates Core facts | Keeps one source of truth for verification/package/credits/consent/eligibility |
| D3 | Jarvis stays a separate repo; recommends only; no service-role key, no direct Core writes/sends/approvals; Core runs Jarvis-offline | Safety + availability |
| D4 | n8n executes only Core-authorized actions; never an authority | Prevents automation becoming a business-rule owner |
| D5 | Meta non-voice only; voice explicitly excluded | Scope discipline |
| D6 | MVP marketplace is deterministic; no AI scoring/ranking/assignment | Explainability + correctness |
| D7 | SMS fallback, multi-provider, advanced retry, multi-city = KEEP_AS_BUILT; Pune first | Retain value without new MVP work |
| D8 | Legacy phase-governance is LEGACY_NON_BLOCKING; production safety still mandatory; no code changed to satisfy stale pins | Unblock delivery without lowering safety |
| D9 | Uncertain communication outcomes are terminal and never auto-resent | Prevents duplicate/erroneous sends |
| D10 | Assignment caps: 3 active / 6 lifetime; credit deduction idempotent; restoration needs approval | Hard business invariants |

## 10. Phase completion checklist (apply to every phase before marking COMPLETE)
- [ ] All subphase deliverables produced and reviewed.
- [ ] Phase exit criteria objectively met (tests/evidence attached).
- [ ] `verify:mvp` (typecheck + lint + build + focused MVP tests) green.
- [ ] No production code changed merely to satisfy a legacy governance pin.
- [ ] Migration-safety review done **iff** SQL changed; no unsafe managed-DB commands.
- [ ] No unintended DB/provider access during ordinary validation.
- [ ] Ownership boundaries respected (Core authority; Jarvis recommends; n8n executes; Meta delivers; results return to Core).
- [ ] Audit records exist for every important action introduced.
- [ ] Kill switches / pause controls exist for any new automated path.
- [ ] Execution board updated (status, blockers, decisions).

---

**Reminder:** QF-MVP-00 is **COMPLETE**. QF-MVP-10 is **COMPLETE** — the evidence-based Core map, runtime inventory, migration ledger, authority audit, cleanup plan, DB-reconciliation procedure, **and the executed production SELECT-only reconciliation (22 July 2026)** are all done. The reconciliation ran through the connected Supabase integration under a founder-approved, process-enforced SELECT-only allowlist (the connection was not technically read-only); no database change, migration application or provider access occurred. Six QF-MVP-10 docs + `docs/generated/*.json` are the authoritative map; the four locked docs (`QF-MVP-LOCKED-ROADMAP.md`, `QF-MVP-SCOPE-MATRIX.md`, `QF-MVP-ARCHITECTURE-BOUNDARIES.md`, this board) remain the governing inputs. **QF-MVP-20 is `NOT_STARTED` and opens with authority repair (20.A–20.E); applying that repair is gated on staging provisioning (`OPEN_LAUNCH_PREREQUISITE`).**
